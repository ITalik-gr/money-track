# Money Track — Дизайн-система (живий документ)

> **Для Claude:** читай цей файл ПЕРШИМ перед будь-якою роботою над UI/UX.
> Тут — джерело правди для візуалу: токени, патерни, рішення та референси.
> Код-реалізація токенів живе в `src/index.css`. Якщо цей файл і CSS розходяться —
> цей файл описує НАМІР, CSS треба підтягнути до нього (або оновити цей файл, якщо
> намір змінився свідомо). Кожну зміну дизайну фіксуй у розділі «Журнал рішень».

> **Дата останнього оновлення:** 2026-07-11 (§10.1–10.4 виконано: картки flat+radius,
> ink-кнопки, графіки де-слоп, анімації. Лишилось §10.5-крок 5 — сторінки Статистика/Транзакції)

---

## 0. Як користуватись цим файлом (робочий процес)

1. Користувач кидає **скріншот** красивого дашборда-референсу + опис фічі.
2. Claude **детально аналізує** скрін (розділ 6 — шаблон аналізу) і ставить
   уточнювальні питання, ЯКЩО є неоднозначність.
3. Узгоджене рішення записується в:
   - **розділ 5 «Референси»** — що саме надихнуло і що беремо/не беремо;
   - відповідний розділ токенів/патернів, якщо змінюємо систему;
   - **розділ 8 «Журнал рішень»** — короткий запис «що і чому».
4. Лише після цього — правки в коді (`src/index.css` + компоненти).

Принцип: **референс — це інгредієнт, не рецепт.** Ми не копіюємо чужий дашборд
піксель-в-піксель — витягуємо конкретний прийом (ритм, тінь, спосіб показу цифри)
і вплітаємо в наш «точний інструмент».

---

## 1. Дизайн-філософія (стартова позиція — відкрита до перегляду)

> **Режим роботи: СМІЛИВИЙ РЕДИЗАЙН.** Це ядро — не догма, а точка відліку.
> Референси можуть переписати навіть палітру й загальний характер. Якщо скрін
> дає сильнішу ідею — міняємо ядро свідомо й фіксуємо в §8. Незмінне: **чесність
> цифр** (`tabular-nums` для вирівнювання) і **чіткий поділ власні/борг**.

**🎯 НАПРЯМОК (зафіксовано 2026-07-04 після батчу #1):** світлий дашборд у стилі
DeliFin, кобальт-акцент, **гібридна типографіка**. «Точний інструмент» лишається
духом (стриманість, повітря, дані-герой), але подача — тепліша й дружніша, ближча
до референсу-бази; НЕ сувора темна-моно, як було.

- **Гроші — головний герой.** Герой-суми (баланс, KPI) — жирний **Geist sans**;
  таблиці й списки — **Geist Mono** `tabular-nums` для вирівнювання колонок.
  Скрізь `tabular-nums`, щоб цифри не «стрибали». (Рішення Q2, §8.)
- **Тихе, дисципліноване оточення.** Багато повітря, стримані кольори,
  мінімум декору. Інтерфейс не змагається з даними за увагу.
- **Сигнатурний елемент — картки-рахунки** (матові «фізичні» картки, без
  глянцевих градієнтів: `.acct.black`, `.acct.fop`, `.acct.jar`…).
- **Свідомо НЕ AI-дефолт.** Уникаємо фіолетових градієнтів, скляного морфізму
  заради морфізму, «типового SaaS-лендінга». Прохолодна spruce-зелень + кобальт.
- **Чесність цифр:** власні кошти й борг ніколи не змішуються (кредитний ліміт —
  §5 плану); позитив/негатив мають окремі кольори, але приглушені.

Якщо референс суперечить цьому ядру — беремо форму, відкидаємо суть, що конфліктує.

---

## 2. Токени (джерело правди — `src/index.css`)

### Кольори — світла тема
| Токен | Значення | Роль |
|---|---|---|
| `--bg` | `#f2f5f3` | тло застосунку (прохолодний off-white) |
| `--surface` | `#ffffff` | картки, панелі |
| `--surface-2` | `#eaeeeb` | вкладені/hover-поверхні, сегменти |
| `--ink` | `#13201b` | основний текст |
| `--ink-2` | `#33443c` | вторинний текст |
| `--muted` | `#5f6b79` | підписи, метадані (⬆ затемнено до AA ≥4.5:1 на surface — 2026-07-11) |
| `--line` | `#e2e7e4` | межі |
| `--line-strong` | `#d3dad5` | інпути, сильніші межі |
| `--accent` | `#2e6be6` | кобальт — акції, активний стан |
| `--accent-soft` | `#eaf1fd` | тло активного/акцентного |
| `--pos` | `#12805c` | дохід/плюс |
| `--neg` | `#d24430` | витрата/мінус/борг |

### Кольори категорій (конверти)
`--c-pine #1f6e4c` · `--c-cobalt #2e6be6` · `--c-plum #7a3e9d` ·
`--c-ochre #c9871a` · `--c-brick #b23a2e` · `--c-teal #127c86`

> Темна тема — дзеркало в `@media (prefers-color-scheme: dark)` + ручний
> `data-theme` оверрайд. Обидві теми — рівноправні, не «світла з інверсією».

### Типографіка (УНІФІКОВАНО — рішення 2026-07-11, замінює гібрид Q2)
> **Одна родина.** Product-UI не потребує пари display/body. Geist Sans несе заголовки,
> кнопки, лейбли, тіло І **всі числа** (через власні tabular-фігури). Прибрано шов sans↔mono.
- **Числа (`--font-num` = `--font-sans`):** усі суми — герой і колонки — Geist Sans
  з `font-variant-numeric: tabular-nums` + `"tnum" 1`. Клас `.money` та інлайн number-класи.
- **Mono (`--font-mono`) — ЛИШЕ для кодів:** PAN (`··4932`), MCC, вебхук-сегменти, ID,
  логотип-марка бренду. Ніколи для грошей. Класи `.mono`, `.acct-pan`, `.m-card`.
- **Лейбли — sans sentence-case** (не UPPERCASE-mono; фідбек #2). Bottom-nav теж sans.
- **Фіксована rem-шкала (не fluid):** product-UI дивляться при сталому DPI, тож `clamp()`
  для заголовків прибрано. Токени `--fs-xs…--fs-4xl`; герой-числа — фікс. rem із bump
  на `@media (min-width:640px)` (структурна адаптивність, не текучий шрифт).

### Радіуси, тінь, рух, простір (шкали — 2026-07-11)
- **Радіус:** `--radius-xs 6px` · `--radius-sm 10px` · `--radius 14px` · `--radius-lg 18px` ·
  `--radius-xl 22px` · `--radius-pill 999px`.
- **Простір (4pt):** `--sp-1 4` … `--sp-8 32`.
- **Тип-шкала:** `--fs-xs 11` … `--fs-4xl 36` (fixed rem).
- **Тінь — одна модель:** `--shadow-sm` / `--shadow` / `--shadow-card`, низькі й прохолодні
  (rgba slate). Ніколи «жирні дропшедоу».
- **Рух:** `--ease-out` / `--ease-std`; `--dur-1 120ms` · `--dur-2 180ms` · `--dur-3 240ms`.
  Product-правило: 120–240ms, лише ease-out, рух = стан (не декор). `prefers-reduced-motion` — глобально.
- `--sidebar-w 248px` · `--content-w 1240px` (desktop dashboard-grid).

---

## 3. Каркас застосунку (layout)

> **Пріоритет: DESKTOP-FIRST.** Головний сценарій — широкий екран/дашборд.
> Мобілка лишається робочою (PWA), але дизайнимо від десктопа й адаптуємо вниз.

- **Desktop (головний):** ліва sticky-сайдбар (244px) + робоча зона.
  Референси — десктопні дашборди, тож тут з'явиться повноцінний dashboard-grid
  (кілька колонок, віджети-плитки, графіки поруч) — **не** вузька колонка 880px.
  ⚠️ Це перегляд поточного `--content-w 880px` → ймовірно розширюємо/робимо
  багатоколонкову сітку. Рішення зафіксувати в §8 після перших референсів.
- **Mobile:** нижня таб-навігація (`.nav.bottom`), одна колонка, safe-area.
  Дашборд-віджети стакаються вертикально.
- **Точки перелому:** узгодити (напр. ≥1200px повна сітка, 900–1200 звужена,
  <900 мобільний стек) — деталі після першого дашборд-референсу.

---

## 4. Каталог компонентів (що вже існує)

| Компонент | Файл | Роль |
|---|---|---|
| Summary hero | `SummaryHeader.tsx` | велика сума-разом + розбивка по валютах |
| Account cards | `Accounts.tsx` / `.acct` | сигнатурні картки-рахунки |
| Envelopes | `EnvelopeGrid.tsx` | категорії-конверти з progress-fill |
| Credit banner | `CreditBanner.tsx` | власні vs борг (§5) |
| AI insight | `Insight.tsx` | тижневий інсайт (teal-акцент) |
| Transaction list | `TransactionList.tsx` / `.tx` | реєстр операцій |
| Stats | `Stats.tsx` | KPI-плитки, розбивка категорій, ранкінг |
| Advisor | `Advisor.tsx` | runway-метрики + поради |

Сторінки: Dashboard, Transactions, Add, Stats, Accounts, Plan, Advisor,
TxDetail, Setup, Login.

### Патерни, які вже усталені
- **Сегмент-контрол** `.seg` (перемикач періоду) — пігулка з активним станом.
- **Чипи-фільтри** `.chip` — горизонтальний скрол, крапка-колір категорії.
- **Ранкінг-рядок** `.rank` — назва + сума + фонова смуга-пропорція.
- **Модалка** — bottom-sheet на мобілці, центр-діалог на десктопі.
- **Envelope fill** — заливка знизу вгору як індикатор витрачено/бюджет.

---

## 5. Референси (наповнюється зі скрінів)

> Формат запису кожного референсу — нижче.
> **Батч #1 надіслано 2026-07-04** (7 скрінів). Скріни 4–6 — поточний стан
> самого Money Track (baseline, який покращуємо), решта — зовнішні референси.

### R1 — DeliFin ⭐ БАЗА / головний напрямок
- **Контекст:** світлий фінтех-дашборд. Користувач: «просто подобається, це буде як база».
- **Що чіпляє:**
  - Каркас: sidebar (іконка+лейбл, згруповане меню Menu / Help&Settings, Dark-Mode
    тумблер унизу) + topbar (заголовок сторінки · search · notif · avatar з роллю+дропдаун).
  - **Асиметрична 2-колонкова сітка** робочої зони (ліва вужча, права ширша) — НЕ вузька
    центр-колонка. Це і є desktop-first dashboard-grid із §3.
  - Привітання «Welcome, Jerry 🔥» + date-picker + синя `Export` кнопка у правому куті.
  - **Balance-картка:** підпис+ⓘ → величезне число → зелений pill `+16.8%` → номер картки
    + `Copy` → ряд дій (Transfer[синя] / Received / `+`).
  - **KPI-плитка** (Monthly Spent/Income): підпис+ⓘ+date-pill → число → дельта-pill
    (зел.↑ / черв.↓) + «Compared to last month». ← «плитки з текстом», які подобаються.
  - **Cashflow-графік:** 2 згладжені лінії Expense(синя)/Income(амбер), ледь-помітна
    area-заливка, чорний tooltip-pill зі значенням, місячна вісь. ⭐ ← еталон для Статистики.
  - Радіуси 16–20px, м'які холодні тіні, hairline-межі, багато повітря.
- **Беремо:** весь каркас (sidebar+topbar), асим. grid, патерн KPI-плитки, dual-line cashflow,
  balance-картку з quick-actions, зелено/червоні дельта-pill'и.
- **Кобальт-акцент збігається з нашим `--accent #2e6be6`** — велика удача, палітра «рідна».
- **Адаптуємо:** DeliFin світлий і числа НЕ-моно — це конфлікт із поточним «mono/dark»
  ядром (§1). Напрямок теми та шрифту грошей — див. §9 Q1/Q2, рішення в §8.
- **НЕ беремо:** Quick-transfer з аватарами людей (у нас особистий трекер, не P2P-банк).
- **Статус:** ПРИЙНЯТО як база каркасу; тема/шрифт — на узгодженні.

### R2 — Lefstyle (настрій, точкові прийоми)
- **Контекст:** земляний, преміальний. Користувач: «просто прикольно, не зациклюйся».
- **Беремо точково:**
  - **AI-Insight картка** — темне коло зі спарклом, «Generated insights…», дві під-картки
    (Money alert / Advice) зі стрілкою-CTA, гра жирний/приглушений текст. ← апгрейд нашого `Insight`.
  - **Gauge-кільце «Financial health»** — дуга green→yellow→orange, велике %, статус-бейдж
    «On track». ← кандидат для Порадника/runway.
  - «Monthly financial rhythm» — великі кольорові блоки-суми з табами Income/Expense/Saving.
- **НЕ беремо:** земляну палітру (беж/лілово/хакі) — конфліктує з кобальтом; «Upgrade to Pro».
- **Статус:** AI-Insight і gauge — у беклог (§7), решта — довідково.

### R3 — Finexa (віджети/плитки + фічі бюджету й категорій)
- **Контекст:** фіолетова рамка, білий застосунок. Користувач: «віджети, плитки прикольні».
- **Беремо:**
  - **Budget Overview**-картка: прогрес-бар бюджету + бейдж стану («Almost reached» амбер /
    «On track» зел.) + «84% of budget used». ⭐ ← ідеально на наші **Конверти**.
  - **Spending by Category**: горизонтальні бари з назвою *усередині* бару + % справа. Компактно.
  - Топ-рядок: 3 картки (Total Balance з 3 вкладеними міні-KPI / Budget / Spending).
  - Категорії-бейджі (кольорові pill) у рядках транзакцій.
- **Адаптуємо:** фіолет як основний — НІ (лишаємо кобальт). Форму віджетів — так.
- **Статус:** Budget-картка + Spending-bars — прийнято в дизайн Статистики/Конвертів.

### R7 — Cazura (підписки ⭐ + транзакції)
- **Контекст:** світлий, teal/dark-green акцент. Користувач: «подобаються підписки — треба
  глибше; транзакції теж, але не точно те».
- **Беремо:**
  - ⭐ **Subscriptions-картка:** бренд-лого (YouTube/Figma/Spotify…) + назва + сума +
    «Today 08:20», кнопки `Ask AI` / `Add`. ← основа для повноцінної фічі Підписок (§7).
  - **Транзакції-таблиця:** Category(іконка+назва)/Date/Time/Amount(зел.-черв.)/Method +
    під-теги на рядку (напр. Savings · «Wedding»). Пагінація «Show 20 of 100».
  - **Expense Breakdown** — circle-packing (бульбашки) з %; кандидат поряд зі звичним donut.
- **Адаптуємо:** teal як головний акцент — ні; беремо структуру, не колір.
- **Статус:** Підписки — окрема фіча (§7). Формат рядка транзакції — вливається в редизайн списку.

### Фідбек #2 (2026-07-04) — по Огляду v1 + рев'ю решти сторінок
**Зроблено одразу (швидкі правки):**
- ❌ mono-uppercase-letterspaced лейбли — **не подобаються**. `.label` і `.side-group`
  переведено на чистий sans sentence-case (як DeliFin). Глобально.
- Транзакції: **лого мерчантів** (АТБ/BlaBlaCar/Netflix…) через clearbit + fallback;
  **бейдж фізичної картки** (чорна/біла/FOP/банка — кольоровий квадратик) у мета-рядку.
- **Прибрано focus-outline з усіх графіків** (recharts) при кліку.
- KPI-плитки: **завжди показують порівняння з минулим місяцем** (сума + дельта-pill).

**Замовлено на наступні ітерації (більші редизайни сторінок):**
- **Статистика** (R14): «мало всього». Треба +≈5 віджетів, **статистика надходжень**
  (не лише витрат), і **вшитий AI**, що коментує/показує. → F5 + F1 + F7(AI-в-дашборді).
- **Порадник** (R15): «інфи нема, нічого не каже». Більше метрик; коли AI видає інсайти —
  **красиво виділяти суми й категорії** (не сірий текст). → F6.
- **Підписки** (R16): **винести в окрему сторінку** (зараз усередині Плану), багато
  функціоналу (лого, наступне списання, сума/міс, авто-детект). Дизайн Плану — ще старий. → F3.

### R4/R5/R6 — Money Track ПОТОЧНИЙ (baseline «таке собі»)
- **Це наш застосунок зараз** (темна тема, суцільний Geist Mono). Не референс — ціль апгрейду.
- **Проблеми, названі користувачем (скрін 6 — список транзакцій):**
  - Мета-рядок `04 лип. · Кафе і ресторани · black ··4932 · HOLD` — усе однаковим сірим,
    нема візуальної ієрархії (дата/категорія/картка/статус зливаються).
  - Символ валюти ₴ замалий відносно суми.
  - Іконка транзакції — лише літера на кольоровому квадраті.
  - **Хоче: бренд-лого/фото для популярних мерчантів** (замість літери), fallback — категорія.
- **R4 (TxDetail):** усе моно, пласко, слабка ієрархія «шапка → деталі → форма».
- **Статус:** список транзакцій і TxDetail — у чергу на редизайн після узгодження напрямку.

---

## 6. Шаблон детального аналізу скріна

Коли надходить скрін, Claude розбирає його за пунктами:

1. **Загальне враження й настрій** — що це за тип екрана, яку емоцію дає.
2. **Сітка й ритм** — колонки, відступи, щільність, вирівнювання.
3. **Типографіка** — скільки розмірів, контраст заголовок/тіло, шрифт-настрій.
4. **Колір** — палітра, роль акценту, як розрізняють плюс/мінус, тло vs картка.
5. **Подача цифр** — як показано головну суму, дрібні суми, дельти, %.
6. **Компоненти** — які патерни (плитки, графіки, списки, бейджі) і як зроблені.
7. **Глибина** — тіні, межі, шари, скляність — і чи це доречно нам.
8. **Що вкрасти, що лишити** — прив'язка до §1 (наше ядро).
9. **Питання до користувача** — якщо є неоднозначність.

---

## 7. Заплановані фічі (візуальний бекап-лог)

> Сюди — фічі, які користувач описує разом зі скрінами, до їх реалізації.

### F1 — Cashflow-графік у Статистиці (дохід + витрата на одному) ⭐
Дві лінії на одному графіку: витрати й надходження, як у DeliFin (R1) / Finexa (R3).
Згладжені лінії, area-заливка, tooltip зі значенням, перемикач періоду.

### F2 — Бренд-лого / фото для транзакцій
Замість літери-аватара показувати лого популярних мерчантів (за назвою/MCC).
Fallback: іконка категорії → літера. Джерело лого — на узгодженні (§9 Q3).

### F3 — Підписки (розгорнута фіча) ⭐
Окремий розділ/картка: список рекурентних платежів (Netflix/Spotify/Anthropic…), бренд-лого,
сума, періодичність, дата наступного списання, сума/міс. Натхнення — Cazura (R7).
Авто-детект рекурентних із транзакцій — кандидат для AI-порадника ([[ai-advisor-vision]]).

### F4 — Budget/Envelope-картки зі станом
Прогрес-бар + бейдж стану («в межах» / «майже вичерпано» / «перевищено») + «X% використано».
Форма з Finexa (R3), лягає на існуючі Конверти.

### F5 — Spending-by-category бари + Expense Breakdown
Горизонтальні бари (назва в барі + %) — Finexa. Опційно bubble/donut — Cazura.

### F6 — AI-Insight картка (апгрейд) + Financial-health gauge
Insight у стилі Lefstyle (R2): спаркл-іконка, alert/advice під-картки. Gauge-кільце для
здоров'я фінансів / runway у Пораднику.

---

## 8. Журнал рішень

| Дата | Рішення | Причина |
|---|---|---|
| 2026-07-04 | Створено DESIGN.md як живий документ дизайну | Починаємо цілеспрямовану роботу над візуалом; потрібне єдине джерело правди й пам'ять між сесіями |
| 2026-07-04 | Режим = **сміливий редизайн** (§1 — не догма) | Користувач готовий переписувати навіть палітру/характер під сильні референси |
| 2026-07-04 | Пріоритет = **desktop-first**, dashboard-grid | Референси — десктопні дашборди; поточну вузьку колонку 880px переглядаємо на багатоколонкову сітку |
| 2026-07-04 | **База каркасу = DeliFin (R1)** | Користувач: «подобається, це буде як база» — sidebar+topbar, асиметрична 2-кол. сітка, KPI-плитки, dual-line cashflow |
| 2026-07-04 | **Q1: основна тема = СВІТЛА** (як DeliFin) | Темна лишається опцією через тумблер, але дизайн ведеться від світлої |
| 2026-07-04 | **Q2: типографіка = ГІБРИД** | Герой-суми — Geist sans жирний; таблиці/списки — Geist Mono tabular. Скрізь tabular-nums |
| 2026-07-04 | **Q3: лого = ГІБРИД** | Бандл лого популярних сервісів локально + іконка категорії як fallback (офлайн, приватно) |
| 2026-07-04 | **Q4: старт редизайну = Огляд (Dashboard)** | Головний екран задає тон каркасу й токенам для решти |
| 2026-07-04 | **Огляд v1 реалізовано** (світла тема, каркас, віджети) | index.css перебудовано (світла-first палітра, num-hero, topbar, dash-grid, kpi, delta, cashflow, tx-row); нові компоненти BalanceCard/KpiRow/CashflowCard; Layout з topbar+тумблером теми; тумблер світла↔темна через localStorage `mt-theme` |
| 2026-07-04 | **Лейбли — sans sentence-case** (не mono-uppercase) | Фідбек #2: старий стиль лейблів не подобався; `.label`+`.side-group` перероблено глобально |
| 2026-07-04 | **Лого мерчантів = clearbit + fallback** (інтерим) | Фідбек #2 хоче фото/лого (АТБ, BlaBlaCar…). Зовнішні запити — приватність; топ-N варто забандлити локально пізніше |
| 2026-07-04 | Бейдж чорна/біла картка + no-outline на графіках + KPI-порівняння | Фідбек #2, швидкі правки |
| 2026-07-04 | **Статистика редизайн** (F5/F1/F6) | page-head+seg; вшита `AiInsightCard` (виділяє суми/%); 4 KPI (витрати/надходження/чистий потік/сер.день з дельтою); спільний `CashflowChart` (дохід+витрата); категорії Finexa-барами; топ-мерчанти з лого; по картках. `CashflowCard` тепер теж через спільний `CashflowChart` |
| 2026-07-04 | Фікс: `.cat-ico` базовий (не лише в `.tx`) + `.cb-fill/.cb-track` display:block | Фідбек: заливка барів категорій була невидима (span без display), плитка-лого мерчанта сплюснута |
| 2026-07-04 | **Порадник редизайн** (F6) | page-head; `Gauge`-кільце runway (tone за місяцями); метрики; поради-картки з номерами + `highlightAmounts`; `AiInsightCard` замість старого `Insight` |
| 2026-07-04 | **Підписки — окрема сторінка** (F3) | новий `pages/Subscriptions.tsx` + маршрут `/subs` + пункт меню; лого, наступне списання, сума/міс, місячний тягар, авто-детект. `Plan` урізано до бюджетів («Бюджети») |
| 2026-07-04 | Полиш решти сторінок | page-head на Рахунках/Додати/Бюджети; шапка TxDetail з лого + sans-сумою. Login/Setup підхопили глобальні стилі |
| 2026-07-05 | **Лого мерчантів → локальні (прибрано clearbit)** | Приватність: зовнішні запити зливали патерни витрат. `lib/brands.tsx` — фірмовий колір + узагальнений гліф/монограма, офлайн. Замінює інтерим-рішення clearbit від 2026-07-04 |
| 2026-07-11 | **Рефактор дизайн-системи (skills: impeccable/taste/review-animations).** Режим — «refine, keep character» (кобальт/світла/DeliFin лишаються) | Аудит impeccable-детектором: код не «слоп», але системний шар був ad-hoc. Ціль — професійність без переучування |
| 2026-07-11 | **Типографіка УНІФІКОВАНА** — одна родина Geist Sans + tabular-figures для всіх чисел; mono лише для кодів (PAN/MCC/ID/марка). Замінює гібрид Q2 | Product-register (impeccable): «одна родина зазвичай правильно». Шов sans↔mono давав розсинхрон вирівнювання. `--font-num` = `--font-sans` |
| 2026-07-11 | **Ребілд токен-шару:** 4pt-простір (`--sp-*`), радіус-шкала (`xs/sm/md/lg/xl/pill`), фіксована rem тип-шкала (`--fs-*`, прибрано `clamp()` заголовків), одна модель тіні, motion-токени (`--ease-out`, `--dur-1..3`) | Ad-hoc значення = головна причина «трохи не так» для досвідченого ока. Fixed-rem — product-UI дивляться при сталому DPI |
| 2026-07-11 | **Прибрано AI-tells:** бічні смуги-акценти (`border-left:3px` на `.cat-drill`, `.toast`), верхні акцент-бордери карток (`.group-card`/`.goal-card`/`.group-detail-head`), градієнт `.ai-badge`→solid | impeccable «absolute bans»: side-stripe = найвпізнаваніший tell AI-UI. Колір-код тепер несе іконка/крапка картки |
| 2026-07-11 | **Контраст + стани кнопки:** `--muted` #7a8797→#5f6b79 (AA ≥4.5:1); `.btn` — tactile `:active` (translateY 1px), `:focus-visible` ring, `.ghost` hover | Muted-текст не проходив AA. Product-register: кожен інтерактив має повний набір станів |
| 2026-07-11 | **Chrome карток — 3 рівні глибини (system-wide):** `.card` за замовч. = hairline + `--shadow-sm` (майже пласка); `.card.hero` = `--shadow` (єдина піднята); floating (модалки/поповери) = `--shadow-card` | Раніше ВСІ картки мали важку `--shadow-card` → «купа коробок на одній глибині», пласко-але-шумно. Спокійніше, з реальною ієрархією шарів (Linear/Stripe) |
| 2026-07-11 | **Live-фідбек по Головній → написано ТЗ §10** (не реалізовано): базові картки (radius 18+тінь) не подобаються → flat+12px; сині кнопки не подобаються → ink-primary; графіки = слоп → §10.3; додати анімації → §10.4; skills-workflow → §10.0 | Скрін показав: попри чистий детектор, картки «надуті», кнопка = дефолт-кобальт, cashflow = recharts-out-of-box. Наступним сесіям — виконувати §10 по порядку §10.5 |
| 2026-07-11 | **Редизайн Головної (перша сторінка за новою системою).** Hero-band: баланс (акцент.) + 2 KPI; широка головна колонка (Cashflow на всю ширину, парні картки SafeToSpend∣Pulse / Forecast∣Subs, CapitalTrend отримав ширину, Конверти) + вузький рейл (Кредит + Останні). Прибрано: чипи-інсайти QuickBar (дублювали картки), дубль-CTA «Додати», emoji в привітанні | Головна була «стіна» з ~11 рівноважних карток + ті самі числа 2–3× + «Додати» 3×. Ціль: ієрархія, без дублю, графікам — простір. Сітка `dash-hero` / `dash` (`1fr 340px`) / `dash-pair`. Простір через `--sp-*` |
| 2026-07-11 | **§10.1 виконано — картки flat + tighter radius.** Радіус-шкала перебудована (монотонна): картки=`--radius`12, floating(модалка/поповер)=`--radius-lg`14, контроли=`--radius-sm`10, бейджі=6, найширша модалка=`--radius-xl`18. `.card` та всі resting-картки (acct2, credit, env, group/goal/cat/budget/sub/report/mv-col, tx-filters, chat-shell) — `box-shadow:none` + hairline (`--line-strong` на головних). Тінь лишилась ЛИШЕ на `.card.hero`, floating (модалки/`.sel-pop`/bulkbar) і `.seg-btn.active` (control-lift). Hover карток = `translateY(-1px)`+border, без тіні. **Список операцій `.ledger.rows` → суцільна поверхня з hairline-роздільниками** (не «стіна окремих карток»); drill-txs лишив компактні плитки | Скрін: radius 18 + тінь = «надуто/бульбашки». §10.1: «точний інструмент, плаский». Розділення несе контраст surface↔bg + 1px, не тінь |
| 2026-07-11 | **§10.2 виконано — primary-кнопка = ink, не кобальт.** `.btn.primary`, `.side-nav .add` → `background:var(--ink); color:var(--surface)` (інвертується в темній темі — світла кнопка на темному). Кобальт лишився ЛИШЕ для лінків/активного меню/selection/focus-ring. Прибрано всі `filter:brightness()` hover → явний `color-mix` hover. radius кнопок=10 (нова шкала). Tactile `:active translateY(1px)` лишено. Danger/чат-self/toggle-on/avatar — accent/neg лишилися (semantic) | §10.2: «дефолтний сатурований синій = generic». Ink-primary — найсильніший де-слоп-хід (акцент перестає бути «кнопка=синя пігулка») |
| 2026-07-11 | **§10.3 виконано — графіки де-слоп.** Прибрано бурштин `#e8a13c` глобально: токени `--chart-expense=var(--accent)` (кобальт, домінує) / `--chart-income=var(--pos)` (green, тихо) в обох темах. Усі 3 лінійні чарти (Cashflow/Capital/Cumulative): пунктир-gridlines → СУЦІЛЬНІ горизонт. `--line` opacity .6 (без вертикальних); лінії stroke 2px `strokeLinecap:round` `dot:false` `activeDot r3.5`; area — одна делікатна під домінантою (opacity .22→.08), дохід без заливки; осі без лінії, `tickCount:4`, 11px muted. Донат Reports — системна кат-палітра (без змін кольору) + анім. Проєкц-пунктир у Cumulative лишено (семантичний, не сітка) | §10.3: cashflow = «recharts з коробки» (бурштин+подвійна area+пунктир-сітка). Ціль — стриманий «власний» вигляд, 2 системні тони |
| 2026-07-11 | **§10.5-крок 5 — сторінки Статистика/Транзакції за системою.** Транзакції — структурно чисті (page-head/btn/seg/flat tx-filters/continuous list вже з глобалів). Слоп прибрано: бінарні split-бари (Будні/Вихідні, Регулярні/Разові) хардкод-охра `#c9871a`/пайн `#1f6e4c` → системна пара `--accent`(дом.)+`--c-teal`(втор.); `.pace-tag` охра → teal-токен (theme-aware, прибрано per-theme оверрайди); SafeToSpend importance-легенда хардкоди → `--c-teal`/`--c-ochre` токени; `transition: all` (`.tx-check`, `.imp-opt`) → явні props (impeccable/review-anim hard-flag). Детектор impeccable по обох сторінках — чисто | «по скілам, без слопу»: сторінки успадкували flat/ink/де-слоп-чарти з глобалів; лишалась локальна колір-слоп + unbounded transition. Категорійні палітри пікерів і token-фолбеки `var(--warn,#…)` — лишено (легітимні) |
| 2026-07-11 | **Прибрано side-stripe карток-рахунків** (`.acct2::before` 4px ліва кольор-смуга) — рішення користувача | impeccable absolute-ban #1 (найвпізнаваніший AI-tell). Колір рахунку вже несе `.acct2-badge` у шапці, тож смуга була дублем. Padding нормалізовано 20px |
| 2026-07-12 | **Транзакції: пивот колонкова→стек-рядок** (§11.9) — банк-стиль `logo∣body∣amount`, body 2 рядки (назва / категорія·дата), сума центрована. Скасовує §11.6 Q1 | Категорія «плавала» (content-трек мерчанта, кожен рядок — власний grid); користувач: «вирівняй + зміни як було раніше, покращено». Стек вирівнює категорію природно (фікс. колонка лого) |
| 2026-07-11 | **§10.4 виконано — анімації (product-стримано, без бібліотек).** `useCountUp` (rAF, ease-out-quart, 500ms, reduced-motion→миттєво) на герой-баланс (BalanceCard) + KPI-числа (KpiRow). Chart draw-in: `CHART_ANIM` (600ms ease-out, one-time, `isAnimationActive` gated by `prefersReducedMotion`) на всі чарти+донат — замість дефолт-1500ms recharts. Hover карток (kpi/fact) — transition `transform`+border на motion-токенах. **Свідомо БЕЗ page-load reveal-stagger** (product-register: «no orchestrated page-load»). **review-animations пройдено → Approve** (2 minor-нотатки: count-up рефайр на кожен mount Головної; hover не gated `@media(hover:hover)` — обидва не-блокуючі) | §10.4: додати рух там де = стан/reveal. count-up = signature-момент даних; draw-in = «дані прийшли». Ніякого декору |

---

## 10. Наступний етап редизайну — ТЗ для наступних сесій (2026-07-11)

> **Контекст:** зроблено рефактор системного шару (токени, уніфікована типографіка,
> прибрано AI-tells, redesign Головної — див. §8). Користувач після live-скріна сказав:
> базові блоки (radius+тінь) не подобаються, сині кнопки не подобаються, графіки =
> «слоп». Нижче — конкретні цілі. **Не реалізовано ще — це інструкція.**

### 10.0 ЯК працювати над дизайном (skills — обовʼязково)
1. **Старт кожної дизайн-сесії:** `node .claude/skills/impeccable/scripts/context.mjs`,
   тоді прочитати `impeccable/reference/product.md` (наш register — **product**, НЕ brand)
   і цей `DESIGN.md`. Register-довідник обовʼязковий, інакше вивід generic.
2. **impeccable — головний інструмент.** Команди: `critique` (рев'ю з оцінкою), потім
   `layout`/`typeset`/`colorize`/`animate`/`polish` для застосування. Абсолютні бани
   impeccable = закон (side-stripe, gradient text, glassmorphism-за-замовч., hero-metric
   шаблон, однакові картки-сітки, eyebrow над кожною секцією).
3. **review-animations — окремий скіл, `disable-model-invocation`:** його треба
   **явно викликати** щоразу, коли додаєш/міняєш анімацію. Він рев'юїть рух за
   стандартами Emil Kowalski (див. §10.4). Не пропускати.
4. **design-taste-frontend — ЛИШЕ довідково.** Він для лендингів, НЕ для дашбордів;
   беремо тільки anti-slop-правила (один акцент, без AI-градієнтів, консистентний radius).
5. **Хук impeccable** авто-ганяє детектор після правок UI-файлів. Знайдені tells —
   **виправляти, не глушити** (config-ignore лише за явним підтвердженням користувача).
6. **Green-bar перед «готово»:** `npm run check` + `npm run build`. Live-перевірка = користувач.

### 10.1 Базові картки/блоки (`.card`) — ЗРОБИТИ «точний інструмент», не «бульбашки»
Проблема (скрін): завеликий border-radius (18px) + мʼяка тінь → «надуто», template-look.
**Ціль — крихкий, точний, плаский:**
- `--radius-lg` для карток **18 → 12px** (макс 14). Плитки/бейджі всередині — 8px.
  Витримати ОДНУ шкалу (impeccable: shape consistency lock).
- **Пласко: `.card { box-shadow: none }`** — лише 1px hairline `--line-strong`.
  Розділення несе контраст surface(#fff) vs bg(#f3f5f8), не тінь. Тінь — ТІЛЬКИ для
  floating (модалка/поповер/дропдаун = `--shadow-card`) і, можливо, hero-баланс (`--shadow`,
  дуже делікатна). Це вже закладено 3-рівневою моделлю — просто прибрати `--shadow-sm`
  з дефолтної `.card` і зменшити radius.
- Наслідок: `.acct2`, `.env-item`, `.budget-card`, `.report-card`, `.sub-card`,
  `.tx` (у списку `.ledger.rows`), `.mv-col`, `.imp-card` — усі теж на flat+12px.
  Пройтись і прибрати їхні локальні `box-shadow: var(--shadow-sm)`.
- Транзакції-рядки на Головній (скрін: кожен рядок — окрема картка з radius+тінню):
  розглянути **суцільний список з hairline-роздільниками** замість окремих плиток-карток
  (спокійніше, щільніше, менше «коробок»). Або flat-плитки 10px без тіні.

### 10.2 Кнопки — прибрати «дефолтний сатурований синій»
Проблема: `.btn.primary` = плаский насичений кобальт `#2e6be6`, великий radius → generic.
**Ціль (рекомендований експеримент):**
- **Первинна кнопка = ink/near-black** (`--ink #0f1620`, білий текст), як «Export» у DeliFin.
  Кобальт лишити ЛИШЕ для лінків, активного пункту меню, selection, focus-ring. Це
  найсильніший де-слоп-хід (акцент перестає бути «кнопка = синя пігулка»).
  Fallback, якщо ink не зайде: глибший, менш «неоновий» синій (`--accent-strong`),
  radius **10px** (не пігулка), тонкий inner-highlight, tactile `:active` (є).
- `.btn` radius під нову шкалу (10px). Secondary — hairline, без заливки (є).
- Прибрати `filter: brightness()` на hover (дешевий приймач) → явний hover-токен кольору.
- Перевірити контраст тексту на кнопці (impeccable: button contrast check).

### 10.3 Графіки — головний «слоп». Переробити подачу (Recharts)
Проблема (cashflow): бурштин `#e8a13c` + синій, згладжена area з подвійною заливкою,
пунктирні горизонтальні gridlines, дефолтні осі → «recharts з коробки».
**Ціль — стриманий, «власний» вигляд:**
- **Колір:** прибрати бурштин. Взяти 2 тони З СИСТЕМИ: або `--pos` (дохід) + графіт/`--ink-2`
  (витрата), або `--accent` + приглушений нейтрал. Один акцент домінує, другий — тихий.
- **Gridlines:** прибрати пунктир. Або зовсім без сітки, або 2-3 ультра-бліді СУЦІЛЬНІ
  лінії (`--line`, opacity ~.5). Жодних вертикальних.
- **Лінії:** stroke 2px, `strokeLinecap:round`, БЕЗ крапок (`dot={false}`), activeDot лише на hover.
- **Area-заливка:** одна, під домінантною серією, opacity ~6-8%, без градієнт-стеку;
  або взагалі без заливки (чисті лінії — часто чистіше).
- **Осі:** без осьової лінії (`axisLine={false} tickLine={false}`), 11px `--muted`,
  мінімум тиків (початок/середина/кінець). Y — максимум 3 тики або приховати.
- **Tooltip:** темний pill вже кастомний (лишити), синхронний cursor-line тонкий `--line-strong`.
- **Простір:** більше повітря навколо, менше елементів. Порожній стан — осмислений.
- Застосувати до ВСІХ графіків: `CashflowChart`, `CapitalTrendCard`, `CumulativeChart`,
  донат у Репортах, income-bars, heat-map, weekday-bars, `md-chart` у чаті.

### 10.4 Анімації — ДОДАТИ (зараз майже нема; токени вже є: `--dur-1/2/3`, `--ease-out`)
> Після БУДЬ-ЯКОЇ доданої анімації — викликати скіл **review-animations** на рев'ю.
> Стандарти: UI <300ms, лише ease-out/кастом-крива (ніколи ease-in), GPU-props
> (transform/opacity), правильний transform-origin, `prefers-reduced-motion` (є глобально).
**Що додати (стримано — product, не «cinematic»):**
- **Hover карток:** `translateY(-1px)` + border-color, `--dur-2 --ease-out` (частково є).
- **Прес кнопки:** `translateY(1px)` (є) — лишити.
- **Reveal чисел:** делікатний count-up для hero-балансу + KPI на першому завантаженні
  (400-600ms ease-out, tabular-nums щоб не стрибало; вимикати при reduced-motion). Опційно.
- **Draw-in ліній графіка:** анімація довжини path на mount, ~600ms ease-out, ОДИН раз.
- **Секції на першому пейнті:** дуже легкий fade+rise stagger 30-60ms (motion `whileInView`,
  `once:true`). УВАГА (product-register): без «оркестрованого» лоуду сторінки — тільки
  ледь-помітно, не блокувати видимість контенту класом.
- **Поповери/дропдауни:** scale з origin 0.96→1 + opacity 120ms (є в `.sel-pop`) — стандарт.
- Бібліотека `motion` — лише де треба (count-up, path-draw); решта — CSS/WAAPI.

### 10.5 Порядок робіт (наступні сесії)
1. `.card` flat + radius 12 (§10.1) — системний, зачіпає все, робити ПЕРШИМ.
2. Кнопки (§10.2) — expериment ink-primary.
3. Графіки (§10.3) — найбільший де-слоп-виграш, окрема сесія.
4. Анімації (§10.4) — після того як статика влаштовує; review-animations обовʼязково.
5. Далі інші сторінки (Статистика, Транзакції) за тією ж системою.

---

## 11. Live-фідбек раунд 2 (2026-07-11, по скрінах Головної + Транзакцій)

> Джерело — скріни користувача. Skills: **impeccable `layout`** для повітря/ритму,
> `critique` для рев'ю. Робити «по скілах, без слопу». Наступне велике — Статистика.

### 11.1 Повітря у щільних блоках (impeccable `layout`)
- **Кредит-картка (`.credit-card`)** — тісно: `padding 15px`, 3 стат-колонки стиснуті,
  мікро-uppercase-лейбли (`ВЛАСНИХ/ВИКОРИСТАНО КРЕДИТУ/ЛІМІТ`) туляться, метр впритул.
  → більше padding (18–20), більші gap між статами, лейбли sentence-case, простір над метром.
- **Конверти-бюджети (`.env-item`, `.budget-card`)** — той самий брак повітря → більший padding/gap.
- Принцип: пройтись `layout`-скілом по ВСІХ щільних картках, ритм 4pt, не тісно.

### 11.2 Базові білі картки — пласкіше + гостріше
- Radius `12 → 10` (картки); floating `14 → 12`. **Тінь прибрати повністю** навіть з `.card.hero`
  (користувач: «шадов не підходить») — розділення несе бордер `--line-strong` + контраст surface↔bg.
  Тінь лишається ЛИШЕ для справді floating-шару (модалки/поповери/дропдауни).

### 11.3 Сторінка Транзакцій — де-слоп (impeccable `layout`)
- Проблема: **порожнеча в центрі рядка** (коротка назва → діра → сума) + **повтор `··4932`** на кожному
  рядку (шум) + високі рядки + товстий скролбар сайдбара.
- Напрям (на узгодженні §11.6): або **колонкова таблиця** (Мерчант · Категорія · Дата · Спосіб · Сума,
  вирівняні колонки — Cazura R7, desktop-grade), або **рефайн-рядки** (тісніше, прибрати `··4932`-повтор).
- Скролбар сайдбара — тонкий/прихований.

### 11.4 Фільтри в сайдбарі — згортання (collapsible)
- Секції (Рахунок / Сума / Період / Категорії) — акордеон, щоб ховати. Дефолт-стан — §11.6.

### 11.5 Дрібне
- **Вирівнювання кнопок**: консистентний `align-items:center` іконка+лейбл скрізь (скрін «Додати»).
- Стилі табів/сегментів (`.seg`) — можна міняти (переважно в раунді Статистики).
- Загальне: усе, що виглядає як слоп / не user-friendly — міняти.

### 11.6 Рішення (узгоджено 2026-07-11)
- **Q1 Транзакції = КОЛОНКОВА ТАБЛИЦЯ.** Вирівняні колонки Мерчант · Категорія · Дата · Сума;
  `··4932` прибрано як шум (видно в деталях). Дрили Статистики лишають compact-рядок.
- **Q2 Картки = radius 10 + ПОВНІСТЮ ПЛАСКО.** Тінь прибрано звідусіль, зокрема `.card.hero`;
  floating (модалка/поповер/дропдаун) — єдине з тінню. `--radius:12→10`, `--radius-lg:14→12`.
- **Q3 Фільтри = акордеон, відкриті лише Категорії.** Пошук + таби Типу завжди вгорі;
  Рахунок/Сума/Період згорнуті за замовч., клік розгортає.

### 11.7 Виконано (2026-07-11) — green-bar, чекає live-рев'ю користувача
- **§11.2:** `--radius 12→10`, `--radius-lg 14→12`, `--radius-sm→9`, `--radius-xl→16`. `.card`+`.card.hero`
  повністю пласкі (тінь лише floating). Бордер головних карток — `--line-strong`.
- **§11.1 повітря:** `.credit-card` padding 20, gap-стати 18, sentence-case лейбли (без uppercase), простір над метром 18;
  `.env-item` padding 15/16 gap 10; `.budget-card` padding 18 gap 13; grid-gap конвертів/бюджетів більший.
- **§11.3 колонкова таблиця:** TxItem переписано на осередки (logo·who·cat·date·amt). CSS — `grid-template-areas`,
  що рефлоуять через **контейнер-запит** (`.ledger.rows { container-type: inline-size }`): stacked <620px (рейл/дрили),
  колонкова таблиця ≥620px (сторінка Транзакцій). Прибрано повтор `··4932` + застарілий «R6 fix»-блок (конфлікт radius/тінь/`.meta`).
- **§11.4 акордеон-фільтри:** `FilterSection` (заголовок+шеврон, крапка активного фільтра в згорнутій). Дефолт відкрито — Категорії.
- **§11.5:** `.btn` = центрований inline-flex+gap за замовч. (консистентне вирівнювання іконка+лейбл); `.btn>svg{display:block}`.
  Тонкий скролбар `.tx-filters` (webkit + scrollbar-width).
- **§11.3 фікс вирівнювання категорій:** мерчант 1.7fr лишав велику діру → категорії «плавали». Перероблено на
  капнутий лівий кластер (мерчант `minmax(120,300)` + категорія `minmax(110,240)`) + гнучкий spacer (`.`) →
  категорії стоять чіткою колонкою впритул, дата+сума — правий кластер.

### 11.10 Тултіпи + Налаштування + Репорти (2026-07-12)
- **Баг тултіпів (скрін «Витрати по категоріях»):** `.section-head` = `space-between` з 3 flex-дітьми
  (h2 · InfoTip · label) → тригер-ⓘ зависав ПОСЕРЕДИНІ. Фікс глобальний: `flex-start` + `gap` +
  `margin-left:auto` на `.label`. Працює навіть коли лейбл у HoverTip (той `display:contents`, тож
  внутрішній `.label` — прямий flex-елемент). Виправляє ВСІ section-head (Статистика, Репорти, Порадник).
- **Налаштування — layout:** була «стіна» з 9 однакових full-width кнопок без групування й без page-head.
  → стандартний `.page-head`; **2-колонкова `.settings-grid`**; дії згруповано в картки з заголовками
  (`Стан бази` · `Перший запуск` · `Обслуговування` · `Telegram`); Профіль + AI-блок на всю ширину (`.set-full`).
  AI-витрати — 3 плитки (Сьогодні/Місяць/Весь час) замість рядків. Де-емоджі (✨/💸 → Icon `spark`).
- **Репорти:** де-емоджі (🧠/✨ → Icon `spark`); `.forecast-hero` діагональний градієнт → пласка
  акцент-тонована поверхня (AI-tell, §10); `.fh-label` UPPERCASE → sentence-case (фідбек #2).

### 11.9 Транзакції — пивот на стек-рядок (2026-07-12)
- **Проблема (скрін):** у колонковій таблиці (§11.6 Q1) категорія «плавала» — трек мерчанта
  `minmax(120,300)` content-залежний, а кожен `.tx` = власний grid, тож довга назва
  (PlayStation Plus) резолвила трек ширше за коротку (Фора) → колонка категорії стартувала
  на різних x. Користувач: «вирівняй категорію всюди однаково» + «зміни дизайн, як було
  раніше, але покращено» (обрано «Помітніша зміна»).
- **Рішення:** відмова від колонкової таблиці → **сучасний стек-рядок** (банк-стиль,
  ближче до старого вигляду): `logo | body | amount`, body = 2 рядки (назва+група /
  `категорія · дата`), сума вертикально центрована праворуч. Категорія завжди стартує
  одразу за лого (фікс. колонка логотипа) → **вирівняна на КОЖНОМУ рядку** без content-треків.
- **Реалізація:** `TxItem` — `.tx-body` (flex-column) з `.tx-line1`/`.tx-line2`; middot-роздільник
  `.tx-date::before`. CSS `.tx` = grid `40px minmax(0,1fr) auto` (без grid-areas). Контейнер-запит
  ≥620px (сторінка Транзакцій) лише масштабує (лого 44, сума 17px, більше повітря); вузький рейл
  і `.tx-sm`-дрили лишаються компактні. Скасовує §11.6 Q1 (колонкова) і §11.7 fr-фікс.

### 11.8 Статистика (2026-07-11, розпочато — headline-зміни; далі по live-скрінах)
- **Секційні таби → підкреслення** (`.stat-tabs`/`.stat-tab`): навігація Огляд/Категорії/Тренди/Мерчанти тепер
  відмінна від pill-фільтра періоду (2 однакові pill-ряди були плутаниною ієрархії). Активний = ink-текст + акцент-underline.
- **Де-емоджі:** перемикач періоду `📅/↔` → Icon `calendar`/`repeat` + текст; картка AI-переказів `✨` → Icon `spark`.
- Кольори-слоп у Статистиці вже прибрано раніше (split-бари/pace-tag/importance → системні токени).
- **Далі (потребує live-рев'ю по табах):** щільність/ритм секцій Огляду, per-tab контент — ітерувати за скрінами.

---

## 9. Відкриті питання

**Батч #1 (2026-07-04) — ВИРІШЕНО** (див. §8): Q1 світла тема · Q2 гібрид шрифтів ·
Q3 гібрид лого · Q4 старт з Огляду. Нових відкритих питань немає — далі редизайн Dashboard.

**Наступне рішення (перед кодом Dashboard):** уточнити світлу палітру — лишити наш
spruce-off-white (`--bg #f2f5f3`) чи перейти на нейтральніший gray-white як у DeliFin.
Вирішимо на етапі мокапу Огляду.
