# ROADMAP — жива черга задач і фіч

> **Це постійний файл.** Тут усе, що треба зробити й доробити. Правила ведення — у `CLAUDE.md §Робочий процес`:
> нову задачу/ідею/баг → сюди; **доробив → видали пункт**; якщо змінює «як усе працює» → онови `CLAUDE.md`.
> Довідник/інваріанти/як працює зараз → `CLAUDE.md`. Дизайн → `DESIGN.md`.
> Поточний зріз — аналіз фідбеку 2026-07-10 (скріншоти). Уточнення — у кінці (§Питання).

## ✅ Opus-перемикач (ЗРОБЛЕНО 2026-07-10)
Перемикач розумної моделі в Налаштуваннях (Sonnet 5 ⇄ Opus 4.8), збережено в `app_state.ai_smart_model`. `getSmartModel(env)` в `ai.ts` резолвить модель для всіх user-facing викликів (порадник/репорти/чат/бюджет/інсайт); enrich/OCR завжди Haiku. Ціни Opus додано в лічильник → глобальна вартість точна. Дефолт — Sonnet.

## ✅ P0 БАГ — репорт за тиждень показує 0 транзакцій (ВИПРАВЛЕНО 2026-07-10)
Корінь: канонічні `SPEND_WHERE`/`INCOME_WHERE` містили `hold=0`, а список транзакцій (`/transactions`) holds показує → UI мав операції, репорт/Статистика — ні. Фікс: прибрано `hold=0` з канону (`stats.ts`) — holds рахуються як реальні витрати (mono шле лише виконані, upsert по `id` без дублю); AI-enrich тепер біжить і на holds (`webhook.ts`); `advisor` citation-запит вирівняно. Інваріант оновлено в `CLAUDE.md`.

---

## 1. Статистика — фільтри + більше видів аналітики
- ✅ **Вирівнювання (скрін №3) — ЗРОБЛЕНО 2026-07-10:** фільтри (період-сегменти | режим | валюта) приведено до однакової висоти (40px, спільний `.pill-toggle`), тултип пояснює календарний vs ковзний.
- **Більше аналітики — частково ЗРОБЛЕНО 2026-07-10:**
  - ✅ Середній чек (Огляд, загалом).
  - ✅ Топ-зростання/падіння категорій (vs минулий) — окремий блок «Топ-рухи» в Порівнянні періодів.
  - ✅ Прогноз витрат на кінець періоду (Огляд, календарний режим).
  - ✅ «Найдорожчий день» + «Днів без витрат» (Глибша аналітика).
  - ✅ Витрати обов'язкові vs дискреційні (§6 вагомість — смуга на Огляді).
  - ✅ Кумулятивний потік (running balance) за період — ЗРОБЛЕНО 2026-07-11 (таб «Тренди», `CumulativeChart`: накопичена різниця надходження−витрати по бакетах, лінія pos/neg + нульова база).
  - ✅ Розподіл по днях місяця / heat-map — ЗРОБЛЕНО 2026-07-11 (Глибша аналітика: грід 1..31, інтенсивність кольору = сума витрат за числом місяця).
  - ✅ Прогноз-лінія на графіку — ЗРОБЛЕНО 2026-07-11 (кумулятивний потік: пунктирна проєкція на решту днів періоду за середнім денним нетто-темпом; лише календарний незавершений період з денними бакетами; `CumulativeChart` рендерить `proj` окремою dashed-лінією).
  - ⬜ Середній чек по категорії/мерчанту.
  - ✅ Аналітика доходу (джерела/стабільність/дельта) — ЗРОБЛЕНО 2026-07-11: endpoint `/analytics/income` (джерела по ефективній категорії, дельта vs минулий, стабільність = коеф. варіації місячного доходу за 6 повних міс) + секція «Надходження» в табі «Тренди» (`IncomeBreakdown`: бари-джерела + бейдж стабільності + міні-бари 6 міс).
- ✅ **Дизайн дрилів (скріни №4, №5) — ЗРОБЛЕНО 2026-07-10:** спільний компонент `TxItem` (`compact`-варіант) рендерить стандартний рядок транзакції (лого/назва/категорія-чип/дата/сума/картка) і в дрилах Статистики, і в списку операцій. `TransactionList` і `DrillTxList` тепер поверх нього. Дрил-список отримав **скрол** (max-height 46vh) + тісніші падінги/відступи (довгі списки на 70+ операцій більше не тягнуться). Бейдж «в обробці» ПРИБРАНО (за проханням — holds від mono вважаємо виконаними).
- ✅ **Підзаголовок «що саме рахується» над дрилами — ЗРОБЛЕНО 2026-07-11:** `DrillTxList` показує канонічне пояснення зрізу (`kind` expense/income): витрати — без переказів/зняття, готівка за реальною категорією, holds рахуються; надходження — без переказів між своїми.

## 2. ✅ Порадник (скрін №6) — ЗРОБЛЕНО 2026-07-10 (більше контексту + історія)
- ✅ **Більше контексту в payload:** тренд **6 місяців** (spend/income по місяцях), **бюджети ліміт vs факт** цього місяця (used_pct, перевитрати), **фіксовані підписки/міс** (сума+кількість), вагомість (§6), групи/події (було). Промпт оновлено, щоб AI це використовував і цитував конкретику.
- ✅ **Opus** — через перемикач у Налаштуваннях (готово раніше).
- ✅ **Історія порад:** знімки (runway/burn/summary, останні 12) у `app_state.advisor_history`, endpoint `/advisor/history`, компактний список на сторінці Порадника.
- ✅ **Цитування операцій — ЗРОБЛЕНО 2026-07-10:** payload має `citable_operations:[{id,label}]`, промпт `generateAdvice` вставляє `[tx:ID]`, Порадник рендерить через спільний `renderRich` (клікабельні чипи → `/tx/:id`) у summary й порадах.

## 3. Бюджети (скрін №7) — переробити повністю
- ✅ **Дизайн — ЗРОБЛЕНО 2026-07-10:** картки-конверти з прогрес-барами (ліміт / факт за місяць / залишок / % / стан ok·warn·over), інлайн-редагування ліміту, сортування (спершу з лімітом, далі за фактом), грід-розкладка. Верхньорівневі витратні категорії (рол-ап). AI-планер лишається зверху.
- ✅ **AI-бюджет через діалог — ЗРОБЛЕНО 2026-07-11:** чат «💬 Бюджет-діалог» на сторінці Бюджетів. Endpoint `/budgets/chat` + `budgetChat` (ai.ts) + `budgetChatReply` (advisor.ts, контекст: категорії з avg/ліміт/вагомість). AI веде розмову, пропонує ліміти з поясненням (`proposals`), можна прийняти окремо/усі одним тапом. Стартові підказки.
- ✅ **Rollover — ЗРОБЛЕНО 2026-07-11:** прапорець на бюджеті (міграція **0017** `budgets.rollover`), ефективний ліміт = базовий + невитрачений залишок минулого місяця; чекбокс + «+X з минулого» на картці.
- ✅ Зв'язано з вагомістю (§6): бюджет-діалог і планер бачать importance (essential не ріже різко).

## 4. Головна (скрін №8) — збільшити, більше інфи/функціоналу
- ✅ **«Конверти» — ЗРОБЛЕНО 2026-07-10:** редизайн `EnvelopeGrid` у зрозумілі міні-бари (назва · факт із ліміту · % · залишок/«перевищено», стан-колір ok·warn·over), сортування за напруженістю, клік → `/plan`. Секцію підписано «Бюджети-конверти». Прибрано абстрактну вертикальну заливку.
- ✅ **Тренд капіталу — ЗРОБЛЕНО 2026-07-11:** картка `CapitalTrendCard` на Головній (ліва колонка) — area-лінія динаміки власних коштів за 6 міс + дельта за період. Endpoint `/analytics/capital-trend` реконструює історію назад від поточного тоталу (`computeSummary`), віднімаючи денну зміну балансу (`Σ amount×курс`; перекази між своїми гасяться, бо обидві ноги в таблиці). Курси поточні (апрокс, історичні не зберігаються).
- ✅ **Пульс місяця — ЗРОБЛЕНО 2026-07-11:** картка `MonthPulse` на Головній (права колонка) — **норма заощаджень** (net/дохід за календарний місяць, тон pos/warn/neg) з розбивкою дохід/витрати/відкладено + **топ-4 категорії міні** (бари, клік → Статистика). Одна overview-вибірка на обидва блоки.
- ✅ **«Скоро спишеться» — ЗРОБЛЕНО 2026-07-11:** віджет `UpcomingSubs` на Головній — прийдешні планові списання/підписки в горизонті 30 днів (лого бренду, дата, «через N дн», підсумок), перетинає межу місяця. Endpoint `/planned/upcoming?days=`. З `ForecastCard` прибрано дубль-перелік (лишився підсумок «ще спишеться цього місяця»).
- ✅ **Швидкі дії + швидкі інсайти — ЗРОБЛЕНО 2026-07-11:** командний рядок `QuickBar` над колонками Головної — кнопки-навігація (Додати/Репорт/Порадник/Бюджети) + детерміновані інсайт-чипи (норма заощаджень, вільно/перевитрата, прогноз-нетто місяця) з safe-to-spend/forecast, без AI (миттєво).
- ✅ **Safe-to-spend — ЗРОБЛЕНО 2026-07-10:** картка «Вільно до кінця місяця» на Головній = дохід(міс) − витрачено(міс) − прийдешні підписки(залишок), з розбивкою (дохід/витрати/підписки) + split обов'язкові/бажані (§6). Endpoint `/analytics/safe-to-spend` (канонічний, календарний місяць). Червона при перевитраті.

## 5. Репорти (скріни №9, №10) — окрема сторінка + наглядність
- ✅ **Окрема сторінка — ЗРОБЛЕНО 2026-07-10:** `/reports` (список карток) + `/reports/:id` (повна деталь). Таб «Репорти» прибрано зі Статистики; новий пункт у навігації (іконка `report`). Нова сторінка `src/pages/Reports.tsx`.
- ✅ **Дизайн + наглядність (частково):** секції-картки в стилі застосунку; **категорії — горизонтальні бари** (колір+сума+дельта-чіп vs минулий); аномалії з кольором за severity (info/warn/high); прогноз — fact-плитки; поради-картки; **суми підсвічені** через `highlightAmounts`; мета (модель+вартість).
- ✅ **AI-цитування операцій — ЗРОБЛЕНО 2026-07-10:** контекст репорту (`notable`/`biggest_expenses`) містить `tx_id`; промпт `generateFinancialReport` інструктує вставляти `[tx:ID]` після назви операції; спільний рендер `src/lib/citations.tsx` (`renderRich`) перетворює токени на клікабельні чипи `↗ → /tx/:id` (+ підсвітка сум) у summary/секціях/аномаліях/порадах. Зворотно сумісно (старі репорти без токенів рендеряться як є).
- ✅ Цитування підключено і в Пораднику (§2).
- ✅ **Донат + тренд-лінія — ЗРОБЛЕНО 2026-07-11:** донат розподілу по категоріях (топ-8 + «інші», сума в центрі) поруч зі списком-барами; лінія тренду 6 міс (spend/income) з детермінованих даних, які тепер зберігаються в `data_json` репорту (`buildReportContext` повертає `trend`).
- ✅ P0-баг даних — виправлено (див. вгорі).

## 6. ✅ Вагомість витрат — ЗРОБЛЕНО 2026-07-10..11 (3 рівні, повна інтеграція)
- **Модель:** `categories.importance` (дефолт) + `transactions.importance` (override), значення `essential|discretionary|optional`, NULL→«бажана». Міграція **0016** (з розумними сід-дефолтами за назвою категорії). Канонічний `EFF_IMPORTANCE` у `stats.ts` (override → рол-ап категорії → дефолт).
- **UI:** пікер у `CategoryModal` (лише витрати) + override-пікер у `TxDetail`; смуга-частка «Вагомість витрат» на вкладці Огляд Статистики (`byImportance` в overview).
- **AI:** порадник отримує `by_importance` у payload + інструкцію («optional найбезпечніше різати»).
- ✅ **Інтеграція — ЗРОБЛЕНО:** Бюджети (діалог/планер бачать importance, §3), Safe-to-spend (split обов'язкові/бажані, §4), **репорти (2026-07-11):** `buildReportContext` рахує канонічну розбивку (`EFF_IMPORTANCE`) → у AI-контекст (`by_importance`, промпт цілить скорочення в optional/discretionary) + детермінований `importance` у `data_json`; секція «Вагомість витрат» (смуга+легенда) на сторінці репорту.

## 7. TG-бот (НЕ пріоритет, записано)
- Доробити фази: пуш репортів/аномалій, швидкий запис витрати текстом/фото, запити балансу/статистики. Активація за TG-секретами (є каркас — див. `telegram-bot.md`).

---

## 📌 Ще НЕ зроблене (беклог)
- **Календар майбутніх списань** — підписки з каденцією → місячний прев'ю прийдешніх списань + вплив на кешфлоу.
- **Аналітика позицій чека** (`receipt_items` зберігаються, не аналізуються).
- **UI-редактор правил** `rules`.
- **Мульти-валютні групи/подорожі** + **CUR2 хвіст:** перевірити зведення в ₴ у TG-бот/підписки (аналітика вже зведена).
- **Prompt-кеш verify** на проді (`cache_read` у логах) — потребує деплою.
- **Lazy-enrich** (варіант B) — за узгодженням.
- **Дизайн:** уточнити світлу палітру (DESIGN.md); `DESIGN.md §4` застарів (згадує видалені компоненти).
