# ROADMAP — жива черга задач і фіч

> **Це постійний файл. Тут — ЛИШЕ те, що ще треба зробити.** Зроблене звідси **видаляється**,
> а стислий підсумок переїжджає в `CLAUDE.md §Зроблено`. Правила — `CLAUDE.md §Робочий процес`.
> Довідник/інваріанти → `CLAUDE.md`. Дизайн → `DESIGN.md` (§8 «Журнал рішень» + §10–11).
>
> Останнє прибирання: **2026-07-20** (винесено ~10 ✅DONE-карток, які тут уже не мали лежати).

## 🚦 Як працювати з цим файлом

1. Бери **найвищу невиконану задачу** з «Черги». Не перестрибуй без причини.
2. Задача = картка: **Ціль · Файли · Кроки · Готово-коли**. Спочатку прочитай згадані файли.
3. **Green-бар перед «готово»:** `npm run check` (tsc + SQL-лінт) + `npm run build`.
   Гроші/статистика — лише через `worker/lib/stats.ts`, не дублюй SQL.
4. **UI/UX** — спершу `DESIGN.md`, зміну фіксуй у його «Журналі рішень». Хук impeccable ганяє
   детектор — знайдене виправляй, не глуши.
5. **Доробив → ВИДАЛИ картку звідси**, підсумок у `CLAUDE.md §Зроблено`. Нове/баг → у «Беклог» чи «Ідеї фіч».
6. **Live-звірку в чергу НЕ писати.** Користувач ганяє її сам і повідомляє, якщо щось не так.
   AI-залежну задачу просто здавай зеленою.
7. Один PR / логічний блок = одна задача.

---

## 🔥 Черга (роби згори вниз)

### 🔴 Хвости живого запуску (2026-07-26) — РОБИТИ ПЕРШИМ

> Зібрано з живої перевірки в проді після першого деплою платформи. Усе нижче — те, що
> користувач побачив на власні очі; фікси того ж прогону вже в `CLAUDE.md §Зроблено`.

**L7 — Скріншоти в README.** Вставити знімки з живого `/demo` у `docs/screenshots/`
(домен уже підставлено). Заразом — місце під них є і на лендінгу: `.lp-flow` у hero
свідомо зроблений слотом, який замінить реальний скрін продукту.
*(L5 і L7(а) закриті 2026-07-26 — див. `CLAUDE.md §Зроблено`.)*

**L8 — Ops, не код (твоє).** (а) `POST /api/admin/import-legacy` — перенести стару історію
(417 tx / 9 рахунків) у свій DO, інакше акаунт порожній. (б) Google Cloud: redirect URI
`https://money-track.vitaliy-50a.workers.dev/auth/google/callback` + усі пошти в **Test users**,
поки застосунок у Testing. (в) `OWNER_EMAIL` = `italik.dev@gmail.com` — заходити саме ним.

---


### ⭐ Наступна велика фаза — Мультиюзер · банк-абстракція · level-up · демо · англійська
**Дизайн/ТЗ — `PLATFORM.md` (читати ПЕРШИМ).** Рішення зафіксовані (invite-only ~10-50 друзів,
повна ізоляція, свої ключі, UA-банки+CSV). Архітектура: **Durable Object на юзера** (нуль
`WHERE user_id`, канон `stats.ts` не переписується).
**Auth = Google OAuth** (зафіксовано, `PLATFORM.md §3`). Картки нижче — по порядку. Кожна =
один PR/логічний блок. Перед кожною читати відповідний § `PLATFORM.md`.

**🎯 Мета всієї фази — ПУБЛІЧНЕ ПОРТФОЛІО** («зроблено за допомоги AI»).

**⚠️ ПОРЯДОК ПЕРЕГЛЯНУТО (рішення користувача 2026-07-24):** пріоритет — **дати людям
залогінитись, показати нормальне демо й зробити це англійською**. Level-up-и й нові банки —
ПІСЛЯ. Новий порядок:
`P0 Платформа (✅) → P3 Англійська → P4 Демо → P5 README/лендінг → (потім) P1.3 нові банки · P2 Level-up`.
- **P0 — зроблено** (лишилась лише твоя ops-звірка: Google Cloud, секрети, міграції — див.
  `PROGRESS.md §Ops-черга`). Це і є «логін для юзерів».
- **P1 банк-фундамент — зроблено** (P1.1 `BankProvider`+реєстр, P1.2 CSV-імпорт). **Реальні нові
  банки (PrivatBank, P1.3) свідомо ВІДКЛАДЕНО** — фундамент закладено, конкретні провайдери
  доробимо потім, коли дійдуть руки. Не блокує портфоліо.
- **P2 level-up (Goals/Accounts/Groups) — ВІДКЛАДЕНО** цілком на «після портфоліо».
- **Далі роблю: P3 → P4 → P5.** Демо після англійської — тверда залежність: рекрутер не прочитає
  українське демо, а пре-беки/стрічку в демо довелось би переписувати мовою аудиторії.
⚠️ Мета «портфоліо» СУПЕРЕЧИТЬ двом базовим рішенням (invite-only; свій ключ у кожного) —
розвʼязання в `PLATFORM.md §0.2`. **Не проєктувати демо як «ще одного юзера зі статусом demo».**

**Конвенції коду з цієї фази (`PLATFORM.md §14`, діє ВІДРАЗУ, не чекаючи P0):**
- **Коментарі в новому коді — англійською.** Наявні українські масово НЕ переписуємо; мігруємо
  принагідно, коли й так правимо файл.
- Коментувати **«чому саме так»** (яку альтернативу відкинуто, який баг закрито, який інваріант
  тримається), а не «що робить рядок». Обовʼязково — біля канону (`stats.ts`), обходів чужих вад
  і перевірок, що існують через невдачу.
- **Перевірка > інструкція.** Якщо коректність тримається на памʼяті розробника чи моделі —
  зробити детерміновану перевірку (як SQL-лінт, як `numbersAreGrounded`).

#### P0 — Платформа (блокер усього іншого)

**P0.1 — Directory + Google OAuth + модель юзерів** — ✅ **КОД ГОТОВИЙ (2026-07-24), чекає твоєї ops-звірки.**
Зроблено: D1 `directory` (`migrations-directory/0001`, `npm run db:dir:migrate:local|remote`),
`worker/lib/directory.ts` (users/invites CRUD, invite-only enforcement в ОДНОМУ місці —
`loginWithGoogle`), `worker/routes/auth.ts` (code-flow + `state`/`nonce`, валідація
`iss/aud/exp/nonce/email_verified`), `worker/lib/auth.ts` (сесія `v2.<userId>.<exp>.<hmac>`,
`verifySession` → `userId | null`), owner-only `worker/routes/admin.ts` (`/api/admin/users*` —
без нього invite-only нічим не скористатись), `/api/me` віддає юзера, `Login.tsx` із кнопкою
Google + людськими текстами відмов. Перевірено локально: невірний пароль, вхід, `/me`,
guard без куки, підроблений `state` → `?error=bad_state`, інвайт із нормалізацією регістру,
ідемпотентність інвайта, `cannot_disable_owner`.
**⚠️ Парольний вхід СВІДОМО лишається** до живої перевірки OAuth: він логінить owner-юзера
тим самим шляхом (сесія з `userId`). Зрізати його — окремим кроком у P0.2, інакше перша
крива галочка в Google Cloud замикає єдиного реального користувача поза робочим застосунком.
- **Ops-звірка (твоя):** 1) `wrangler d1 create directory` → вставити `database_id` у
  `wrangler.jsonc` (зараз плейсхолдер) → `npm run db:dir:migrate:remote`; 2) Google Cloud
  Console: OAuth client «Web application», redirect URI `https://<worker>/auth/google/callback`;
  3) секрети `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, `SESSION_SECRET`, `OWNER_EMAIL`
  (див. `.dev.vars.example`). **`OWNER_EMAIL` мусить збігатись із твоїм Google-акаунтом.**

**P0.2 — DO-скелет: міграції + повний шим** — ✅ **ЗРОБЛЕНО (2026-07-24).**
`worker/do/UserDO.ts` (схема будується в `blockConcurrencyWhile`), `worker/do/migrate.ts`
(реєстр `_mt_migrations`, ідемпотентний, по-стейтментно), `worker/do/migrations.generated.ts`
(ембед 31 файлу + стейл-перевірка в `npm run check`), `worker/lib/db-shim.ts`.
**`Env["DB"]` звужено з `D1Database` до `AppDb`** — тепер `tsc` падає на спробі вжити D1-фічу,
якої DO не має (`exec`/`withSession`/`dump`/`.raw()`), замість падіння в проді в одного юзера.
**Спайк-код прибрано (2026-07-25):** видалено `worker/routes/spike.ts`, `worker/do/spike-*.ts`,
біндинг `SPIKE_DB`, DO-методи `query/script/spikeProbe/migrationReport` + `execScript` шима, роут
`/api/__spike`. Мертвий P0.0-код і зайва поверхня в портфоліо-репо.

**P0.3 — Уся data-частина виконується ВСЕРЕДИНІ DO** — ✅ **ЗРОБЛЕНО (2026-07-24).**
Замість «проксі `db` з воркера в DO по RPC» (десятки round-trip на один аналітичний ендпоінт)
воркер став тонким роутером: автентифікує, резолвить `userId` і **форвардить незмінений
Request** у DO, а `worker/user-app.ts` виконується там із `env.DB` = локальний синхронний шим.
Ціна запиту — один хоп; канонічний SQL не змінився; **жоден із 427 call-site не правився**
(підміна робиться на рівні `env`). У воркері лишились ті, що працюють БЕЗ знання «чиї дані»:
health, login/OAuth, `/me`, owner-admin, статика.
Перевірено локально: створення рахунку й операцій, `/analytics/overview` рахує канон по DO,
невідомий `/api/*` віддає `not_found` із DO.

**P0.4 — Per-user секрети** — ✅ **КОД ГОТОВИЙ (2026-07-24).**
Міграція **0031** `user_secrets` (шифротекст + IV), `worker/lib/secrets.ts` (AES-GCM,
майстер-ключ `SECRETS_MASTER_KEY` — Worker-секрет, у БД не потрапляє), `worker/routes/credentials.ts`
(виконується в DO), `CredentialsCard` у Налаштуваннях. **Ключ ніколи не повертається клієнту** —
UI живе зі статусу `set` + `last_ok_at`. Значення **звіряється ДО запису** (mono `getClientInfo`,
Anthropic `count_tokens` — безкоштовний), інакше неправильний ключ виглядав би як правильний
і виплив би через півдня як «бекфіл нічого не робить».
Секрети підставляються в `env` при вході в DO — знову нуль правок у ~40 місцях читання ключа;
кеш у памʼяті DO скидається через `onSecretsChanged`.
Перевірено локально: збереження реального ключа зі звіркою, статус, видалення, переживання
рестарту (шлях розшифрування), **у `.wrangler/state` нема плейнтексту ключа**.
- **Лишилось (свідомо відкладено):** прибрати глобальні `MONO_TOKEN`/`ANTHROPIC_API_KEY` з env.
  Зараз вони — фолбек, коли в юзера свого ключа ще нема. Зрізати їх до P0.7 означає, що власник
  лишиться без AI і без синку на час міграції.

**P0.5 — Вебхук mono + TG у DO** — ✅ **ЗРОБЛЕНО (2026-07-24).**
Шлях вебхука тепер **підписаний і per-user**: `/webhook/<userId>.<hmac>` (`webhookToken`/
`verifyWebhookToken` у `lib/auth.ts`). Виведений, а не збережений: URL вебхука реєструється
раз і живе роками, тож таблиця мапінгу була б рядком, який не можна втратити, у базі, що не
належить юзеру. Підпис дає перевірку без жодного стану.
**Старий `WEBHOOK_SECRET` навмисно ЛИШЕНО** і мапиться на власника: зрізати його того ж дня
означало б тихо зламати URL, який monobank УЖЕ має зареєстрованим — події почали б віддавати
403, поки хтось не здогадається натиснути «зареєструвати вебхук», а операції за цей час
просто не прийшли б.
Обробник переїхав у `user-app` (виконується в DO); перевірка секрету лишилась у воркері —
всередині обʼєкта поняття «того самого секрету» вже не існує. TG-роут форвардиться в DO
**власника** (бот один, `TG_CHAT_ID` глобальний) — інакше після P0.3 бот тихо писав би у
стару глобальну D1.
`USER_ID` доїжджає в DO заголовком `x-mt-user`, який воркер **перезаписує** (не мержить) —
клієнт його підробити не може; потрібен, бо DO не може дізнатись власне імʼя (`idFromName`
односторонній), а `setWebhook` мусить зібрати саме свій URL.
Перевірено локально: подія з валідним токеном доїхала в DO і категоризувалась (АТБ→Продукти),
підроблений підпис → 403, чужий `userId` із нашим підписом → 403, старий секрет → 200.

**P0.6 — Крон на фан-аут + бекфіл на alarm + курси в спільний кеш** — ✅ **ЗРОБЛЕНО (2026-07-24).**
- **Курси — раз на всіх:** нова таблиця `shared_state` у **directory** (міграція
  `migrations-directory/0002`). Курс — факт про світ, а не про людину, і mono жорстко лімітує
  цей ендпоінт: тягнути його з кожного DO означало б N однакових запитів і відмови вже на
  кількох юзерах. Крон пише один раз, кожен DO копіює у свій `app_state.rates` — тож
  `getRates()` і канонічне зведення в ₴ не змінились.
- **⚠️ Свідоме відхилення від `PLATFORM.md §2` («усе на alarm»):** добові/тижневі/місячні
  задачі лишились **кроном із фан-аутом** у DO (`UserDO.runCron`). Причина: DO тримає рівно
  ОДИН відкладений alarm, тож три різні каденції зсередини вимагали б власного планувальника
  в кожному обʼєкті. На ~10-50 юзерів один добовий прохід — ніщо, а фан-аут значно легше
  спостерігати. Гілки ізольовані: впав репорт — сповіщення все одно запишуться, а `failed[]`
  вертається в лог із іменем юзера й задачі.
- **Бекфіл — таки alarm** (`UserDO.alarm`), і саме він виправдовує alarm: він тикає раз на 60с
  (ліміт mono 1 запит/60с) і лише в того, хто реально бекфілить. **Хвилинний крон прибрано з
  `wrangler.jsonc`** — як глобальний він будив би КОЖЕН обʼєкт 1440 разів на добу, щоб
  дізнатись, що роботи нема. Курсор у `app_state`, тож рестарт/евікшн продовжує, а не починає
  спочатку; помилка кроку теж перепланує alarm, інакше недороблений бекфіл кинуло б назавжди.
Перевірено локально: тригер добового крону → `shared_state.rates` заповнено, фан-аут пройшов
без `failed`. (Тижневий крон навмисно НЕ тригерив — він генерує платний AI-репорт.)

**P0.7 — Міграція даних власника в його DO** — ✅ **ЗРОБЛЕНО (2026-07-24). P0 ЗАКРИТО.**
`worker/do/import-legacy.ts` + owner-only `POST /api/admin/import-legacy`. Копіювання йде
**всередині DO**: обʼєкт має і свій шим, і сиру глобальну D1 у `this.env` — тож ні дампів,
ні чанкованого аплоаду, ні скрипта з двома креденшелами.
Правила, кожне з яких закриває спосіб втратити дані:
- **спершу читаємо все, потім пишемо** — напівпрочитане джерело + напівзаписаний приймач це
  єдиний стан, з якого нема чистого виходу;
- **один `transactionSync` + `PRAGMA defer_foreign_keys`** — FK перевіряються на коміті, тож
  порядок таблиць не має значення, а битий звʼязок валить увесь імпорт, а не лишає сиріт;
- **`INSERT OR REPLACE`** — DO вже прогнав міграції, отже вже має сідовані категорії з 0002;
  реальні дані мусять перемогти сід;
- **відмова, якщо в DO вже є транзакції** — злиття двох історій подвоїло б кожне число канону
  без жодного способу розрізнити рядки потім;
- **`ok` означає, що зійшлись ГРОШІ**, а не «нічого не кинуло»: звіряються кількість
  транзакцій, сума `amount` і сума балансів рахунків до/після.
Перевірено локально: імпорт у порожній DO — 5 tx, сума −14230, баланси 150000, усе збіглося
точно; застосунок одразу читає імпортоване; повторний запуск і запуск на непорожньому DO
відмовлено з поясненням.

#### P1 — Банк-абстракція

**P1.1 — `BankProvider` + реєстр + mono під інтерфейс** — ✅ **ЗРОБЛЕНО (2026-07-24).**
Міграція **0032**: `accounts.provider` (+`connection_id`) і таблиця `bank_connections`.
`provider` **бекфілиться з `is_manual`**, а не ставиться дефолтом усім: готівка й ручні картки
— не monobank, і позначити їх так означало б, що синк рахунків спробує перетерти введений
руками баланс даними з API, яке про них не чуло.
`worker/lib/banks/` — `provider.ts` (інтерфейс+реєстр), `mono.ts` (адаптер над наявним
клієнтом), `manual.ts`, `csv.ts`, `index.ts` (реєстрація). `setup.ts` більше не кличе mono
напряму — ходить через `getProvider("mono")`.
**Інваріант, заради якого це існує:** знак/валюта/копійки нормалізуються рівно в ОДНОМУ місці
на провайдера. Усі валютні баги цього проєкту виникли там, де друге місце вирішувало, що
означає число.

**P1.2 — CSV-імпортер** — ✅ **ЗРОБЛЕНО (2026-07-24).**
`worker/lib/banks/csv.ts` + `worker/routes/import.ts` + `CsvImportCard` у Налаштуваннях.
**Два кроки навмисно** (`/preview` нічого не пише, `/commit` пише): одноетапний імпорт означав
би, що криво вгадану колонку суми видно вже після того, як місяць кривих чисел зайшов у канон
— а вдруге ту саму виписку ніхто не перечитує.
- **Роздільник детектиться по ЗАГОЛОВКУ**, не по файлу: коми й крапки з комою всередині лапок
  на довгій виписці переголосували б справжній роздільник.
- **Суми:** `-1 234,56` (нерозривні пробіли), `(89,50)` (бухгалтерський мінус), `1.234,56` і
  `1,234.56` — десятковий роздільник визначається як ОСТАННІЙ із `.`/`,`; >2 цифр після нього
  означає, що це був розряд. Округлення рівно одне, одразу в копійки.
- **Дати:** `dd.mm.yyyy [hh:mm[:ss]]`, ISO, unix. Не розпізнав — рядок **пропускається з
  причиною і показується**, бо тихо проковтнутий рядок = зникла операція.
- **Дедуп по ХЕШУ ВМІСТУ** (`sha256(account|time|amount|description)` як id) + `INSERT OR
  IGNORE`: люди імпортують «останні 3 місяці» щомісяця, і перекриття не має дублювати покупки.
  Жодного обліку «чи я вже вантажив цей файл» — він усе одно був би хибним при перекритті.
- **MCC мапиться теж** — знайдено на прогоні: без нього імпорт лягав купою безкатегорійних
  рядків, хоча банк прямо сказав, що це за покупка (детермінований категоризатор тримається
  саме на 188 MCC-правилах).
Перевірено локально на реальному форматі: роздільник `;` вгадано, «Сума в валюті картки» не
програла «Залишку», `-1 234,56`→−123456, `(89,50)`→−8950, крива дата пропущена з причиною,
повторний імпорт дав `inserted:0, duplicates:4`, категорії проставились (5411→Продукти,
4121→Транспорт).


> **P1.3 PrivatBank і весь P2 Level-up ВІДКЛАДЕНО** (рішення 2026-07-24) — картки перенесені
> в кінець, у «⏸ Відкладено до кінця портфоліо-фази». Фундамент банків (P1.1/P1.2) уже є, тож
> нові банки додаються поверх нього без переробок.

#### P3 — Англійська версія (`PLATFORM.md §12`) · залежить від P0 · **РОБИМО ЗАРАЗ**

**P3.1 — Каркас i18n + локаль як параметр системи** — ✅ **ЗРОБЛЕНО (2026-07-24).**
`src/i18n/locale.ts` (ЄДИНЕ місце BCP-47 тегів — `localeTag()`; модульна `getLocale/setLocale`, тож
format-хелпери лишили сигнатури), `src/i18n/index.ts` (`t()` з типізованими ключами `keyof typeof en`,
`{param}`-інтерполяція, `LocaleProvider/useT/useLocale`), `src/i18n/{en,uk}.json` (стартові словники),
сервер `/settings/locale` (`app_state.locale` у DO), `scripts/check-i18n.mjs` у `npm run check`
(хардкод-тег + парність словників). 44 наявних `uk-UA` у 25 файлах переведено на `localeTag(getLocale())`.
Layout: nav через `t()` + перемикач `UA/EN`. Деталі — `PROGRESS.md`. **check+build зелені.**

**P3.2 — Переклад клієнта** — ✅ **ЗРОБЛЕНО (перевірено 2026-07-26).**
`node scripts/find-i18n-todo.mjs` дає 27 рядків, і всі 27 — свідомо неперекладні: матч-ключі
й регекси (`brands.tsx`, `merchant.ts` `.includes("фоп"/"банка")`, `/переказ|зняття/i` у
`Stats.tsx`/`TxDetail.tsx`, регекс валют у `highlight.tsx`). Перекласти їх означало б зламати
матчинг. `npm run check` тримає парність en/uk. **`I18N_GUIDE.md` видалено** — інструкція
виконавцю для завершеної фази; чинне правило «що не перекладати» живе в `CLAUDE.md §i18n`.

**P3.3 — Локалізація тексту, що ЛЕЖИТЬ У БД** — ✅ **ЗРОБЛЕНО (2026-07-24).**
Генератори `notify.ts` пишуть `tkey`+`tparams` (JSON) замість готової фрази; фраза збирається
при читанні в поточній локалі. **ЄДИНЕ джерело композиції — `shared/notif-i18n.ts`**
(`renderNotif(locale,key,params)`, чистий модуль, імпортують і воркер, і клієнт — single-source
§14.2). Гроші/дати форматуються в локалі НА РЕНДЕРІ (params несуть сирі копійки/unix). Клієнт
(`Notifications.tsx` `Row`) ре-рендерить стрічку живо на перемиканні мови (`useLocale`); TG-пуш
рендерить у локалі власника на момент відправлення. Міграція **0033** додає `notif_key`/
`notif_params`; старі `title`/`body` лишились як fallback (legacy-рядки + вільнотекстовий `ai`).
14 шаблонів; `ai` — без шаблону (вільний текст моделі). check+build зелені, обидві локалі
прогнано (uk байт-у-байт як було). **Категорії в params лишаються мовою юзера — це P3.4.**

**P3.4 — Категорії + AI-локаль** — ✅ **ЗРОБЛЕНО (2026-07-24).**
- **Категорії — СЕРВЕР-РЕЗОЛВ у локалі власника** (рішення користувача). Реалізовано БЕЗ
  `name_key`-колонки й БЕЗ міграції: `worker/lib/categories-i18n.ts` тримає `CAT_EN` (сідова uk-назва
  → en) + `catNameSql(locale, expr)` (інлайн-CASE, як `uahMult`; no-op для uk) + `localizeCatName`
  (JS). Резолв keyed по ЗНАЧЕННЮ назви — чиста функція сідового рядка, тож нуль плюмбінгу; юзер-назва
  (не в мапі) проходить як є = «перейменував → його назва». Middleware в `api` кладе локаль власника
  в `c.get("locale")` раз/запит; ~15 SQL-продюсерів category_name обгорнуто `catNameSql`, `oneoff_items`
  зі stats.ts пост-локалізовано в хендлері. **Клієнт незмінний**; перемикання мови інвалідує RTK-теги
  (refetch), бо строку вже зрезолвлену не перекласти назад.
- **AI-локаль — лише user-facing** (рішення користувача): `replyLangDirective(env)` (no-op для uk;
  для en — емфатична пізня директива, що перекриває інлайн «українською», не чіпаючи кеш-стабільний
  блок персони) додано в chat/advisor/report/insight/budget/notify. Enrich/OCR/parse НЕ чіпані.
- **Свідомо НЕ в скоупі:** назви категорій у КОНТЕКСТІ для AI лишаються uk (знімок серверний, не UI);
  модель отримує «respond in English» і перекладає сама. EN-версії доків корпусу — окремий хвіст.
- **Готово-коли:** ✅ сідовані категорії англійською при `en` (Stats/Tx/Budgets/Categories/пошук);
  ✅ AI-текст англійською. check+build зелені; `catNameSql`/`localizeCatName` прогнано (модиф.-апостроф теж).

#### P4 — Демо-акаунт (`PLATFORM.md §11`) · залежить від P0 + P3

**P4.1 — Демо-датасет** — ✅ **ЗРОБЛЕНО (2026-07-25).**
`scripts/seed-demo.mjs` (детермінований, seeded PRNG) → `worker/demo/dataset.json`. **351 tx / 184 дн /
8 рахунків усіх типів** (кредитка з боргом+`payment_day`, крипта+брокер `role=investment`, jar, cash,
USD/EUR картки), 6 підписок (Netflix із подорожчанням, Adobe «мертва»), 2 цілі (одна відстає →
`goal_risk`), подія «Kraków weekend» з бюджетом, 5 бюджетів (over/warn/ok від реальних сум місяця),
2 спліти, **компенсація v2 (одне надходження покриває ДВІ витрати)**, пара-переказ, рефанд (§REFUND),
3 зняття готівки з `real_category_id`, 12 без категорії (`todo`), receipts+items, 46 точок
`health_history`, курси, `locale='en'`, `finance_profile`. **Час — абсолютний, anchored `meta.anchor`;
контракт ребейсу для P4.2 у `meta.timeFields`** (зсув на `nowAtLoad − anchor`, найновіша tx ≈ сьогодні).
Інваріанти прогнано (спліт=сума, компенсація≤витрати, пара, рефанд, uncat≥10): funds — cushion ₴245k /
debt ₴1.2k / invest ₴274k, runway ≈ 7.5 міс. check зелений.
- **Хвіст для P4.2:** лоадер (replay + ребейс часу + recompute `health_history.day` з `ts`).

**P4.2 — Ефемерний демо-DO + прапорець** — ✅ **ЗРОБЛЕНО (2026-07-25).**
- `GET /demo` (worker, ВІДКРИТИЙ) → `newDemoId()` → DO `demo:<id>` → `stub.seedDemo(now)` (лоадер
  `worker/do/demo-load.ts` ребейсить час, FK-порядок, `db.batch`) → підписана demo-cookie
  (`mt_demo`, 24h, окремі `createDemoToken`/`verifyDemoToken`) → redirect `/`. **Namespace `demo:`
  фізично відрізаний від реальних юзерів** (голий hex) — cookie одного типу НІКОЛИ не резолвить
  DO іншого. `resolveRequestUser` (session → demo) у guard і `/me` (demo → `{demo:true}`).
- **Self-destruct:** `seedDemo` ставить `app_state.demo_expires_at` + `ctx.storage.setAlarm(+24h)`;
  `UserDO.alarm()` бачить маркер → `reset()` (не backfill — демо не має mono). **Orphan-sweep**
  (evicted alarm): directory-міграція **0003 `demo_sessions`** + добовий крон-прохід (`reset()` +
  видалення рядка). Обидва best-effort (try/catch: без таблиці — лишається alarm-only).
- **Guard в ОДНОМУ місці** — denylist-middleware в `user-app.ts` (isDemo з `env.USER_ID` `demo:`-
  префікса): блокує НЕ-GET до `/api/{credentials,setup,import,admin}` (зовнішні ефекти/секрети),
  читання відкриті. AI-кап — P4.3.
- **Готово-коли:** ✅ дві вкладки = два random-id = дві пісочниці; ✅ 24h alarm + sweep стирають.
  check+build зелені; лоадер прогнано (439 стейтментів=439 рядків, час ребейснуто, binds примітивні).
- **⚙️ Ops:** `npm run db:dir:migrate:remote` (і `:local`) тепер тягне 0003 `demo_sessions`.

**P4.3 — Ліміти AI в демо (3 рівні + пре-беки)** — ✅ **ЗРОБЛЕНО (2026-07-25).**
- **Кап (`worker/lib/demo.ts` `demoAiGate`, викликається перед КОЖНИМ fetch до Anthropic у 3
  точках ai.ts):** (1) per-session лічильник у демо-DO `app_state.demo_ai_count` (кап 12);
  (2) **ГЛОБАЛЬНИЙ денний** — атомарний `INSERT…ON CONFLICT DO UPDATE …+1 RETURNING` у directory
  `shared_state` (кап 300/добу; race-safe, бо RETURNING дає число цього виклику). Спільний KV не
  знадобився — directory `shared_state` уже є. IP-кап покривається глобальним (багато сесій×малий
  ліміт = глобальний ловить). No-op для реальних юзерів.
- **Примусово Haiku:** `getTaskModel` віддає `MODEL_FAST` для демо (ігнорує `ai_model_*`).
- **DEMO-ключ:** `appEnv` для демо ставить `ANTHROPIC_API_KEY = DEMO_ANTHROPIC_KEY ?? глобальний`
  + `MONO_TOKEN=""` (сандбокс ніколи не дістане банк). Env += `DEMO_ANTHROPIC_KEY?`, `.dev.vars.example`.
- **Важкі гілки:** демо не в directory users → крон-фан-аут (репорти/insight) його НЕ будить;
  backfill-alarm — демо-гілка `alarm()` виходить рано; setup/import/credentials заблоковані guard-ом (P4.2).
- **Кап вичерпано → чесне повідомлення** (`DemoAiLimitError`, текст видно через `errText`): «shared
  AI budget used up — the examples on screen are real, pre-generated».
- **Пре-беки в датасеті (повний вигляд при $0):** 6 сповіщень (5 детермінованих + 1 `kind='ai'`,
  unread → бейдж), 2 `ai_reports` (week+month, валідний `FinancialReport` JSON), `app_state.advisor`
  (StoredAdvice: facts/suggestions/create_budget-action) + `advisor_history` (3 точки тренду), 8
  `ai_note` на операціях. (Insight свідомо пропущено — картка має empty-state.)
- **Готово-коли:** ✅ сесія понад ліміт → пояснення; ✅ глобальний кап зупиняє ВСІ демо-сесії.
  check+build зелені; лоадер прогнано (449 стейтментів=449 рядків, +6 notif +2 report).
- **⚙️ Ops:** секрет `DEMO_ANTHROPIC_KEY` (опц.); при регенерації датасету — `node scripts/seed-demo.mjs`.

**P4.4 — Демо-банер** — ✅ **ЗРОБЛЕНО (2026-07-25).**
`DemoBanner` у `Layout` (на кожному екрані, sticky над топбаром, `.has-demo-banner` зсуває топбар).
Показ за `/me.demo`; `/me` віддає `demo_expires_at` (з exp демо-cookie) → зворотний відлік годин.
Текст i18n (`demo.*` — badge/fictional/resets/aiLimited/exit): «Demo · data is fictional · resets in
Nh · AI is limited» + «Exit demo» (logout → reload `/`). Non-dismissable (нема close). Аватар-чіп теж
demo-aware («Demo»/«D»). Токени `--warn`/`--warn-soft`. check+build зелені.

#### P5 — Портфоліо-обгортка (`PLATFORM.md §13`)

**P5.1 — `README.md` англійською + діаграма** — ✅ **ЗРОБЛЕНО (2026-07-25).**
Створено `README.md` (був відсутній): що це / фічі / **архітектура з mermaid** (Worker → DO-на-юзера →
D1/R2/Anthropic/Mono, directory-D1) + «чому DO-на-юзера / чому лишається швидко / демо як той самий DO»
/ **розділ «Built with AI — and kept honest»** (канон `stats.ts`, `numbersAreGrounded`, SQL-лінт,
гейт фактів, типізовані i18n-ключі, робочі документи) / стек-таблиця / локальний запуск + green-бар /
структура проєкту / статус. Mermaid без `\n`/entities (тільки `<br/>`) — рендериться на GitHub.
- **⏳ Хвіст:** демо-посилання (плейсхолдер-домен — вставити реальний після деплою) і **скріншоти зі
  справжнього демо-датасету** (робить користувач із живого `/demo`; папка `docs/screenshots/`).

**P5.2 — Лендінг `/` для незалогінених** — ✅ **ЗРОБЛЕНО (2026-07-25).**
`src/pages/Landing.tsx` — hero (H1 «doesn't lie about the numbers» + sub + 2 CTA: «Try the demo»→`/demo`
primary, «Sign in»→показує `Login`) + 3 фіче-картки (correct-by-construction / AI-kept-honest /
yours-isolated) + футер + перемикач UA/EN. `App.tsx` гейт `LoggedOut`: лендінг за замовч., `Login` за
кліком АБО коли OAuth повернув `?error=` (щоб причина була видна). i18n `landing.*`, CSS `.lp-*`/`.btn.lg`
на токенах. **Готово-коли:** ✅ незалогінений бачить лендінг, не форму входу. check+build зелені.

_Решту дрібного беремо з «Ідей фіч» нижче._

---

#### ⏸ Відкладено до кінця портфоліо-фази (P3→P4→P5)

> Свідомо не робимо зараз (рішення 2026-07-24). Повертаємось ПІСЛЯ англійської + демо + README.
> Фундамент під це вже закладено — додавання не потребує переробок.

**P1.3 — PrivatBank провайдер (poll)** — фундамент готовий (P1.1 реєстр + `mode:"poll"` у типі)
- **Ціль:** тягнути виписку Приват24 у DO через alarm-polling.
- **Файли:** `worker/lib/banks/privat.ts` (новий) + реєстрація в `banks/index.ts`, `worker/do/UserDO.ts` (poll-alarm).
- **Кроки:** 1) ⚠️ **звірити актуальний особистий API PrivatBank** (токен/merchant-id/формат/ліміти);
  2) реалізувати `BankProvider` з `mode:"poll"` + `poll(since)`; 3) DO-alarm тягне дельту; 4) нормалізація лише в провайдері.
- **Готово-коли:** тестовий приват-рахунок синкається; або — якщо API незручний — задокументований fallback на CSV (уже є).

**P2.1 — Goals lvl-up** (`PLATFORM.md §6`)
- **Ціль:** з плоского `current_amount` — у живий інструмент.
- **Файли:** `goal_contributions`-таблиця (міграція), `worker/routes/api.ts` (`/goals*`), `src/pages/Goals.tsx`, `GoalCard`.
- **Кроки:** 1) історія внесків (current_amount = SUM); 2) авто-поповнення (round-up / % доходу / фікс);
  3) типи (save-up/debt-payoff/sinking-fund) → математика ETA; 4) статус on-track/behind (звʼязати з `goal_risk` у `notify.ts`); 5) зняття/пауза.
- **Готово-коли:** графік прогресу з внесків; ETA/статус рахуються; авто-поповнення резервує в What-if/Пораднику.

**P2.2 — Accounts lvl-up** (`PLATFORM.md §7`) — ✅ **ЗРОБЛЕНО (2026-07-25).**
`Accounts.tsx` групує по ІНСТИТУЦІЇ (`accounts.provider`), не по типу: одна група на реальний банк
(усі його рахунки — картки+банки+ФОП разом, `BANK_LABEL` mono→Monobank/privat→PrivatBank), решта
(manual/csv) — типові бакети (Manual/ФОП/Банки). Групи сортуються за ₴-підсумком (банк із зарплатою
вгорі), кожна з підсумком (`Section`). `Account`-тип += `provider`. Прапорці картки (editable/renameable/
muted) винесено з рівня секції в per-account (`is_manual`/`type==='jar'`), тож групування type-agnostic.
Демо: Monobank(White,Black,jar) + Manual(Cash,USD,EUR,Crypto,Brokerage). check+build зелені.

**P2.3 — Groups lvl-up** (`PLATFORM.md §8`) — ✅ **CORE ЗРОБЛЕНО (2026-07-25).**
- ✅ **План vs факт:** міграція **0034 `event_planned`** (лайн-айтеми плану, суми в ₴); `GET /events/:id`
  віддає `planned`+`planned_total`; `POST/DELETE /events/:id/planned`; `EventPlan` на сторінці —
  смуга план↔факт, список айтемів, форма додавання, тег «Лишилось/Перевитрата».
- ✅ **Шаблони** (подорож/проєкт/подія) — клієнтські пресети лайн-айтемів (i18n `evt.tpl.*`), сідяться
  через той самий endpoint при порожньому плані.
- ✅ **Мультивалюта** — план у ₴, факт уже ₴-зведений (0025), тож порівняння коректне.
- **⏸ Відкладено:** (3) привʼязка події до цілі — залежить від **P2.1**; (5) AI-close-out на числах
  плану — 1-рядкове розширення `/events/:id/ai` (context += planned_total), зробити після P2.1.

---

## 📌 Хвости / беклог

### Тех-борг (дрібне, з відомим корінням)
_Порожньо._

### Від юзера
_Порожньо — усі три пункти закриті 2026-07-24 (підсумок у `CLAUDE.md §Зроблено`)._

**Хвости, що лишились із цього блоку:**
- Бейдж «компенсовано» в СПИСКУ транзакцій (`TxItem`) — зараз §COMPENSATION видно лише в
  деталях операції. Треба довезти `reimbursed` у `/transactions` і намалювати чип.
- Юзер-аплоад **PDF** у корпус знань (зараз лише .md/.txt через читання на клієнті) — див.
  «Хвости AI 4.0» нижче, це той самий пункт.

### Архітектурне / потребує рішення
- **«AI думає між сторінками»** — фонове продовження аналізу при навігації (Durable Object / фонові задачі). L.
- **UI-редактор правил** `rules` — зараз правки лише через код/БД. Перетинається з «Правила-автотеги».
- **Lazy-enrich** (варіант B) — за узгодженням.
- **Мульти-валютні групи/подорожі** (CUR2-хвіст): лишився TG-бот (аналітика й самі групи вже зведені в ₴ — 2026-07-21).
- **Prompt-кеш verify** на проді (`cache_read` у логах) — потребує деплою. Перетин із live-звіркою §A5.

### Безпека — хвости аудиту 2026-07-26 (знайдено, свідомо не робилось зараз)
- **Відкликання сесії.** Сесія stateless, тож вкрадена кукі валідна до 30 днів, а «вимкнути
  юзера» діє через ≤60с (кеш `userAccess`). Правильний фікс — `token_version` у directory,
  вшитий у підпис: інкремент = миттєвий вихід з усіх пристроїв. S, без міграції даних юзера.
- **Rate limiting для РЕАЛЬНИХ юзерів відсутній** (демо своє має). Дешевий шлях — правило
  Cloudflare WAF на `/api/*` (ops, не код); у коді — лише якщо знадобиться per-endpoint.
- **`__Host-` префікс для cookie сесії** — зараз просто `mt_session`. Перейменування розлогінить
  усіх наявних, тож робити разом із `token_version`.
- **Тексти помилок віддають сиру причину** (свідоме рішення §Обробка помилок). Якщо колись
  зʼявляться сторонні юзери поза колом друзів — сховати `detail` за owner-прапорцем.

### Мультипровайдерний AI (кол. L6 — відкладено 2026-07-26, «колись потім»)
Дати юзеру вибір Anthropic / OpenAI / Grok замість жорсткої привʼязки. **Не «на ізі»:**
розходяться tool-use (`runToolConversation` написаний під блоки Anthropic), prompt-кеш
(`cache_control ttl:1h` — на ньому тримається економіка корпусу §A5), серверний `web_search`
і таблиці цін `priceFor`. **Дешевий шлях:** адаптер лише для одноходових викликів
(enrich/OCR/parse/insight), агентний чат лишити на Anthropic; Grok і DeepSeek дають
OpenAI-сумісний ендпоінт, тож один адаптер відкриває кількох.
**Готово-коли:** ключ будь-якого підтримуваного провайдера дає робочу категоризацію, а UI
чесно каже, які фічі доступні для цього провайдера.
⚠️ Зачепить `demoClamp` (`ai.ts`) — він знає лише моделі Anthropic; чужий провайдер у демо
має бути або так само затиснутий, або заборонений.

### Хвости AI 4.0 (відкладено свідомо)
- Юзер-аплоад власних PDF/MD у корпус знань (R2 + D1).
- Нативний PDF `document`-блок (100 стор Haiku / 600 Sonnet) + `citations`.
- `output_config.format` (structured outputs) замість хака `repairTruncatedJson`.

---

## 🎨 Дизайн — майбутнє
> Як працювати — `DESIGN.md §10.0` (skills: `impeccable` основний, `review-animations` для руху).
> Кожну зміну фіксувати в «Журналі рішень» DESIGN.md.
- **Статистика** — редизайн вкладок **Огляд + Категорії/Тренди**. Потрібен live-скрін вкладок (§0-воркфлоу).
- **Сторінки ще не переглянуті вживу:** Порадник, Підписки, Бюджети, Рахунки, Категорії.
  Чек-лист: локальний хардкод-колір / тінь / `transition:all` / тіснота.
- **Розмітка для >1920px** — як стоять блоки на широких моніторах.
- **Скелетони завантаження** — примітив `Skeleton`/`SkeletonRows`/`StatsSkeleton` (`src/components/Skeleton.tsx`)
  + CSS `.skeleton` (shimmer). **Застосовано на Статистиці** (головний loading + ліниві панелі дрилу).
  Лишилось адаптувати на інших сторінках (Порадник/Підписки/Бюджети/Рахунки) — там фетч по-карткова,
  тож підставляти `<Skeleton>` у кожен блок точково.

## 🔵 TG-бот (не пріоритет)
Швидкий запис витрати текстом/фото, запити балансу/статистики. Каркас є, активація за TG-секретами.
Пуш подій у TG — не тут, а в Черзі №2 (фаза 2 Центру сповіщень): туди йде лише severity >= warn.
⚠️ `proactive.ts` (тижневий TG-пуш) досі рахує бюджети НЕ по канону (`t.hold=0 AND t.is_transfer=0`
замість `SPEND_WHERE`) → його цифри розходяться зі стрічкою й Статистикою. Звести при фазі 2.

---

## 💡 Ідеї фіч — брейнсторм для тріажу
> Сирий список для пріоритезації. **[нове]** / **[лвл-ап]** / **[тех]**; розмір S/M/L — груба оцінка.
> Реалізоване звідси видаляється (не викреслюється).

### A. ФОП / податки (на паузі — рішення користувача)
- **[нове] Податковий конверт-автовідкладання (S/M).** З кожного доходу відкладати % (ЄП 5% + ЄСВ + ВЗ)
  у віртуальний конверт → видно, скільки з балансу насправді «не твоє».
- **[нове] Податковий календар ФОП (M).** Дедлайни ЄП/ЄСВ/ВЗ із сумами, відлік, нагадування; «сплачено» → списання з конверта.
- **[нове] Трекер річного ліміту доходу ФОП (S).** Прогрес-бар до ліміту групи + прогноз перевищення за темпом.
- **[нове] Розрахунок ЄП за квартал (S).** Дохід × ставка; валютний дохід — за курсом НБУ на дату надходження.
- **[нове] Валютний дохід ФОП — курс НБУ на дату (M).** Для USD/EUR-інвойсів фіксувати офіційний курс дня зарахування.
- **[лвл-ап] Позначка «робочі витрати» (S).** Тег бізнес/особисте → окремий підсумок (корисно для маржі).

### B. Аналітика та прогноз

### C. Порадник / AI / автоматизація
- **[лвл-ап] Більше метрик усередині поради (S).** Норма заощаджень цього міс, топ-категорія-драйвер
  зростання, «на що пішла різниця vs минулий міс».
- **[нове] Щомісячний авто-огляд у стрічку (M).** AI генерує підсумок 1-го числа й кладе в стрічку/пуш.
- **[нове] Розумні цілі-виклики (S).** AI пропонує реалістичний виклик («−15% на доставку = +1200₴») і трекає.

### D. Бюджети / цілі / заощадження

### E. Транзакції / введення / категоризація
- **[нове] Розстрочка як окрема сутність (M).** Хвіст §SPLIT: спліт зробили, розстрочку — ні.
- **[нове] Правила-автотеги в UI (M).** Мерчант містить X → категорія Y + тег Z, із превʼю впливу.

### F. Сповіщення / проактивність
- **[лвл-ап] Достроїти TG-бота (M).** Запис витрати текстом/фото, запит балансу. Пуш подій — **зроблено**
  (Центр сповіщень шле `severity >= warn`).
- **[нове] Web-Push (PWA) (M).** Свідомо відкладено: VAPID-ключі, перехід vite-plugin-pwa на
  `injectManifest` із власним SW (`push`/`notificationclick`), таблиця підписок, підпис JWT через
  Web Crypto у Worker. На iOS працює лише для PWA, доданої на «Додому». Зараз ту саму роль грає TG-пуш.

### G. Валюти / інвестиції / нетворт
- **[лвл-ап] Вплив курсу окремою цифрою (S).** Базу зроблено (`rate_history` 0024, нетворт рахує
  кожну точку курсом своєї дати). Лишилось показати «скільки заробило/коштувало коливання на
  USD-залишках» — розділити зміну нетворту на «рух грошей» і «рух курсу». Має сенс, коли
  історія накопичиться за кілька місяців.

### H. UX / продуктивність / пошук
- **[нове] PWA-шорткати й шейр-таргет (S).** «Додати витрату» з домашнього екрана; поділитись фото чека.
- **[нове] Онбординг-чеклист (S).** Прогрес налаштування (рахунки→вебхук→бекфіл→курси→профіль).
- **[лвл-ап] Плавність дашборду (S).** Дотягнути наявні анімації, прибрати ривки.

### I. Дані / надійність / приватність
- **[нове] Бекап/відновлення (M).** Експорт-імпорт усього стану D1 у файл; ручний бекап у R2.
- **[нове] Пасткод/біометрія на вхід (S).** Локальний лок PWA (додатково до `APP_PASSWORD`).
- **[тех] Audit-лог AI-змін (S).** Коли AI змінив категорію/переказ — журнал + «відкотити».
