# ROADMAP — жива черга задач і фіч

> **Це постійний файл. Тут — ЛИШЕ те, що ще треба зробити.** Зроблене звідси **видаляється**,
> а стислий підсумок переїжджає в `CLAUDE.md §Зроблено`. Правила — `CLAUDE.md §Робочий процес`.
> Довідник/інваріанти → `CLAUDE.md`. Дизайн → `DESIGN.md` (§8 «Журнал рішень» + §10–11).
>
> Останнє прибирання: **2026-07-31** (винесено ВСЮ закриту платформа-фазу — ~300 рядків ✅-карток,
> що дублювали `HISTORY.md`. Правило: картка зі статусом ✅ у цьому файлі не живе взагалі.)

## 🚦 Як працювати з цим файлом

1. Бери **найвищу невиконану задачу** з «Черги». Не перестрибуй без причини.
2. Задача = картка: **Ціль · Файли · Кроки · Готово-коли**. Спочатку прочитай згадані файли.
3. **Green-бар перед «готово»:** `npm run check` (tsc + SQL-лінт) + `npm run build`.
   Гроші/статистика — лише через `worker/lib/stats.ts`, не дублюй SQL.
4. **UI/UX** — спершу `DESIGN.md`, зміну фіксуй у його «Журналі рішень». Хук impeccable ганяє
   детектор — знайдене виправляй, не глуши.
5. **Доробив → ВИДАЛИ картку звідси**, підсумок у `CLAUDE.md §Зроблено`. Нове/баг → у «Беклог» чи «Ідеї фіч».
6. **Live-звірку в чергу НЕ писати.** Користувач ганяє її сам і повідомляє, якщо щось не так.
   AI-залежну задачу просто здавай зеленою.
7. Один PR / логічний блок = одна задача.

---

## 🔥 Черга (роби згори вниз)

### 🔴 Хвости живого запуску (2026-07-26)

> Зібрано з живої перевірки в проді після першого деплою платформи.
> *(L5, L7, L8, L9 закриті — див. `CLAUDE.md`. L9 зробив `worker/lib/platform/i18n.ts st()`,
> L7 — `docs/screenshots/` у README. Хвіст L7: `.lp-flow` на лендінгу досі мальований
> плейсхолдер замість справжнього скріна — перенесено в §Дизайн.)*

*(D1 — Telegram для всіх юзерів: ✅ зроблено 2026-08-01. Правила — `CLAUDE.md §Безпека §D1`.
**Хвіст:** вхідні КОМАНДИ бота (`/balance`, `/last`, запис витрати текстом) лишились
owner-only — щоб роутити їх за чатом, потрібен індекс `chat_id → user` у directory. Вихідні
пуші персональні вже зараз. Це і є те, що лишилось від §TG-бот нижче.)*

---


### ✅ Платформа-фаза — ЗАКРИТА (запущено в проді 2026-07-26)

> Мультиюзер (DO-на-юзера, Google OAuth, per-user ключі, вебхук/крон у DO) · банк-фундамент
> (`BankProvider` + CSV-імпорт) · англійська · демо · README+лендінг — **усе зроблено**.
> Покартковий журнал і ТЗ фази — `HISTORY.md` (архіви `PROGRESS.md` + `PLATFORM.md`);
> durable-правила з неї — `CLAUDE.md`. Тут вони більше не лежать: 300 рядків ✅-карток,
> які ніхто не читає, ховали те єдине, заради чого цей файл існує — невиконане.
>
> **Свідомо НЕ зроблено з тієї фази:** реальні нові банки (P1.3) і level-up Goals (P2.1) — нижче.

#### ⏸ Відкладено до кінця портфоліо-фази (P3→P4→P5)

> Свідомо не робимо зараз (рішення 2026-07-24). Повертаємось ПІСЛЯ англійської + демо + README.
> Фундамент під це вже закладено — додавання не потребує переробок.

**P1.3 — PrivatBank провайдер (poll)** — фундамент готовий (P1.1 реєстр + `mode:"poll"` у типі)
- **Ціль:** тягнути виписку Приват24 у DO через alarm-polling.
- **Файли:** `worker/lib/banks/privat.ts` (новий) + реєстрація в `banks/index.ts`, `worker/do/UserDO.ts` (poll-alarm).
- **Кроки:** 1) ⚠️ **звірити актуальний особистий API PrivatBank** (токен/merchant-id/формат/ліміти);
  2) реалізувати `BankProvider` з `mode:"poll"` + `poll(since)`; 3) DO-alarm тягне дельту; 4) нормалізація лише в провайдері.
- **Готово-коли:** тестовий приват-рахунок синкається; або — якщо API незручний — задокументований fallback на CSV (уже є).

**P2.1 — Goals lvl-up** — кроки 1-3 ✅ (2026-08-01), решта відкрита
- ✅ **Історія внесків** (міграція 0036 `goal_contributions`, `current_amount` = SUM, ЄДИНИЙ
  писар — `recalcGoal`). Знак значущий: + внесок, − зняття, тож п.5 «зняття» теж закрито.
  Ціль-банка внесків не приймає (джерело правди — баланс рахунку), сервер це відхиляє.
- ✅ **Авто-поповнення** (фікс / % доходу) + **типи цілі** (save_up/debt_payoff/sinking_fund) —
  міграція 0037, `worker/lib/finance/goals.ts`. Внески пише добовий крон, ідемпотентно за
  МІСЯЦЕМ (`autofill_last_ym`), `source='auto'`. Round-up лишився поза: він потребує гачка на
  кожній транзакції, а не місячного проходу — це інша механіка, не варіант того самого поля.
- ⬜ **Статус on-track/behind** — звʼязати з `goal_risk` у `notify.ts`.
- ⬜ **Графік прогресу** з історії внесків (зараз історія — списком).
- ⬜ Два хвости P2.3, що чекали саме на це: привʼязка події до цілі + AI-close-out на числах плану.

*(P2.2 Accounts lvl-up і P2.3 Groups core — ✅ зроблено 2026-07-25, підсумки в `HISTORY.md`.
З P2.3 лишились два хвости, обидва ЧЕКАЮТЬ на P2.1: привʼязка події до цілі та AI-close-out на
числах плану — однорядкове розширення `/events/:id/ai`, `context += planned_total`.)*

---

## 📌 Хвости / беклог

### Тех-борг (дрібне, з відомим корінням)
_Порожньо._

### Від юзера

**🔴 ARCH — рефактор структури під «одна цифра = одне місце в коді»**
> Аналіз, реєстр дублювання, цільова архітектура й повний план — **`ARCHITECTURE.md`**
> (створено 2026-08-03). Тут — лише картка-трекер. Рішення на старті:
> характеризація → потім рух · строго поведінко-зберігаюче · провайдери = тільки шви.

- **Ціль:** прибрати кореневу причину класу багів «різні функції бачать різні дані»
  (§CUR-PLAN, §SUB-MONTH, §REFUND, §SPLIT — усі чотири мають один механізм).
  Виміряно: `api.ts` = 3331 рядок / **179 `.prepare()`** / 129 роутів, хоча `CLAUDE.md`
  каже «routes = транспорт, lib = логіка». Розрізати файл без виносу SQL — косметика.
- **Файли:** `worker/routes/api.ts` · новий `worker/repo/*` · `shared/types.ts` (зараз
  **0 імпортів із воркера** — контракт фіктивний) · `src/store/api.ts` (86 руками
  написаних типів) · `worker/lib/ai/ai.ts` (6 відповідальностей) · `scripts/check-*.mjs`.
- **Кроки (фази, деталі в `ARCHITECTURE.md §6`):**
  0. ✅ **Golden-тести — ЗРОБЛЕНО 2026-08-03.** `worker/test/` (harness над `node:sqlite` як третя
     реалізація `AppDb` + фікстура + 36 знімків). Тестів 23 → **59**. Продакшн-код не змінювався:
     час заморожено підміною `Date.now`, бо `stats.ts` уже бере `now` параметром. Не вакуумні —
     дві мутації канону валять 12/36 і 11/36. Перезапис: `UPDATE_GOLDEN=1 npm test`.
  0б. ✅ **Golden-тести на ЗАПИСИ — 2026-08-04.** `worker/test/writes.test.ts`, 32 сценарії
     (11 із них — гілки помилок). Знімається не лише відповідь, а СТАН БД після запису: у запису
     відповідь зазвичай `{ok:true}`, і сама по собі не доводить нічого. Тестів → **92**.
     Не вакуумні — три мутації валять 4/2/2. Побічно виявили дефект у самій фікстурі: вона сіяла
     `reimbursed`/`reimburses_total` БЕЗ рядків `tx_reimbursements`, з яких вони виводяться —
     стан, недосяжний у проді, через який стеля «компенсація > витрати» не тестувалась узагалі.
  1. ✅ **`api.ts` ЧИСТИЙ — 2026-08-05.** Виніс 179 запитів у `repo/` **без зміни тексту SQL** +
     лінт «`.prepare()` лише в `repo/`» (`scripts/check-repo-layer.mjs`, храповик: стеля не
     росте ніколи, а якщо реальне число впало — білд червоний, поки не знизиш бюджет).
     **`api.ts`: 179 → 0**, рядок ВИДАЛЕНО з мапи бюджетів, тобто діє вже не храповик, а пряма
     заборона. Партії C (категорії, 21 — каскад видалення), D (планування: бюджети/репорти/
     підписки/події, 30) і E (рахунки, корпус знань, експорти, 19) закриті цією сесією.
     Тестів 92 → **169**; жоден наявний golden не зрушив. `catNameSql` сам випав з імпортів
     `api.ts` — `tsc` повідомив без підказки, як і про канон раніше.
     Побічно знайдено ДРУГИЙ дефект фікстури: `txSeq` був модульним, тож id транзакцій залежали
     від того, СКІЛЬКИ сценаріїв відпрацювало раніше — додавання сценарію перезаписувало golden
     несуміжних. Це вчить перезаписувати не читаючи, тобто знецінює весь набір. Полагоджено.
     ⚠️ **Лишилось 10 запитів у інших роут-файлах** (`import.ts` 3, `telegram.ts` 3, `setup.ts` 2,
     `webhook.ts` 2) — вебхук, бот, легасі-імпорт. У них НЕМА жодного характеризаційного покриття,
     і помилка там виглядає не як червоний знімок, а як транзакція, що тихо не приїхала. Спершу
     сценарії на кожну точку входу. Журнал сесій — `ARCHITECTURE.md §10`.
  3. ✅ **`api.ts` БІЛЬШЕ НЕ ІСНУЄ — 2026-08-07.** 138 роутів → **16 файлів у `routes/api/`**
     (найбільший 769 рядків, було 3331); `index.ts` — таблиця монтування на 66 рядків, власних
     роутів не оголошує. Групування за ПЕРШИМ СЕГМЕНТОМ шляху, а не за змістом: один файл володіє
     цілим префіксом, і правило «літерал вище параметризованого» перевіряється читанням ОДНОГО
     файлу, а не міркуванням про порядок монтування. Створено `worker/services/` — туди пішли три
     обробники-ПОСЛІДОВНОСТІ (`PATCH /transactions/:id`, перерахунок компенсацій, каскад видалення
     категорії). Межа шару: сервіс НАЗИВАЄ помилку (`errReimbCurrency`), роут її СЛОВЕСНО оформлює
     через `st(locale)` — правила в одному місці, код статусу й мова в іншому.
     Лінт C1 став рекурсивним (інакше кожен новий `routes/api/*.ts` тихо вийшов би з-під заборони,
     а чек і далі малював би галочку) і накрив `services/` — там бюджету нема й не буде.
     **Лінт C3 (`scripts/check-route-size.mjs`)**: 400 рядків на файл у `routes/`+`services/`,
     два записані винятки, які можуть лише падати. Свідомо НЕ двобічний храповик, як C1: рядки
     міняються на кожній правці, і червоний білд через «файл став на 3 рядки коротший» вчить
     правити бюджет не читаючи. Тестів — **169**, жоден знімок не зрушив.
  2. ✅ **КОНТРАКТ ЗАКРИТО — 2026-08-07.** `shared/api/` (6 файлів, дзеркалять `routes/api/*`) —
     ЄДИНЕ оголошення форми кожної відповіді. Клієнт: `src/store/api.ts` 1287 → **809 рядків, 0**
     власних типів (імпорт + ре-експорт, бо 66 файлів уже беруть імена звідти).
     **Друга половина — воркер**, без неї контракт однобічний: `repo/` повертає контрактні типи
     (26 `Record<string, unknown>` зникли), хендлери анотують `satisfies`.
     Що виявив сам `tsc`, коли обидві сторони нарешті стали порівнюваними:
     • **4 «напівтвіни»** (`AccountRow`, `CategoryRow`, `GoalRow`, `FactRow`) — кожен перелічував
       ЧАСТИНУ колонок + люк `[key: string]: unknown`, хоча запит під ним — `SELECT *`. Тобто
       клієнт вважав, що отримує `provider`/`iban`/умови кредитки, тип у репо казав, що їх нема,
       і зіставити це не міг ніхто;
     • `localizeCatName` розширював назву категорії до `string | null`, хоча колонка `NOT NULL` —
       тип, що каже МЕНШЕ за базу, і змушує викликача написати власну вужчу копію;
     • `periodTotals` було типізовано nullable дарма (агрегат без GROUP BY завжди дає рядок), а
       `n` усередині нього виявився nullable по-справжньому — цього не знав ніхто.
     **Лінти C2+C4** (`check-api-contract.mjs`) — тепер зелені ВСІ шість перевірок C1–C6.
     Тестів 169 → **209**: новий golden-прогін по ПОРОЖНІЙ базі (стан кожного новачка й демо
     перші хвилини) — раніше не покритий нічим.
  4. ✅ **ДУБЛІ КОНСОЛІДОВАНО — 2026-08-07.** D1 власні кошти → `lib/finance/own-funds.ts`;
     D4 `period_mode` → `getPeriodMode()`; D5 — три подвійні читання курсів У МЕЖАХ однієї
     відповіді (`collectFinanceSnapshot`/`financeHealth`/`budgetChatReply` читали курси й у тому
     самому `Promise.all` кликали те, що читає їх ЗНОВУ) закриті передачею вже прочитаного знімка.
     **Фаза планувалась як «та, що змінює числа» — і не змінила жодного.** Усі 4 серверні копії
     власних коштів рахували одне й те саме, тож голдени не зрушили; розбіжність, записана в
     реєстрі, була між сервером і КЛІЄНТОМ (виправлено ще 2026-08-03).
     ⚠️ **Реєстр помилявся про «шосту, інвертовану копію»** (`notify.ts:316`): вона рахує БОРГ —
     іншу величину, правильно. Урок записано в `ARCHITECTURE.md §3 D1`: реєстр дублювання — це
     доказ, а не вирок; звіряти треба «чи це та сама ВЕЛИЧИНА», а не «чи схожий вираз».
     Повний `RequestContext` свідомо НЕ вводився: він потребує протягнути параметр крізь усі
     сигнатури `lib/` — великий ризикований дифф заради трьох місць виклику.
  5. ✅ **`ai.ts` РОЗШАРОВАНО — 2026-08-07. 1335 → 212 рядків, лише транспорт.**
     L2/L3/L4/L5 → `cost.ts`/`models.ts`/`json.ts`/`prompt.ts`; L6 поїхав у ті фіча-файли, що
     ВЖЕ існували (`report.ts`, `insight.ts`, `enrich.ts`, `receipt.ts`) — саме це й було
     аномалією: `generateFinancialReport` жив у `ai.ts`, хоча поруч лежав 330-рядковий
     `report.ts`, і без правила «куди що кладемо» фіча розмазувалась по обох. Розмовні виклики
     (порадник, чат по операції, бюджет-чат, вердикт по події, спостереження у стрічку) — новий
     `tasks.ts`. **Шов провайдера — `json.ts`**: усе вище нього вже провайдер-агностичне.
     ⚠️ `demoClamp` лишився Anthropic-only свідомо: правило «затискай там, де `fetch`, а не на
     виклику» — це те, що робить його незабутнім, а провайдер-обізнаність потребує ДРУГОГО
     провайдера, щоб було про що знати.
- **Готово-коли:** `npm run check` зелений із новими лінтами C1–C5; golden-тести до копійки
  збігаються з допровідними; жодного `.prepare()` поза `repo/`; ✅ `api.ts` більше не існує.

**🔴 ARCH-SEC — безпека + підготовка до опен-сорсу** *(частково зроблено 2026-08-03)*
> ⚠️ Репо **вже публічне** (`github.com/ITalik-gr/money-track`), тому гігієна даних виконана
> ПЕРШОЮ, а не в кінці. `HISTORY.md` перевірено — він у `.gitignore`, ніколи не публікувався.
- ✅ Реальні суми з живого акаунта прибрано з `CLAUDE.md`/`DESIGN.md`/`README.md` (урок
  збережено, число зникло); прибрано імʼя третьої особи з прикладу P2P.
- ✅ `SECURITY.md` + `CONTRIBUTING.md` створено. `LICENSE` уже був (MIT).
- ⬜ Повний скан історії git секрет-сканером (`gitleaks`/`trufflehog`, не `git log -S`).
- ⬜ `/security-review` по НОВІЙ структурі — після ARCH, бо периметр переїде разом із кодом.
- ⬜ **Рішення власника:** (а) чи переписувати історію git — прибрані суми лишились у старих
  коммітах; (б) чи виносити D1 `database_id` із `wrangler.jsonc`.
- **Чек-лист:** `ARCHITECTURE.md §8`, рішення — `§9`.

**⚠️ Знайдено самоперевіркою Фази 2 (2026-08-07) — тех-борг, виміряно:**
- **Стрічка операцій шле поля, потрібні лише сторінці операції.** `GET /transactions` віддає
  **38 полів на рядок**, а контракт (`TxRow`) описує **28**. Зайві 10 — `raw_json`, `ai_note`,
  `alerted`, `name_locked`, `reimburses_total`, `balance_after`, `cashback`, `receipt_id`,
  `created_at`, `ai_enriched` — усі читає лише `TxDetail` (одна операція), а не список.
  **Виміряно на фікстурі: 24% відповіді** (8 460 з 35 719 байт на 50 рядках). У проді частка
  більша: `raw_json` у фікстурі порожній, а в реальних mono-операціях це повний payload банку —
  тобто сирі дані банку їдуть у КОЖНОМУ рядку списку, який їх не показує.
  **Причина:** `SELECT t.*` у `listFeed`. **Фікс:** перелічити колонки явно.
  ⚠️ Змінює відповідь → перезаписати golden і перевірити, що жоден компонент не читає їх з
  рядка СПИСКУ (у `TxDetail` вони лишаються).
  *(Показово, чому це не спіймалось анотацією: `satisfies` доводить, що всі оголошені поля Є, але
  не доводить, що інших немає — excess-property-check діє лише для об'єктних літералів, а тут
  spread рядка з `repo/`. Записано в `shared/api/index.ts`.)*

**⚠️ Знайдено характеризацією ВЕБХУКА (2026-08-07) — рішення потрібне:**
- **Подія по рахунку, якого ще нема в базі, ВТРАЧАЄТЬСЯ (500).** `transactions.account_id` —
  `NOT NULL REFERENCES accounts(id)`, а FK у Durable Object увімкнені, тож `upsertMonoTx` падає,
  вебхук віддає 500 і транзакція не зберігається. Зафіксовано голденом
  `__golden__/ingest/webhook-an-event-for-an-unknown-account-...json`.
  **Коли це реально:** відкрив нову картку / банку → mono шле подію ДО того, як застосунок
  зробив синк рахунків. Mono ретраїть, тож зазвичай подія доїде пізніше — але це залежить від
  його політики ретраїв, а не від нас, і мовчазна залежність такого роду і є проблема.
  **Варіанти:** (а) створювати рахунок-заглушку з події й доповнювати на синку; (б) складати
  такі події в чергу й програвати після синку; (в) лишити як є, але свідомо — тоді записати
  ЧОМУ і зняти питання.
  *(Показово: жоден із 209 тестів цю гілку не бачив — ingest-шлях не мав покриття взагалі,
  саме тому фаза 1 і не чіпала його 10 запитів.)*

**⚠️ Знайдено під час аналізу — баг, чиниться ОКРЕМО (не в ARCH):**
- **Кредитка в боргу дає дві різні цифри.** `src/pages/Accounts.tsx:31-33` затискає власні кошти
  в нуль (`Math.max(own, 0)`), а сервер (`finance.ts:155`, `advisor.ts:64`, `api.ts:1612`) —
  ні. Тобто сторінка Рахунків показує `0`, а подушка/нетворт рахують від'ємне.
  **Рішення потрібне:** яка поведінка правильна. Деталі — `ARCHITECTURE.md §3 D1`.

**Хвости, що лишились із цього блоку:**
- Юзер-аплоад **PDF** у корпус знань (зараз лише .md/.txt через читання на клієнті) — див.
  «Хвости AI 4.0» нижче, це той самий пункт.
*(Бейдж «компенсовано» в списку операцій — закрито ще 2026-07-27: `/transactions` віддає
`reimbursed` (`SELECT t.*`), а `TxItem` веде скориговану суму зі списаною під нею закресленою.
Картка просто не була видалена.)*

### Архітектурне / потребує рішення
*(«AI думає між сторінками» — ✅ зроблено 2026-08-01 (§A6: `ai_jobs` + alarm-планувальник +
чіп у топбарі). Правила — `CLAUDE.md §Фонові задачі`. **Хвіст:** масові прогони (ре-світ,
батч-enrich) у чергу НЕ заводили — у них інша природа, прогрес у %, а не «готово».)*
- **UI-редактор правил** `rules` — зараз правки лише через код/БД. Перетинається з «Правила-автотеги».
- **Lazy-enrich** (варіант B) — за узгодженням.
- **Мульти-валютні групи/подорожі** (CUR2-хвіст): лишився TG-бот (аналітика й самі групи вже зведені в ₴ — 2026-07-21).
- **Prompt-кеш verify** на проді (`cache_read` у логах) — потребує деплою. Перетин із live-звіркою §A5.

### Безпека — хвости аудиту 2026-07-26
*(Відкликання сесії + `__Host-` — ✅ 2026-08-01, directory 0005. Rate limiting — ✅ 2026-08-01:
дві стелі на юзера в `worker/index.ts` (загальна + вужча на AI-шляхи). ⚠️ Вікно живе в памʼяті
ІЗОЛЯТА, тож реальна стеля — на ізолят; правило Cloudflare WAF на `/api/*` лишається доречним
доповненням з боку ops.)*
- **Тексти помилок віддають сиру причину** (свідоме рішення §Обробка помилок). Якщо колись
  зʼявляться сторонні юзери поза колом друзів — сховати `detail` за owner-прапорцем.

### Мультипровайдерний AI (кол. L6 — відкладено 2026-07-26, «колись потім»)
Дати юзеру вибір Anthropic / OpenAI / Grok замість жорсткої привʼязки. **Не «на ізі»:**
розходяться tool-use (`runToolConversation` написаний під блоки Anthropic), prompt-кеш
(`cache_control ttl:1h` — на ньому тримається економіка корпусу §A5), серверний `web_search`
і таблиці цін `priceFor`. **Дешевий шлях:** адаптер лише для одноходових викликів
(enrich/OCR/parse/insight), агентний чат лишити на Anthropic; Grok і DeepSeek дають
OpenAI-сумісний ендпоінт, тож один адаптер відкриває кількох.
**Готово-коли:** ключ будь-якого підтримуваного провайдера дає робочу категоризацію, а UI
чесно каже, які фічі доступні для цього провайдера.
⚠️ Зачепить `demoClamp` (`ai.ts`) — він знає лише моделі Anthropic; чужий провайдер у демо
має бути або так само затиснутий, або заборонений.

### Хвости AI 4.0 (відкладено свідомо)
- Юзер-аплоад власних PDF/MD у корпус знань (R2 + D1).
- Нативний PDF `document`-блок (100 стор Haiku / 600 Sonnet) + `citations`.
- `output_config.format` (structured outputs) замість хака `repairTruncatedJson`.

---

## 🎨 Дизайн — майбутнє
> Як працювати — `DESIGN.md §10.0` (skills: `impeccable` основний, `review-animations` для руху).
> Кожну зміну фіксувати в «Журналі рішень» DESIGN.md.
- **Статистика** — редизайн вкладок **Огляд + Категорії/Тренди**. Потрібен live-скрін вкладок (§0-воркфлоу).
- **Сторінки ще не переглянуті вживу:** Порадник, Підписки, Бюджети, Рахунки, Категорії.
  Чек-лист: локальний хардкод-колір / тінь / `transition:all` / тіснота.
- **Розмітка для >1920px** — як стоять блоки на широких моніторах.
- **Справжній скрін продукту в hero лендінгу** (хвіст L7): `.lp-flow` — свідомо зроблений слот
  під реальний знімок, зараз там мальована схема. Знімки вже є в `docs/screenshots/`.
*(Скелетони завантаження — ✅ ЗАКРИТО 2026-08-01: додано `GoalGridSkeleton`/`GroupGridSkeleton`/
`CategoryGridSkeleton`, підключено на Цілях, Групах і Категоріях. Ті три сторінки читали дані з
дефолтом `= []`, тож поки запит летів, показували порожній стан («Цілей ще немає») або взагалі
білий екран — тобто порожній акаунт і незавершене завантаження виглядали однаково.
**Правило лишається: скелет повторює КЛАСИ справжнього блоку.**)*

## 🔵 TG-бот (не пріоритет)
Швидкий запис витрати текстом/фото, запити балансу/статистики. Каркас є, активація за TG-секретами.
Пуш подій у TG — вже зроблено й **персональний** (§D1, 2026-08-01).
**Що лишилось саме тут:** вхідні команди для НЕ-власника. Потрібен індекс `chat_id → user_id`
у спільній directory — інакше воркер не знає, чий обʼєкт будити на `/balance` від довільного
чату (вихідним пушам індекс не потрібен: обʼєкт знає свій chat_id сам).
*(Розходження цифр `proactive.ts` закрито 2026-07-31: бюджети рахує канонічний `budgetStatus`
зі `stats.ts` — те саме джерело, що й стрічка сповіщень.)*

---

## 💡 Ідеї фіч — брейнсторм для тріажу
> Сирий список для пріоритезації. **[нове]** / **[лвл-ап]** / **[тех]**; розмір S/M/L — груба оцінка.
> Реалізоване звідси видаляється (не викреслюється).

### A. ФОП / податки (на паузі — рішення користувача)
- **[нове] Податковий конверт-автовідкладання (S/M).** З кожного доходу відкладати % (ЄП 5% + ЄСВ + ВЗ)
  у віртуальний конверт → видно, скільки з балансу насправді «не твоє».
- **[нове] Податковий календар ФОП (M).** Дедлайни ЄП/ЄСВ/ВЗ із сумами, відлік, нагадування; «сплачено» → списання з конверта.
- **[нове] Трекер річного ліміту доходу ФОП (S).** Прогрес-бар до ліміту групи + прогноз перевищення за темпом.
- **[нове] Розрахунок ЄП за квартал (S).** Дохід × ставка; валютний дохід — за курсом НБУ на дату надходження.
- **[нове] Валютний дохід ФОП — курс НБУ на дату (M).** Для USD/EUR-інвойсів фіксувати офіційний курс дня зарахування.
- **[лвл-ап] Позначка «робочі витрати» (S).** Тег бізнес/особисте → окремий підсумок (корисно для маржі).

### B. Аналітика та прогноз

### C. Порадник / AI / автоматизація
- **[нове] Розумні цілі-виклики (S).** AI пропонує реалістичний виклик («−15% на доставку = +1200₴») і трекає.

### D. Бюджети / цілі / заощадження

### E. Транзакції / введення / категоризація
- **[нове] Розстрочка як окрема сутність (M).** Хвіст §SPLIT: спліт зробили, розстрочку — ні.
- **[нове] Правила-автотеги в UI (M).** Мерчант містить X → категорія Y + тег Z, із превʼю впливу.

### F. Сповіщення / проактивність
- **[лвл-ап] Достроїти TG-бота (M).** Запис витрати текстом/фото, запит балансу. Пуш подій — **зроблено**
  (Центр сповіщень шле `severity >= warn`).
- **[нове] Web-Push (PWA) (M).** Свідомо відкладено: VAPID-ключі, перехід vite-plugin-pwa на
  `injectManifest` із власним SW (`push`/`notificationclick`), таблиця підписок, підпис JWT через
  Web Crypto у Worker. На iOS працює лише для PWA, доданої на «Додому». Зараз ту саму роль грає TG-пуш.

### G. Валюти / інвестиції / нетворт
- **[лвл-ап] Вплив курсу окремою цифрою (S).** Базу зроблено (`rate_history` 0024, нетворт рахує
  кожну точку курсом своєї дати). Лишилось показати «скільки заробило/коштувало коливання на
  USD-залишках» — розділити зміну нетворту на «рух грошей» і «рух курсу». Має сенс, коли
  історія накопичиться за кілька місяців.

### H. UX / продуктивність / пошук
- **[нове] Шейр-таргет для ФОТО чека (M).** Текстовий share-target і шорткати — ✅ зроблено
  2026-08-01. Фото лишилось: файловий share-target МУСИТЬ бути `method: "POST"`, а POST
  доставляється у fetch-хендлер service worker'а — тобто перехід із `generateSW` на
  `injectManifest` і власний SW. Той самий розмін, що вже відкладено для Web-Push.
- **[нове] Онбординг-чеклист (S).** Прогрес налаштування (рахунки→вебхук→бекфіл→курси→профіль).
- **[лвл-ап] Плавність дашборду (S).** Дотягнути наявні анімації, прибрати ривки.

### I. Дані / надійність / приватність
- **[нове] Бекап/відновлення (M).** Експорт-імпорт усього стану D1 у файл; ручний бекап у R2.
- **[нове] Пасткод/біометрія на вхід (S).** Локальний лок PWA (додатково до `APP_PASSWORD`).
- **[тех] Audit-лог AI-змін (S).** Коли AI змінив категорію/переказ — журнал + «відкотити».
