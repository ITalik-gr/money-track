# ROADMAP — жива черга задач і фіч

> **Це постійний файл. Тут — ЛИШЕ те, що ще треба зробити.** Зроблене звідси **видаляється**,
> а стислий підсумок переїжджає в `CLAUDE.md §Зроблено`. Правила — `CLAUDE.md §Робочий процес`.
> Довідник/інваріанти → `CLAUDE.md`. Дизайн → `DESIGN.md` (§8 «Журнал рішень» + §10–11).
>
> Останнє прибирання: **2026-07-26** — винесено ВСЮ платформа-фазу (P0/P1/P3/P4/P5, ~360 рядків
> закритих карток; підсумки в `CLAUDE.md §Зроблено`), чергу пересортовано на блоки A–F, дублі злито.

## 🚦 Як працювати з цим файлом

1. Бери **найвищу невиконану задачу** з «Черги». Не перестрибуй без причини.
2. Задача = картка: **Ціль · Файли · Кроки · Готово-коли**. Спочатку прочитай згадані файли.
3. **Green-бар перед «готово»:** `npm run check` (tsc + SQL-лінт) + `npm run build`.
   Гроші/статистика — лише через `worker/lib/finance/stats.ts`, не дублюй SQL.
4. **UI/UX** — спершу `DESIGN.md`, зміну фіксуй у його «Журналі рішень». Хук impeccable ганяє
   детектор — знайдене виправляй, не глуши.
5. **Доробив → ВИДАЛИ картку звідси**, підсумок у `CLAUDE.md §Зроблено`. Нове/баг → у «Беклог» чи «Ідеї фіч».
6. **Live-звірку в чергу НЕ писати.** Користувач ганяє її сам і повідомляє, якщо щось не так.
   AI-залежну задачу просто здавай зеленою.
7. Один PR / логічний блок = одна задача.

---

## 🔥 Черга (роби згори вниз)

> Один блок = один пріоритет. Усередині блоку — теж згори вниз. **Готово → картку ВИДАЛИТИ**,
> підсумок у `CLAUDE.md §Зроблено`. Порядок переглянуто 2026-07-26 (після аудиту безпеки).

### 🔴 A. Блокер — деплой

**A1 — Задеплоїти накопичене.** У проді працює код ДО трьох сесій правок: фіксів безпеки
(owner-only ключі й TG-пуш, відкликання доступу, CSP, ліміти демо, CSV-injection), видалення
акаунта, лендінгу, порожніх станів, доменної структури. **Поки не задеплоєно — дірки живі.**
Кроки: `npm run check` + `npm run build` → `npm run deploy`. Міграцій НЕМАЄ (нічого не
накочувати). Після — жива перевірка в браузері: консоль на CSP-violation, `/demo`, вхід Google.

---

### 🟠 B. Баги з живого прогону (користувач бачив на власні очі)

**B1 — Немає індикації при кліку «Спробувати демо».** Сторінка біліє на кілька секунд:
`GET /demo` синхронно сіє DO (~350 транзакцій) і лише потім редіректить, а це звичайний
`<a href>` — браузер не показує нічого. **Кроки:** перехопити клік на клієнті, показати стан
кнопки («Готуємо пісочницю…» + спінер/скелетон) і вже тоді переходити; врахувати відповідь
**429** (стеля нових пісочниць — `demoSandboxAllowed`) окремим людським текстом.
**Файли:** `src/pages/Landing.tsx`, `worker/index.ts` (`/demo`). **Готово-коли:** від кліку до
дашборда видно прогрес, а вичерпаний ліміт пояснено, а не показано білим екраном.

**B2 — CSV-експорт «виглядає зламаним» у таблиці.** Причини (перевірено по коду
`worker/routes/api.ts` `/export/transactions.csv`, всі — про локаль Excel):
1. **роздільник `,`** — Excel в українській/європейській локалі чекає `;` і кладе весь рядок
   в ОДНУ колонку. Найімовірніша причина побаченого;
2. **десяткова крапка** у `Сума` (`toFixed(2)`) — у тій самій локалі читається як текст,
   колонка не сумується;
3. шапка й «так» — українською навіть в EN-версії (перетин із B3);
4. побічний ефект anti-formula-injection: значення, що починається з `-`/`=`/`+`/`@` і не є
   чистим числом, отримує префікс `'` — у Excel це видно в рядку формул.
**Рішення (обрати одне, не змішувати):** або рядок `sep=;` першим і `;`+кома як десятковий
роздільник, або лишити RFC-4180 (`,`+крапка) і додати підказку «відкривати через Імпорт даних».
**⚠️ Не втратити:** BOM (кирилиця), CRLF, екранування лапок і захист від формул — усе це вже
працює й тестувалось. **Готово-коли:** файл відкривається подвійним кліком у Excel і Google
Sheets, колонки роздільні, `Сума` сумується.

**B3 — Українські рядки з СЕРВЕРА при вибраній англійській** (кол. L9). Перевірено 2026-07-26:
клієнт чистий (єдина кирилиця в рендері — мапа серверних значень у ключі, `IncomeBreakdown`),
тече з воркера. Точний список:
- `api.ts:200,208` — шапка CSV-експорту («Дата, Мерчант…», «так»);
- `api.ts:1259,1264` — caveats під графіком нетворту (довгі речення, дуже помітні);
- `api.ts:1528,1755` · `insight.ts:39` · `report.ts:125` · `notify.ts:269` — фолбеки
  «без категорії» / «Інше» (лізуть у графіки й списки);
- `api.ts:1534,1540` — мітки стабільності доходу; «мало даних» не має мапи на клієнті;
- `api.ts:493,922,2089` — тексти помилок і `fallback_reason`;
- `credentials.ts:53,59` · `ingest.ts:15` — повідомлення в Налаштуваннях;
- `csv.ts:211,216,220,291` — причини пропуску рядків імпорту + мітка джерела;
- `insight.ts:86` — «За обраний період витрат не було».
**НЕ чіпати:** промти моделі, матч-ключі (`transfers.ts`, `REFUND_PREFIXES`, аліаси заголовків
CSV), TG-бот (owner-only, українською свідомо).
**Механізм:** локаль на сервері вже є (`c.get("locale")`, `ownerLocale`); зробити словник за
патерном `shared/notif-i18n.ts`. **НЕ ліпити `if (locale === "en")` по місцях.**
**Готово-коли:** сканер (той, яким збирався цей список) не знаходить кирилиці у відповідях API,
крім промтів і матч-ключів.

---

### 🟡 C. Безпека — те, що аудит лишив свідомо

**C1 — Rate limiting для реальних юзерів.** Зараз стелі має ЛИШЕ демо; автентифікований юзер
може довбати будь-який ендпоінт без обмежень, а дорогі — це AI-роути й аналітика.
**Дешевий шлях (ops, не код):** правило Cloudflare WAF на `/api/*`.
**У коді — лише якщо треба per-endpoint:** лічильник у DO (він однопотоковий, тож без гонок),
окремо жорсткіший для `/advisor/*`, `/chat`, `/reports/generate`.
**Готово-коли:** сплеск запитів від одного акаунта впирається у стелю, а не в рахунок Anthropic.

**C2 — Миттєве відкликання сесії (`token_version`).** Сесія stateless: вкрадена кукі валідна
30 днів, «вимкнути юзера» діє через ≤60с (кеш `userAccess`). Фікс: `token_version` у directory,
вшитий у підпис; інкремент = вихід з усіх пристроїв. **Заразом** перейменувати кукі на
`__Host-mt_session` (префікс дає гарантії path/domain) — обидві зміни розлогінюють усіх, тож
робити ОДНИМ кроком, не двома. S, без міграції даних юзера.

**C3 — Бекап даних** (кол. L10). Дані живуть в одному Durable Object; є лише ручний CSV
операцій. **Мінімум:** кнопка «вивантажити все» — JSON-дамп власного DO (закриває найгірший
сценарій дешево). **Максимум:** періодичний експорт у R2 за розкладом.
Дублює ідею «Бекап/відновлення» з §I — вести тут.

---

### 🟣 D. Продукт

**D1 — Telegram для КОЖНОГО юзера (не лише власника).** Зараз бот — одна глобальна інсталяція,
привʼязана до `TG_CHAT_ID` власника; після аудиту 2026-07-26 усі пуш-шляхи гейтяться
`env.IS_OWNER`, а controls прибрані з Налаштувань для не-власників і демо (інакше кнопки або
відповідали б 403, або брехали, що налаштовують ТВІЙ бот). **Це тимчасовий стан, не кінцевий.**
- **Ціль:** кожен юзер підключає свій чат і отримує СВОЇ сповіщення; демо показує флоу підключення.
- **Модель:** `TG_BOT_TOKEN` лишається один (один бот на застосунок) — персональним стає **chat_id**.
  Звʼязування: юзер тисне «Підключити Telegram» → отримує **одноразовий код** (короткоживучий,
  підписаний, як `signShortLived`) → шле боту `/start <код>` → воркер резолвить код у `userId`
  і пише `tg_chat_id` у DO ТОГО юзера. Так chat_id доводить володіння, а не вводиться руками.
- **Файли:** `worker/routes/telegram.ts` (роутинг апдейту за chat_id → потрібен реверс-мап
  chat_id→userId у `directory`, бо воркер має знати, чий це DO ДО входу в обʼєкт),
  `worker/lib/messaging/{notify,alert,proactive}.ts` (замінити `env.TG_CHAT_ID` на per-user),
  `worker/lib/platform/directory.ts` (таблиця `tg_links`), `src/pages/Setup.tsx` (повернути картку
  всім, з новим флоу), `worker/index.ts` (`/tg/*` більше не «завжди власник»).
- **⚠️ Пастки:** (1) allowlist `TG_CHAT_ID` зараз ЄДИНИЙ захист від сторонніх апдейтів — замінити
  на «chat_id має бути звʼязаний», інакше бот почне відповідати будь-кому; (2) `IS_OWNER`-гейти
  в трьох файлах треба зняти САМЕ тоді, коли зʼявиться per-user chat, не раніше;
  (3) демо: підключати реальний Telegram із пісочниці не можна — показувати флоу до кроку коду
  й зупинятись (або мокати), інакше сторонні чати чіплятимуться до ефемерних DO.
- **Готово-коли:** двоє різних юзерів отримують кожен свої сповіщення; демо показує екран
  підключення без реального звʼязування; жоден `env.TG_CHAT_ID` не лишився в шляхах відправки.

---

### 🟢 E. Інженерна якість

**E1 — Тести на канон `stats.ts`.** У проєкті НУЛЬ автотестів (є лише два саморобні лінти —
SQL-джоїни та i18n-парність, обидва вже окупились). Найдорожче непокрите — канон розрахунків:
рефанди (§REFUND), спліт (§SPLIT), компенсації (§COMPENSATION), ролап валют, вагомість.
**Кроки:** vitest + фікстура-набір транзакцій → перевіряти `spendSum`/`incomeSum`/`EFF_AMOUNT`
на відомих числах; окремо — «спліт не подвоює суму» й «компенсація не зникає з обліку».
**Чому це наступне за важливістю:** ці інваріанти вже ЛАМАЛИСЬ у проді (регресія §SPLIT,
завищений дохід на 10 776 ₴), і кожен раз ловились очима. Плюс для портфоліо: відсутність
тестів видно швидше за будь-яку фічу.

---

### 🔵 F. Портфоліо

**F1 — Скріншоти** (кол. L7). Знімки з живого `/demo` у `docs/screenshots/` для README; на
лендінгу місце вже підготовлене — `.lp-flow` у hero свідомо зроблений слотом під реальний скрін.

---

### ⚪ G. Ops (твоє, не код)

**G1** — (а) `POST /api/admin/import-legacy` — перенести стару історію (417 tx / 9 рахунків) у
свій DO, інакше акаунт порожній. (б) Google Cloud: redirect URI
`https://money-track.vitaliy-50a.workers.dev/auth/google/callback` + усі пошти в **Test users**,
поки застосунок у Testing. (в) `OWNER_EMAIL` = `italik.dev@gmail.com` — заходити саме ним.
✅ `DEMO_ANTHROPIC_KEY` — поставлено 2026-07-26.

---

### ⏸ Відкладено свідомо (фундамент є, повертаємось після черги вище)

**P1.3 — PrivatBank провайдер (poll).** Реєстр (`P1.1`) і `mode:"poll"` у типі готові.
- **Файли:** `worker/lib/bank/providers/privat.ts` (новий) + реєстрація в `providers/index.ts`,
  `worker/do/UserDO.ts` (poll-alarm).
- **Кроки:** 1) ⚠️ звірити АКТУАЛЬНИЙ особистий API PrivatBank (токен/merchant-id/формат/ліміти);
  2) `BankProvider` з `poll(since)`; 3) DO-alarm тягне дельту; 4) нормалізація лише в провайдері.
- **Готово-коли:** тестовий приват-рахунок синкається; або задокументований fallback на CSV.

**P2.1 — Goals lvl-up** (`PLATFORM.md §6`). З плоского `current_amount` — у живий інструмент.
- **Файли:** `goal_contributions` (міграція), `/goals*` в `api.ts`, `src/pages/Goals.tsx`, `GoalCard`.
- **Кроки:** 1) історія внесків (`current_amount = SUM`); 2) авто-поповнення (round-up / % доходу
  / фікс); 3) типи (save-up/debt-payoff/sinking-fund) → математика ETA; 4) статус on-track/behind
  (звʼязати з `goal_risk` у `notify.ts`); 5) зняття/пауза.
- **Готово-коли:** графік прогресу з внесків; ETA/статус рахуються; авто-поповнення резервує
  суму у What-if і Пораднику.

**P2.3 хвости** (core зроблено): привʼязка події до цілі — залежить від P2.1; AI-close-out на
числах плану — однорядкове розширення `/events/:id/ai` (context += `planned_total`), теж після P2.1.

---

## 📌 Беклог (не в черзі — беремо, коли черга спорожніє)

### Тех-борг
- **Бейдж «компенсовано» у СПИСКУ транзакцій** (`TxItem`) — §COMPENSATION зараз видно лише в
  деталях операції. Треба довезти `reimbursed` у `/transactions` і намалювати чип.
- **`proactive.ts` рахує бюджети НЕ по канону** (`t.hold=0 AND t.is_transfer=0` замість
  `SPEND_WHERE`) → цифри тижневого TG-пушу розходяться зі стрічкою й Статистикою.
- **Тексти помилок віддають сиру причину** — свідоме рішення (§Обробка помилок). Переглянути,
  якщо колись зʼявляться юзери поза колом друзів: сховати `detail` за owner-прапорцем.

### Архітектурне / потребує рішення
- **«AI думає між сторінками»** — фонове продовження аналізу при навігації (DO / фонові задачі). L.
- **UI-редактор правил** `rules` — зараз лише через код/БД. Перетинається з «Правила-автотеги» (§E).
- **Lazy-enrich** (варіант B) — за узгодженням.
- **Мульти-валютні групи/подорожі** — лишився TG-бот (аналітика й групи вже зведені в ₴).
- **Prompt-кеш verify** на проді (`cache_read` у логах) — потребує деплою (A1).

### Мультипровайдерний AI (кол. L6)
Дати юзеру вибір Anthropic / OpenAI / Grok замість жорсткої привʼязки. **Не «на ізі»:**
розходяться tool-use (`runToolConversation` написаний під блоки Anthropic), prompt-кеш
(`cache_control ttl:1h` — на ньому тримається економіка корпусу §A5), серверний `web_search`
і таблиці цін `priceFor`. **Дешевий шлях:** адаптер лише для одноходових викликів
(enrich/OCR/parse/insight), агентний чат лишити на Anthropic; Grok і DeepSeek дають
OpenAI-сумісний ендпоінт, тож один адаптер відкриває кількох.
**Готово-коли:** ключ будь-якого підтримуваного провайдера дає робочу категоризацію, а UI
чесно каже, які фічі доступні для цього провайдера.
⚠️ Зачепить `demoClamp` (`ai.ts`) — він знає лише моделі Anthropic; чужий провайдер у демо
має бути або так само затиснутий, або заборонений.

### Хвости AI 4.0 (відкладено свідомо)
- Юзер-аплоад власних PDF/MD у корпус знань (R2 + D1). *(Той самий пункт, що «PDF у корпус» —
  дубль злито 2026-07-26.)*
- Нативний PDF `document`-блок (100 стор Haiku / 600 Sonnet) + `citations`.
- `output_config.format` (structured outputs) замість хака `repairTruncatedJson`.

---

## 🎨 Дизайн — майбутнє
> Як працювати — `DESIGN.md §10.0` (skills: `impeccable` основний, `review-animations` для руху).
> Кожну зміну фіксувати в «Журналі рішень» DESIGN.md.
- **Статистика** — редизайн вкладок **Огляд + Категорії/Тренди**. Потрібен live-скрін вкладок (§0-воркфлоу).
- **Сторінки ще не переглянуті вживу:** Порадник, Підписки, Бюджети, Рахунки, Категорії.
  Чек-лист: локальний хардкод-колір / тінь / `transition:all` / тіснота.
- **Розмітка для >1920px** — як стоять блоки на широких моніторах.
- **Скелетони завантаження** — примітив `Skeleton`/`SkeletonRows`/`StatsSkeleton` (`src/components/ui/Skeleton.tsx`)
  + CSS `.skeleton` (shimmer). **Застосовано на Статистиці** (головний loading + ліниві панелі дрилу).
  Лишилось адаптувати на інших сторінках (Порадник/Підписки/Бюджети/Рахунки) — там фетч по-карткова,
  тож підставляти `<Skeleton>` у кожен блок точково.

## 🔵 TG-бот — решта
Підключення для всіх юзерів — **картка D1 у Черзі** (не тут). Тут лишається лише те, що поверх:
швидкий запис витрати текстом/фото та запити балансу/статистики з чату. Каркас є.
⚠️ Хиба в `proactive.ts` (бюджети не по канону) — у §Беклог/Тех-борг, щоб не жила у двох місцях.

---

## 💡 Ідеї фіч — брейнсторм для тріажу
> Сирий список для пріоритезації. **[нове]** / **[лвл-ап]** / **[тех]**; розмір S/M/L — груба оцінка.
> Реалізоване звідси видаляється (не викреслюється).

### A. ФОП / податки (на паузі — рішення користувача)
- **[нове] Податковий конверт-автовідкладання (S/M).** З кожного доходу відкладати % (ЄП 5% + ЄСВ + ВЗ)
  у віртуальний конверт → видно, скільки з балансу насправді «не твоє».
- **[нове] Податковий календар ФОП (M).** Дедлайни ЄП/ЄСВ/ВЗ із сумами, відлік, нагадування; «сплачено» → списання з конверта.
- **[нове] Трекер річного ліміту доходу ФОП (S).** Прогрес-бар до ліміту групи + прогноз перевищення за темпом.
- **[нове] Розрахунок ЄП за квартал (S).** Дохід × ставка; валютний дохід — за курсом НБУ на дату надходження.
- **[нове] Валютний дохід ФОП — курс НБУ на дату (M).** Для USD/EUR-інвойсів фіксувати офіційний курс дня зарахування.
- **[лвл-ап] Позначка «робочі витрати» (S).** Тег бізнес/особисте → окремий підсумок (корисно для маржі).

### B. Аналітика та прогноз
_Порожньо — усе, що було, зроблено._

### C. Порадник / AI / автоматизація
- **[лвл-ап] Більше метрик усередині поради (S).** Норма заощаджень цього міс, топ-категорія-драйвер
  зростання, «на що пішла різниця vs минулий міс».
- **[нове] Щомісячний авто-огляд у стрічку (M).** AI генерує підсумок 1-го числа й кладе в стрічку/пуш.
- **[нове] Розумні цілі-виклики (S).** AI пропонує реалістичний виклик («−15% на доставку = +1200₴») і трекає.

### D. Бюджети / цілі / заощадження
_Порожньо — усе, що було, зроблено._

### E. Транзакції / введення / категоризація
- **[нове] Розстрочка як окрема сутність (M).** Хвіст §SPLIT: спліт зробили, розстрочку — ні.
- **[нове] Правила-автотеги в UI (M).** Мерчант містить X → категорія Y + тег Z, із превʼю впливу.

### F. Сповіщення / проактивність
- **[лвл-ап] Достроїти TG-бота (M).** Запис витрати текстом/фото, запит балансу.
  Пуш подій — зроблено; підключення для всіх юзерів — **D1 у Черзі**.
- **[нове] Web-Push (PWA) (M).** Свідомо відкладено: VAPID-ключі, перехід vite-plugin-pwa на
  `injectManifest` із власним SW (`push`/`notificationclick`), таблиця підписок, підпис JWT через
  Web Crypto у Worker. На iOS працює лише для PWA, доданої на «Додому». Зараз ту саму роль грає TG-пуш.

### G. Валюти / інвестиції / нетворт
- **[лвл-ап] Вплив курсу окремою цифрою (S).** Базу зроблено (`rate_history` 0024, нетворт рахує
  кожну точку курсом своєї дати). Лишилось показати «скільки заробило/коштувало коливання на
  USD-залишках» — розділити зміну нетворту на «рух грошей» і «рух курсу». Має сенс, коли
  історія накопичиться за кілька місяців.

### H. UX / продуктивність / пошук
- **[нове] PWA-шорткати й шейр-таргет (S).** «Додати витрату» з домашнього екрана; поділитись фото чека.
- **[нове] Онбординг-чеклист (S).** Прогрес налаштування (рахунки→вебхук→бекфіл→курси→профіль).
- **[лвл-ап] Плавність дашборду (S).** Дотягнути наявні анімації, прибрати ривки.

### I. Дані / надійність / приватність
- ~~Бекап/відновлення~~ → піднято в чергу як **C3** (не дублювати тут).
- **[нове] Пасткод/біометрія на вхід (S).** Локальний лок PWA поверх Google-сесії
  (парольного входу більше немає; `APP_PASSWORD` лишився тільки як legacy-ключ підпису).
- **[тех] Audit-лог AI-змін (S).** Коли AI змінив категорію/переказ — журнал + «відкотити».
