# ROADMAP — жива черга задач і фіч

> **Це постійний файл.** Тут — лише те, що ще треба зробити. Правила ведення — у `CLAUDE.md §Робочий процес`:
> нову задачу/ідею/баг → сюди; **доробив → видали пункт**; якщо результат важливий довгостроково → перенеси стислий підсумок у `CLAUDE.md §Зроблено`.
> Довідник/інваріанти/як працює зараз → `CLAUDE.md`. Дизайн → `DESIGN.md` (там повний «Журнал рішень» + §10–11).
>
> Закриті фази: Аналітика 2.0, Порадник, Бюджети, Головна, Репорти, Вагомість, **AI 3.0 (A–F)**,
> **Фідбек-раунд live-тесту (#1–#7)** — підсумки у `CLAUDE.md §Зроблено`.
> **Дизайн-рефактор + редизайн Головної/Транзакцій/Налаштувань/тултіпів** — зроблено, історія в `DESIGN.md` (§8 «Журнал рішень», §10–11).

## 🚦 ПЛАН ВИКОНАННЯ (старт для наступних сесій)

> **Як працювати з цим планом (читай першим):**
> 1. Бери **найвищу невиконану задачу** з «Черги» нижче (вони впорядковані за пріоритетом×простотою). Не перестрибуй без причини.
> 2. Кожна задача = картка: **Ціль · Файли · Кроки · Готово-коли**. Спочатку прочитай згадані файли, тоді роби.
> 3. **Обов'язково green-bar перед «готово»:** `npm run check` + `npm run build`. Гроші/статистика — лише через `worker/lib/stats.ts` (не дублюй SQL).
> 4. **UI/UX** — спершу `DESIGN.md`, зміну фіксуй у його «Журналі рішень». Хук impeccable ганяє детектор — знайдене виправляй, не глуши.
> 5. **Доробив:** признач `✅ DONE (дата)` перед карткою АБО перенеси стислий підсумок у `CLAUDE.md §Зроблено` і видали картку. Нове/баг → у «Беклог» чи «Ідеї фіч».
> 6. **AI-залежні задачі** не перевіриш без реального `ANTHROPIC_API_KEY` — познач `⏳ чекає live` і йди далі; live-звірка = рутина користувача.
> 7. Один PR/логічний блок = одна задача. Не роби кілька карток «в одному замісі», якщо не сказано.

### Черга (роби згори вниз)

> **✅ Закрито цю сесію (2026-07-14)** — підсумки в `CLAUDE.md §Зроблено`, дизайн-деталі в `DESIGN.md §Журнал`:
> чат-порадник lvl-up (єдиний контекст `collectFinanceSnapshot`, персона фінменеджера, таблиці/графіки, кілька розмов, вибір моделі чату) · **агентні tool-use** (`query_spend`/`find_transactions`/`list_categories`) · консистентність burn (`sumLevels`) · сторінка мерчанта (`/merchant/:name`) · What-if симулятор · ставка накопичення в Цілях · редизайн layout Порадника (2-кол дашборд) + фікси висоти/вводу чату. Усе ⏳ чекає live-звірки з реальним ключем/даними.

> **🧭 ФАЗА AI 4.0 — дизайн і обґрунтування в `CLAUDE.md §AI 4.0 (§A1–A5)`. ЧИТАЙ ЙОГО ПЕРШИМ.**
> Картки нижче — лише кроки виконання; «чому саме так» — там. Порядок карток = порядок робіт.

**P0 — §A2 Фікс цін AI-лічильника (S, без live-ключа). 🐞**
- **Ціль:** картка «💸 Витрати на AI» має казати правду. Зараз Sonnet завищено ~на третину, cache-write занижено.
- **Файли:** `worker/lib/ai.ts` (рядки ~78–93).
- **Кроки:** (1) Ціна Sonnet 5 date-aware: до **2026-08-31** вступна `{in: 2.0, out: 10.0}`, після — `{in: 3.0, out: 15.0}`. (2) Cache-write множник за TTL: `1.25×` для 5 хв, **`2×` для `ttl:"1h"`** — `buildSystemPrefix` пише саме годинний. Прокинути TTL у `recordUsage` (зараз хардкод 1.25).
- **Готово-коли:** `npm run check` + `npm run build` зелені; у коді нема хардкоду 1.25 для 1-год кешу; ціна перемикається за датою.

**P0 — §A4 Перевірка tool-use чату на live (⏳ чекає live, S).**
- **Ціль:** переконатися, що чат РЕАЛЬНО кличе `query_spend`/`find_transactions`, а не відповідає з фіксованого контексту.
- **Чому:** `thinkingOff()` (`ai.ts:41`) вимикає thinking для Sonnet 5, а за докою Anthropic Sonnet 5 без thinking **рідше тягнеться до інструментів** (`CLAUDE.md §A4`).
- **Файли:** `worker/lib/ai.ts` (`runToolConversation`, `thinkingOff`), `worker/lib/advisor.ts` (`chatAdvice`, `financeChatTools`).
- **Кроки:** (1) Спитати чат щось, що ВИМАГАЄ бази («скільки на таксі влітку торік») і глянути в лічильник/логи, чи був tool-хід. (2) Якщо ні — явний нюдж у промт («коли відповідь залежить від даних поза фіксованим контекстом — ОБОВ'ЯЗКОВО виклич інструмент»). (3) Якщо не помогло — adaptive-thinking саме для task `chat`.
- **Готово-коли:** питання про дані поза снапшотом стабільно дає tool-хід.

**P1 — §A3 web_search на Sonnet-задачах (M, ⏳ live).**
- **Ціль:** AI знає, що зараз коїться (курси, тарифи, рішення), не з тренувального зрізу.
- **Файли:** `worker/lib/ai.ts` (збірка `tools`), `worker/lib/advisor.ts` (`chatAdvice`/`buildAdvice`).
- **Кроки:** (1) Додати `{type:"web_search_20260209", name:"web_search", allowed_domains:[…], max_uses:N}` **лише для Sonnet-задач** — Haiku 4.5 підтримує тільки базовий `web_search_20250305` (`CLAUDE.md §A3`). (2) Обмежити домени офіційними. (3) Врахувати серверні блоки в `runToolConversation` (це НЕ client-side tool — результат приходить у тій самій відповіді). (4) Перевірити `stop_reason: "pause_turn"` — серверний цикл може паузитись.
- **Готово-коли:** питання «який курс/тариф зараз» дає відповідь із джерелом; лічильник рахує; Haiku-шляхи не зачеплені.

**P1 — §A1 Шар фактів ⭐ (L, головна задача фази). Міграція 0020.**
- **Ціль:** користувач пише «з 15.07 метро 30 ₴ замість 8» → AI пояснює І runway рахується з цим.
- **🔑 Прочитай `CLAUDE.md §A1` — там корінь:** факт мусить дійти до ДВОХ місць (контекст AI + число). Тільки контекст = косметика, бо `categoryMonthlyLevels` бере 6 ПОВНИХ місяців і застаріла цифра доживе до осені.
- **Файли:** `migrations/0020_facts.sql` (нова), `worker/lib/stats.ts` (`categoryMonthlyLevels` — override), `worker/lib/advisor.ts` (`collectFinanceSnapshot` + `financeChatTools`), `worker/routes/api.ts`, UI (Порадник/Налаштування).
- **Кроки:** (1) Міграція `facts` за схемою з §A1. (2) Наратив у `collectFinanceSnapshot` — Порадник і Чат підхоплять автоматом (ЄДИНЕ джерело, не додавати в чат окремо). (3) Override числа ВСЕРЕДИНІ `categoryMonthlyLevels` (не в ендпоінті!) — лише `confirmed_at IS NOT NULL`. (4) Tool `remember_fact`: AI рахує ефект детерміновано через `find_transactions` (поїздки × нова ціна), НЕ з голови. (5) **Гейт підтвердження в UI** — без нього не чіпати числа. (6) Ручна форма додавання факту.
- **Готово-коли:** факт про метро змінює `usual` Транспорту, burn і runway ОДНАКОВО в Пораднику/Патернах/чаті; непідтверджений факт числа НЕ рухає; `npm run check` + `build` зелені.
- **⚠️ Вирішити на старті:** `category_id` nullable — глобальні факти («я звільнився») лише наратив, без коригування числа?

**P2 — §A5 Корпус знань PDF/MD (M, коли з'явиться сценарій).**
- **Ціль:** згодувати моделі доменні знання/документи.
- **Кроки:** (1) **Спершу заміряти `count_tokens` мінімум кешу для Sonnet 5** — у доці його НЕМА, а нижче порога `cache_control` МОВЧКИ не працює (`CLAUDE.md §A5`). (2) MD-корпус у наявний `CACHE_GUIDE`. (3) PDF — нативний `document`-блок, без beta; ⚠️ 100 сторінок для Haiku 4.5 (200k), 600 для Sonnet 5. (4) Розглянути `output_config.format` замість хака `repairTruncatedJson`.
- **Готово-коли:** `usage.cache_read_input_tokens > 0` на повторних викликах (інакше кеш не працює).

**P1 — Детект «мертвих»/дорогих підписок + річна вартість (лвл-ап, S).**
- **Ціль:** показати сумарну річну вартість підписок, % від доходу, топ-найдорожчі; підказка «X не використовується N міс — скасувати?».
- **Файли:** `src/pages/Subscriptions.tsx`, `worker/lib/subscriptions.ts`, наявний детект подорожчання (`price_change_pct`).
- **Кроки:** (1) Агрегат річної вартості (нормалізувати періоди через `nextChargeUnix`/period). (2) Секція «скільки з'їдають підписки». (3) Евристика «мертвої» (давно без активності поряд) — обережно, лише підказка.
- **Готово-коли:** видно річну суму й топ; підказки не нав'язливі.

**P2+ — далі з «Ідеї фіч» за пріоритетом користувача.** Сильні кандидати: Cashflow-календар (M), Індекс фінздоров'я (M), Спарклайни в списках (S), Експорт CSV (S), Web-push/TG-пуш дедлайнів (M, TG вже налаштований). Перед стартом великих — уточнити в користувача.

### Правила-нагадування
- **Міграції:** будь-яка нова таблиця/колонка → новий файл `migrations/00XX` + рядок у `CLAUDE.md §Ops` (перелік remote-міграцій). Зараз остання — 0019.
- **AI-виклики:** JSON-надійність уже загартована (`parseJson`+repair, ескалація токенів) — нові структуровані виклики роби через `callHaikuJson`/`callHaikuMessagesJson`, не «сирим» fetch.
- **Числа:** новий «місячний рівень» категорії — ЛИШЕ `stats.categoryMonthlyLevels()`; прогноз — `stats.projectSpend()`. Не рахувати вручну.

---

## 🎨 Дизайн — майбутнє / колись
> Як працювати над дизайном — `DESIGN.md §10.0` (skills: `impeccable/scripts/context.mjs` + `reference/product.md`;
> impeccable — основний, `review-animations` — явно для руху). Кожну зміну фіксувати в «Журналі рішень» DESIGN.md.
- **Статистика** — редизайн вкладок **Огляд + Категорії/Тренди** (фокус користувача). Потрібен live-скрін вкладок
  (§0-воркфлоу, як для Транзакцій). Стилі табів/сегментів можна міняти; усе, що слоп/не-UX-friendly.
- **Сторінки ще не переглянуті вживу:** Порадник, Підписки, Бюджети, Рахунки, Категорії — чек-лист:
  локальний хардкод-колір / тінь / `transition:all` / тіснота (flat уже глобальний).
- **Хвіст де-емоджі:** `✨`-маркери ще лишились у TransferReviewModal / EventDetail / Plan / TxDetail → Icon `spark`.
- **Розмітка для великих екранів (>1920px)** — як стоять блоки на широких моніторах.
- **Хвіст анімацій:** hover-стани карток gate за `@media (hover:hover) and (pointer:fine)` (touch-stick); опц. count-up 1×/сесію.

## 🔵 TG-бот (не пріоритет)
- Доробити фази: пуш репортів/аномалій, швидкий запис витрати текстом/фото, запити балансу/статистики. Активація за TG-секретами (є каркас — див. `telegram-bot.md`).

## 📌 Хвости / беклог
- **AI-надійність (порадник/бюджети «кидає не валід») — полагоджено 2026-07-12, потребує live-перевірки з реальним ключем.** Корінь = обрив JSON по `max_tokens`. Зроблено: truncation-repair у `parseJson` (рятує обірваний масив proposals/suggestions), ескалація токенів на ретраї при `stop_reason=max_tokens`, підняті ліміти (advisor/budget-plan 2200, budget-chat 1400), inline-помилка в блоці «Поради на твоїх числах» (не лише зникомий toast). Деталі — `CLAUDE.md §Зроблено`.
- ~~**Порадник — розширити продуктово (+1 lvl)**~~ — зроблено 2026-07-12: тренд-графік runway в «Історії порад», дельти runway/burn у кожному знімку, смуга «зміни від минулого разу» над порадами, кнопка «Очистити» історію (DELETE `/advisor/history`, історія 12→24). Потребує live-перевірки. *Далі за бажанням:* більше метрик усередині поради (напр. норма заощаджень цього міс, топ-категорія-драйвер).
- ~~**Оренда 8к vs 12500 — КОРІНЬ + КОНСИСТЕНТНІСТЬ**~~ — виправлено 2026-07-12. Корінь: різні екрани рахували «місячну» суму по різних вікнах (Патерни 6-міс середнє / Порадник-Бюджети `spent_90d÷3` / факт — остання операція) → після стрибка ренти 8к→12500 розходились. **Рішення:** новий ЄДИНИЙ хелпер `stats.categoryMonthlyLevels()` — fixed-кости (рента/підписка: останні 2-3 міс стабільні, CV≤0.12) дають рівень = останні платежі (ловить стрибок), змінні категорії = середнє за вікно (не хапає випадковий пік). Застосовано в **Патернах** (`usual`), **Пораднику** (`avg_month_uah`), **Бюджетах** (план + чат). Покрито числовим тестом. **Лишилось (за бажанням):** дашборд-`/analytics/forecast` (агрегат, менш видно) — проєкція поточного місяця, свідомо на власному бленді. `monthly_burn` = сума рівнів — ✅ зроблено P1 2026-07-14. Потребує live-звірки.
- **Аудит розмітки кнопок (частково 2026-07-12):** оверсайз-кнопку в `.section-head` виправлено (`.btn.sm` + `.section-head > .btn` авто-право). Лишилось пройтись рештою: інші ad-hoc `style={{padding,fontSize}}` на кнопках → на `.btn.sm`/`.btn`; перевірити вирівнювання іконка+лейбл, висоту рядків, переповнення на вузьких екранах.
- **#4-хвіст: «AI думає між сторінками»** — фонове продовження аналізу при навігації (велика архітектурна задача: Durable Object / фонові задачі).
- **#5-хвіст: календар підписок** — причепурити візуал (каденцію вже виправлено канонічним `nextChargeUnix`).
- **UI-редактор правил** `rules` (зараз правки лише через код/БД).
- **Мульти-валютні групи/подорожі** + **CUR2 хвіст:** перевірити зведення в ₴ у TG-бот/підписки (аналітика вже зведена).
- **Prompt-кеш verify** на проді (`cache_read` у логах) — потребує деплою.
- **Lazy-enrich** (варіант B) — за узгодженням.

---

## 💡 Ідеї фіч — брейнсторм для тріажу (2026-07-12)
> Великий сирий список від Claude на прохання «придумай багато реально корисного». Користувач
> сам почистить/пріоритезує. Позначки: **[нове]** = нова фіча, **[лвл-ап]** = розширення наявного,
> **[тех]** = внутрішнє/надійність. «Розмір» — груба оцінка (S/M/L). Нічого з цього ще не робилось.

### A. ФОП / податки — найвищий потенціал для цільового юзера
- **[нове] Податковий конверт-автовідкладання (S/M).** З кожного доходу автоматично «відкладати» % (ЄП 5% + ЄСВ + ВЗ) у віртуальний конверт → завжди видно, скільки з балансу насправді «не твоє». Показувати «доступно після податків».
- **[нове] Податковий календар ФОП (M).** Дедлайни ЄП/ЄСВ/ВЗ (квартал/місяць/рік) з сумами до сплати, зворотний відлік, нагадування (TG/push). Позначка «сплачено» → списання з конверта.
- **[нове] Трекер річного ліміту доходу ФОП (S).** Прогрес-бар до ліміту групи (напр. 3-тя гр.), скільки лишилось, прогноз перевищення за поточним темпом доходу.
- **[нове] Розрахунок ЄП за квартал (S).** Сума доходу за квартал × ставка = до сплати; врахувати валютний дохід за курсом НБУ на дату надходження (експорт послуг).
- **[нове] Валютний дохід ФОП — курс НБУ на дату (M).** Для інвойсів у USD/EUR фіксувати офіційний курс НБУ дня зарахування (окремо від mono-курсу) — для коректної податкової бази й звіту.
- **[лвл-ап] Позначка «робочі витрати» (S).** Тег на транзакції «бізнес/особисте» → окремий підсумок робочих витрат (навіть якщо ФОП на ЄП їх не віднімає — корисно для розуміння маржі).

### B. Аналітика та прогноз — лвл-ап наявного ядра
- ~~**[тех/лвл-ап] Спільний `categoryMonthlyLevel()`**~~ — ✅ зроблено 2026-07-12 (`stats.categoryMonthlyLevels`, застосовано в Патернах/Пораднику/Бюджетах). Burn=сума рівнів — ✅ P1 2026-07-14. Лишилось: forecast-агрегат (окрема семантика — проєкція місяця).
- **[лвл-ап] Прогноз із діапазоном довіри (S).** Замість однієї цифри — «12–15к» (низ/верх) на основі розкиду історії; чесніше за точкову оцінку.
- **[нове] Cashflow-календар (M).** Місячна сітка: очікувані надходження й списання по днях (підписки, планові, регулярна зарплата) → видно «провали ліквідності» наперед.
- ~~**[нове] What-if симулятор (M).**~~ — ✅ зроблено 2026-07-14 (`WhatIf.tsx` на Пораднику).
- ~~**[нове] Сторінка мерчанта (S/M).**~~ — ✅ зроблено 2026-07-14 (`/merchant/:name`).
- **[лвл-ап] Спарклайни в рядках категорій/мерчантів (S).** Міні-тренд 6 міс прямо в списку — швидкий контекст без переходу.
- **[нове] Порівняння місяців пліч-о-пліч (S).** Обрати 2 місяці → діфф по категоріях (що зросло/впало й на скільки).
- **[нове] Композитний «Індекс фінздоровʼя» (M).** Один бал 0–100 із runway + норма заощаджень + борг/дохід + стабільність доходу; gauge на Головній, тренд у часі.

### C. Порадник / AI / автоматизація
- **[лвл-ап] Більше метрик усередині поради (S).** Норма заощаджень цього міс, топ-категорія-драйвер зростання, «на що пішла різниця vs минулий міс».
- **[нове] Щомісячний авто-огляд у стрічку (M).** AI сам генерує короткий місячний підсумок 1-го числа й кладе в стрічку/пуш (не треба тиснути «Отримати»).
- ~~**[нове] «Спитай про будь-що» над усіма даними (M).**~~ — ✅ зроблено 2026-07-14 (агентні tool-use в чаті: `query_spend`/`find_transactions`/`list_categories`).
- **[нове] Розумні цілі-виклики (S).** AI пропонує реалістичний виклик («−15% на доставку цього міс = +1200₴») і трекає його виконання.
- **[тех] Детермінований fallback для поради (S).** Якщо AI недоступний — показати детерміновану «міні-пораду» з чисел (найбільша категорія, аномалії, runway), а не порожньо.

### D. Бюджети / цілі / заощадження
- ~~**[нове] Цілі-заощадження з датою (M).**~~ — ✅ фіча вже була (0007), 2026-07-14 додано «потрібно відкладати X/міс до дедлайну» в `GoalCard`.
- **[лвл-ап] Rollover-бюджети — візуалізація перенесення (S).** Уже є `rollover` у БД (0017) — показати «+800₴ з минулого міс» у картці конверта.
- **[нове] Автобюджет із історії одним тапом (S).** «Постав ліміти = мій середній −10%» без чату (детерміновано; AI-план лишається як опція).
- **[нове] Бюджет на подію/подорож (S).** Ліміт на `event_group` + прогрес (перевикористати конверти на групах).

### E. Транзакції / введення / категоризація
- **[нове] Спліт транзакції (M).** Одна покупка → кілька категорій із сумами (напр. в супермаркеті продукти+побутхімія+алкоголь). Основа — receipt_items уже є. Також про розстрочку треба подумати
- **[нове] Bulk-дії у списку (S).** Виділити кілька → змінити категорію/тег/вагомість гуртом (bulkbar частково є).
- **[нове] Детект дублів / подвійних списань (S).** Та сама сума+мерчант у короткому вікні → бейдж «можливий дубль».
- **[лвл-ап] Звʼязка повернення↔покупка (S).** Рефанд матчиться до оригінальної покупки → «чисті» витрати категорії.
- **[нове] Правила-автотеги в UI (M).** = хвіст «UI-редактор правил»: якщо мерчант містить X → категорія Y + тег Z; прев'ю впливу.


### F. Підписки / регулярні
- **[нове] Детект «мертвих» підписок (S).** Підписка платиться, але поряд нема активності/користі за N міс → підказка «скасувати? −X₴/рік».
- **[лвл-ап] Річна вартість і «скільки з’їдають підписки» (S).** Сумарно/рік, % від доходу, топ-найдорожчі.
- **[лвл-ап] Алерт подорожчання (S).** Уже детектимо `price_change_pct` — додати явний пуш/бейдж «Netflix +50₴».
- **[лвл-ап] Причепурити календар підписок (#5, S).** Візуальний рефайн SubsCalendar.

### G. Сповіщення / проактивність (TG + PWA push)
- **[нове] Web-Push (PWA) (M).** Не лише TG: пуш прямо в застосунок — аномалія, великий платіж, кінець бюджету, дедлайн податку.
- **[лвл-ап] Достроїти TG-бота (M).** Пуш репортів/аномалій, швидкий запис витрати текстом/фото, запит балансу (каркас є).
- **[нове] Алерти-правила (S).** «Сповісти, якщо в категорії X перевитрата» / «якщо баланс < N» / «списання > N₴».
- **[нове] Тижневий дайджест (S).** Короткий підсумок тижня в TG/push щонеділі.

### H. Багатовалютність / інвестиції / нетворт
- **[нове] Нетворт у часі (M).** Активи (рахунки + крипта + інвест) − борги, графік по місяцях; уже є `capital-trend` для власних коштів — розширити на все.
- **[лвл-ап] Мультивалютні групи/подорожі (M).** = CUR2-хвіст: зведення в ₴ у групах/підписках/TG.
- **[нове] Історія курсів і вплив (S).** Скільки «коштувало/заробило» коливання курсу на USD-залишках.

### I. UX / продуктивність / пошук
- **[нове] Збережені фільтри / смарт-списки (S).** «Робочі витрати», «Готівка», «Цього тижня» — швидкий доступ (фільтри в URL уже є).
- **[нове] Глобальний пошук/командна панель (M).** Ctrl-K: мерчант, категорія, сторінка, дія.
- **[лвл-ап] Скелетони й осмислені empty-states (S).** Замість порожнечі під час завантаження/без даних.
- **[нове] PWA-шорткати й шейр-таргет (S).** Швидка дія «Додати витрату» з домашнього екрана; поділитись фото чека в застосунок.
- **[нове] Онбординг-чеклист (S).** Прогрес налаштування (рахунки→вебхук→бекфіл→курси→профіль) з підказками.
- **[нове] Додати анімації, покращити наяві. Щоб весь дашборд працював плавно

### J. Дані / надійність / приватність
- **[нове] Експорт CSV/Excel (S).** Вивантаження транзакцій/звітів (для бухгалтера/податкової).
- **[нове] Бекап/відновлення (M).** Експорт-імпорт усього стану (D1) у файл; ручний бекап у R2.
- **[нове] Пасткод/біометрія на вхід (S).** Локальний лок PWA (додатково до APP_PASSWORD).
- **[тех] Audit-лог AI-змін (S).** Коли AI змінив категорію/переказ — журнал + «відкотити».

### K. Дизайн / консистентність (тех-борг)
- **[тех] Уніфікувати розміри кнопок (S).** Пройтись ad-hoc `style padding/fontSize` → `.btn`/`.btn.sm` (почато 2026-07-12).
- **[тех] Прибрати layout-transition warnings (S).** `transition` на width/height/padding (impeccable-хук L482/1550/1798) → transform/grid-rows.
- **[тех] Аудит section-head/спейсингу по всіх сторінках (S).** За скрінами — ритм 4pt, вирівнювання, переповнення.
- **[тех] Live-звірка після фіксів прогнозу/AI (S).** Прогноз, порадник, бюджети — перевірити на реальних даних + ключі.
