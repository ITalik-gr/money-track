# ROADMAP — жива черга задач і фіч

> **Це постійний файл. Тут — ЛИШЕ те, що ще треба зробити.** Зроблене звідси **видаляється**,
> а стислий підсумок переїжджає в `CLAUDE.md §Зроблено`. Правила — `CLAUDE.md §Робочий процес`.
> Довідник/інваріанти → `CLAUDE.md`. Дизайн → `DESIGN.md` (§8 «Журнал рішень» + §10–11).
>
> Останнє прибирання: **2026-07-20** (винесено ~10 ✅DONE-карток, які тут уже не мали лежати).

## 🚦 Як працювати з цим файлом

1. Бери **найвищу невиконану задачу** з «Черги». Не перестрибуй без причини.
2. Задача = картка: **Ціль · Файли · Кроки · Готово-коли**. Спочатку прочитай згадані файли.
3. **Green-бар перед «готово»:** `npm run check` (tsc + SQL-лінт) + `npm run build`.
   Гроші/статистика — лише через `worker/lib/stats.ts`, не дублюй SQL.
4. **UI/UX** — спершу `DESIGN.md`, зміну фіксуй у його «Журналі рішень». Хук impeccable ганяє
   детектор — знайдене виправляй, не глуши.
5. **Доробив → ВИДАЛИ картку звідси**, підсумок у `CLAUDE.md §Зроблено`. Нове/баг → у «Беклог» чи «Ідеї фіч».
6. **Live-звірку в чергу НЕ писати.** Користувач ганяє її сам і повідомляє, якщо щось не так.
   AI-залежну задачу просто здавай зеленою.
7. Один PR / логічний блок = одна задача.

---

## 🔥 Черга (роби згори вниз)

### 1. Регулярний дохід у Cashflow-календарі (M)
**Ціль:** зняти обмеження «аутфлоу-only» — календар показує лише списання, тож проєкція подушки
песимістична (зарплата не врахована). **Файли:** `worker/routes/api.ts` (`/analytics/cashflow-calendar`),
`worker/lib/stats.ts`, `src/components/CashflowCalendar.tsx`.
**Кроки:** детект регулярного доходу (мерчант/джерело з надходженням у ≥3 різних місяцях, стабільна сума)
→ канонічний хелпер у `stats.ts` (НЕ рахувати в ендпоінті) → додати інфлоу-дні в сітку й у проєкцію.
**Готово-коли:** проєкція подушки враховує зарплату; попередження про мінус не спрацьовує хибно.

### 2. 🔔 Центр сповіщень — окрема сторінка (L, СПЕКА, ще не роблено)
**Ціль:** стрічка того, що система «хоче сказати» — репорти, факти про мої гроші, аномалії,
дедлайни. Зараз усе це або губиться (репорт треба піти й відкрити), або не існує.
**Точка входу вже є:** кнопка-дзвіночок біля інпута «Пошук операцій» — веде на нову сторінку.

**Що показувати (типи подій):**
- **Репорти** — «готовий тижневий репорт» з коротким витягом (не самим репортом), лінк на `/reports/:id`.
- **Факти й спостереження** — «на доставку цього місяця вже 2 400 ₴, це +60% до звичного»,
  «Netflix подорожчав на 50 ₴», «7 днів без витрат у категорії Кафе».
- **Дедлайни** — списання підписки/розстрочки за N днів, провал ліквідності з Cashflow-календаря.
- **Аномалії** — з наявного «Радара аномалій» (`/analytics/patterns`).
- **AI-ініціатива** — модель сама формулює спостереження за знімком `collectFinanceSnapshot`.
  Це головна відмінність від тупих алертів: не «поріг перевищено», а «ось що я помітив і що з цим робити».

**Модель даних (нова міграція):** `notifications` — `id, kind, title, body, severity(info|warn|urgent),
entity_type/entity_id` (лінк на репорт/tx/підписку), `created_at, read_at, pushed_tg_at, dedup_key`.
`dedup_key` обовʼязковий — крон не має плодити ту саму подію щодня.

**Генерація:** детерміновані події — з наявних ендпоінтів (підписки/календар/аномалії/патерни) у крон;
AI-події — окремим проходом, дешева модель, СТРОГО з канону (`stats.ts`), не «на око».
Ліміт на добу, щоб не перетворилось на спам.

**Telegram:** туди йде **лише важливе** — `severity >= warn` (провал ліквідності, різке подорожчання,
перевитрата бюджету, дедлайн ≤2 дн). Решта живе тільки в застосунку. Прапорець `pushed_tg_at`
захищає від повторів. Каркас бота вже є.

**UI:** сторінка `/notifications` — стрічка з групуванням по днях, фільтр за типом, «прочитано»,
бейдж-лічильник непрочитаних на дзвіночку. Клік по події → відповідна сторінка.

**Відкриті питання (вирішити перед кодом):** чи може користувач вимикати окремі типи;
чи потрібен Web-Push (PWA) одразу, чи спершу лише TG + in-app; як часто крон.

### 3. Порівняння місяців пліч-о-пліч (S)
Обрати 2 місяці → діфф по категоріях (що зросло/впало й на скільки). Канон — `stats.ts`.

### 4. Детермінований fallback для поради (S)
Якщо AI недоступний — показати міні-пораду з чисел (найбільша категорія, аномалії, runway),
а не порожньо. Перегукується з §Обробка помилок: краще деградувати, ніж мовчати.

**Далі — кандидати з «Ідей фіч»:** нетворт у часі (M), детект дублів списань (S),
автобюджет із історії (S). Перед стартом великих — уточнити з користувачем.

---

## 📌 Хвости / беклог

### Тех-борг (дрібне, з відомим корінням)
- **`COUNT(*)` у спліт-запитах.** Полагоджено там, де торкались 2026-07-20 (`COUNT(DISTINCT t.id)`),
  але решта STATS_JOINS-запитів досі рахує частини спліту як окремі операції → середній чек
  трохи занижений на спліт-tx. Пройтись рештою.
- **Аудит розмірів кнопок.** `.section-head` оверсайз виправлено (2026-07-12). Лишилось: ad-hoc
  `style={{padding,fontSize}}` на кнопках → `.btn.sm`/`.btn`; вирівнювання іконка+лейбл; вузькі екрани.
- **Дубль вокабуляру карток:** `KIND_LABEL` в `Accounts.tsx` дублює `cardKindLabel` з `lib/merchant.ts` —
  звести в одне джерело (`DESIGN.md §Журнал 2026-07-15`).
- **`AGENTS.md`** дублює caveman-правила, які й так інжектить хук — вирішити, лишати чи прибрати.

### Архітектурне / потребує рішення
- **«AI думає між сторінками»** — фонове продовження аналізу при навігації (Durable Object / фонові задачі). L.
- **UI-редактор правил** `rules` — зараз правки лише через код/БД. Перетинається з «Правила-автотеги».
- **Lazy-enrich** (варіант B) — за узгодженням.
- **Мульти-валютні групи/подорожі** (CUR2-хвіст): звести в ₴ у TG-боті й підписках (аналітика вже зведена).
- **Prompt-кеш verify** на проді (`cache_read` у логах) — потребує деплою. Перетин із live-звіркою §A5.

### Хвости AI 4.0 (відкладено свідомо)
- Юзер-аплоад власних PDF/MD у корпус знань (R2 + D1).
- Нативний PDF `document`-блок (100 стор Haiku / 600 Sonnet) + `citations`.
- `output_config.format` (structured outputs) замість хака `repairTruncatedJson`.

---

## 🎨 Дизайн — майбутнє
> Як працювати — `DESIGN.md §10.0` (skills: `impeccable` основний, `review-animations` для руху).
> Кожну зміну фіксувати в «Журналі рішень» DESIGN.md.
- **Статистика** — редизайн вкладок **Огляд + Категорії/Тренди**. Потрібен live-скрін вкладок (§0-воркфлоу).
- **Сторінки ще не переглянуті вживу:** Порадник, Підписки, Бюджети, Рахунки, Категорії.
  Чек-лист: локальний хардкод-колір / тінь / `transition:all` / тіснота.
- **Хвіст де-емоджі:** `✨` ще лишились у TransferReviewModal / EventDetail / Plan / TxDetail → Icon `spark`.
- **Розмітка для >1920px** — як стоять блоки на широких моніторах.
- **Хвіст анімацій:** hover-стани карток gate за `@media (hover:hover) and (pointer:fine)`.
- **Скелетони й осмислені empty-states** — доповнює `ErrorNote` (помилка вже покрита, порожнеча ще ні).

## 🔵 TG-бот (не пріоритет)
Швидкий запис витрати текстом/фото, запити балансу/статистики. Каркас є, активація за TG-секретами.
Пуш подій у TG — не тут, а в спеці «Центр сповіщень» (Черга №2): туди йде лише severity >= warn.

---

## 💡 Ідеї фіч — брейнсторм для тріажу
> Сирий список для пріоритезації. **[нове]** / **[лвл-ап]** / **[тех]**; розмір S/M/L — груба оцінка.
> Реалізоване звідси видаляється (не викреслюється).

### A. ФОП / податки (на паузі — рішення користувача)
- **[нове] Податковий конверт-автовідкладання (S/M).** З кожного доходу відкладати % (ЄП 5% + ЄСВ + ВЗ)
  у віртуальний конверт → видно, скільки з балансу насправді «не твоє».
- **[нове] Податковий календар ФОП (M).** Дедлайни ЄП/ЄСВ/ВЗ із сумами, відлік, нагадування; «сплачено» → списання з конверта.
- **[нове] Трекер річного ліміту доходу ФОП (S).** Прогрес-бар до ліміту групи + прогноз перевищення за темпом.
- **[нове] Розрахунок ЄП за квартал (S).** Дохід × ставка; валютний дохід — за курсом НБУ на дату надходження.
- **[нове] Валютний дохід ФОП — курс НБУ на дату (M).** Для USD/EUR-інвойсів фіксувати офіційний курс дня зарахування.
- **[лвл-ап] Позначка «робочі витрати» (S).** Тег бізнес/особисте → окремий підсумок (корисно для маржі).

### B. Аналітика та прогноз
- **[лвл-ап] Тренд індексу здоровʼя на Головній (S).** Бекенд+картка вже є — лишився gauge на дашборді.

### C. Порадник / AI / автоматизація
- **[лвл-ап] Більше метрик усередині поради (S).** Норма заощаджень цього міс, топ-категорія-драйвер
  зростання, «на що пішла різниця vs минулий міс».
- **[нове] Щомісячний авто-огляд у стрічку (M).** AI генерує підсумок 1-го числа й кладе в стрічку/пуш.
- **[нове] Розумні цілі-виклики (S).** AI пропонує реалістичний виклик («−15% на доставку = +1200₴») і трекає.

### D. Бюджети / цілі / заощадження
- **[нове] Автобюджет із історії одним тапом (S).** «Ліміти = мій середній −10%» детерміновано, без чату.
- **[нове] Бюджет на подію/подорож (S).** Ліміт на `event_group` + прогрес (перевикористати конверти).

### E. Транзакції / введення / категоризація
- **[нове] Розстрочка як окрема сутність (M).** Хвіст §SPLIT: спліт зробили, розстрочку — ні.
- **[нове] Bulk-дії у списку (S).** Виділити кілька → категорія/тег/вагомість гуртом (bulkbar частково є).
- **[нове] Детект дублів / подвійних списань (S).** Та сама сума+мерчант у короткому вікні → бейдж.
- **[лвл-ап] Звʼязка повернення↔покупка (S).** Рефанд матчиться до покупки → «чисті» витрати категорії.
- **[нове] Правила-автотеги в UI (M).** Мерчант містить X → категорія Y + тег Z, із превʼю впливу.

### F. Сповіщення / проактивність
- **[лвл-ап] Достроїти TG-бота (M).** Запис витрати текстом/фото, запит балансу. Пуш подій — уже покрито спекою «Центр сповіщень» (Черга №2).
- **Поглинуто спекою №2:** Web-Push (PWA), алерти-правила, тижневий дайджест.

### G. Валюти / інвестиції / нетворт
- **[нове] Нетворт у часі (M).** Активи (рахунки + крипта + інвест) − борги, графік по місяцях;
  `capital-trend` уже є для власних коштів — розширити на все.
- **[нове] Історія курсів і вплив (S).** Скільки «коштувало/заробило» коливання курсу на USD-залишках.

### H. UX / продуктивність / пошук
- **[нове] Збережені фільтри / смарт-списки (S).** «Робочі витрати», «Готівка», «Цього тижня» (фільтри в URL є).
- **[нове] Глобальний пошук / командна панель (M).** Ctrl-K: мерчант, категорія, сторінка, дія.
- **[нове] PWA-шорткати й шейр-таргет (S).** «Додати витрату» з домашнього екрана; поділитись фото чека.
- **[нове] Онбординг-чеклист (S).** Прогрес налаштування (рахунки→вебхук→бекфіл→курси→профіль).
- **[лвл-ап] Плавність дашборду (S).** Дотягнути наявні анімації, прибрати ривки.

### I. Дані / надійність / приватність
- **[нове] Бекап/відновлення (M).** Експорт-імпорт усього стану D1 у файл; ручний бекап у R2.
- **[нове] Пасткод/біометрія на вхід (S).** Локальний лок PWA (додатково до `APP_PASSWORD`).
- **[тех] Audit-лог AI-змін (S).** Коли AI змінив категорію/переказ — журнал + «відкотити».
