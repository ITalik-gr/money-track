-- §Хвіст: зворотний зв'язок від правок. Розрізняємо alias, навчений КОРИСТУВАЧЕМ вручну
-- (PATCH learn) від того, що записав AI під час enrich. Мета:
--   (а) ре-світ/enrich НІКОЛИ не перетирає ручну правку (guard у enrich.ts);
--   (б) консенсус мерчанта важить ручні голоси вище (weight×3 в enrich.ts).
-- Дефолт 'ai' для наявних рядків — поведінка не змінюється, поки користувач не правив.
ALTER TABLE merchant_aliases ADD COLUMN source TEXT NOT NULL DEFAULT 'ai';
