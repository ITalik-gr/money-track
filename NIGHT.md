# NIGHT.md — овернайт-черга та журнал

> Два адресати в одному файлі. **§0 — власнику** (живі дії, яких код зробити не може).
> **§1–§7 — моделі** в автономному прогоні: протокол, черга, і журнал, який вона веде ТУТ.
>
> Правило файлу: **прогрес пишеться сюди одразу після кожної картки**, не наприкінці сесії.
> Прогон, що впаде або впреться в ліміт, має лишити по собі читабельний слід.
>
> Створено 2026-09-18. Черга — `ROADMAP.md`; цей файл її не замінює, а веде НІЧНУ роботу.

---

## §0 — ЩО МАЄ ЗРОБИТИ ВЛАСНИК

### 0.1 Деплой (без цього ФОП і пошук не працюють у проді)

- [ ] **Створити індекс Vectorize** — без нього семантичний пошук падає на старті:
      `wrangler vectorize create money-track-tx --dimensions=1024 --metric=cosine`
- [ ] **Перевірити, що Workers AI увімкнено** на акаунті (біндинг `AI` у `wrangler.jsonc`).
- [x] **Міграції на remote** — 0050–0052 поїхали 2026-09-20.
- [ ] **Ще одна міграція з цього раунду:** 0053 (`progress_done/total` для масових прогонів,
      §A6-BATCH). `npm run db:migrate:remote`, бекап перед цим (`docs/OPS.md`).
- [ ] `npm run deploy`.
- [ ] Після деплою перевірити, що **`/business`** відкривається (`/fop` тепер редірект), а
      `/api/tax/status` віддає `enabled:false`, а не 500.

### 0.2 Налаштувати свій ФОП (одноразово)

> ⚠️ §FOP-GATE (2026-09-20): `/fop` тепер видно ЛИШЕ твоєму акаунту — ні іншим юзерам, ні демо.
> Гейт на `env.IS_OWNER`, знімається одним рядком, коли модуль стане тим, що ти планував
> (`docs/TAX.md §0.15`).

- [ ] На `/business` → таб «Налаштування» увімкнути **Бізнес-облік**, далі **ФОП і податки**,
      і ввести **групу**, ставку (3% з ПДВ чи 5%), прапорець звільнення від ЄСВ.
- [ ] Позначити **ФОП-рахунок** як робочий: `/accounts` → іконка редагування на картці рахунку →
      перемикач «ФОП-рахунок» (зроблено 2026-09-18; на картці зʼявиться бейдж «ФОП»). Перемикач
      видно лише коли модуль увімкнено на `/fop`.
- [ ] Перевірити кілька операцій: чи правильно успадкувався «діловий» статус.
- [ ] Натиснути **«Підтягнути курси»**, якщо є валютні надходження.
- [ ] Звірити суму конверта з тим, що ти реально маєш сплатити цього кварталу. **Це головна
      жива перевірка всього модуля.** Якщо число не сходиться — записати сюди, в §7, ЧИМ саме.

### 0.3 Регуляторний нагляд

- [ ] Додати сторінку **своєї** податкової/громади (сідові джерела — загальнодержавні).
- [ ] Ввести свої реквізити ЄП / ВЗ / ЄСВ і натиснути «Зберегти й позначити перевіреним».
- [ ] Через кілька днів глянути, чи не помітились джерела шумними (якщо так — це сигнал, що
      нормалізація сторінки замало агресивна; записати в §7).

### 0.4 Семантичний пошук

- [ ] Увімкнути в Налаштуваннях, дочекатись бекфілу.
- [ ] **Живий тест якості:** знайти 5 операцій, назв яких не памʼятаєш. Записати сюди, що
      знайшлось і що ні — це єдине джерело правди про поріг `minScore = 0.45`.

### 0.5 Живі перевірки, що лишились із попередніх раундів

- [ ] MCP-конектор ChatGPT (`ROADMAP.md`).
- [ ] 13 класів-модифікаторів, що нічого не роблять.
- [ ] STYLES фази 0.5 + 4 (8 конфліктів селекторів).
- [ ] Приват на живому API.
- [ ] `npm run eval` після будь-якої зміни промту — і записати рух точності.

---

## §1 — Протокол нічної сесії (для моделі)

1. **Читати перед роботою:** `CLAUDE.md` завжди; `docs/*.md` того домену, якого торкаєшся.
2. **Одна картка за раз.** Дописати рядок у §7 ДО переходу до наступної.
3. **Зелений бар обовʼязковий:** `npm run check` + `npm run build` перед «готово».
4. **Не чіпати нічого з §0** — це блоковано на власника. Якщо картка впирається в живий екран,
   живий ключ або живий конектор — записати це в §7 і взяти наступну.
5. **Не перезаписувати golden/baseline, щоб позеленіти.** Діф означає зміну поведінки; або це
   регресія (відкотити), або навмисна зміна (сказати, чому, в §7).
6. **`npm run eval` платний і недетермінований** — запускати лише коли змінювався промт, гейт або
   модель, і записувати рух чисел.
7. **Знахідка, яку не можна полагодити наосліп, — це запис, а не фікс.** У §7 і в `ROADMAP.md`.
8. **Мова:** код і `.md` — англійською; відповіді власнику — українською.

---

## §2 — Аудити (беремо першими)

### A1. Аудит ФОП-модуля наскрізь
Мета: довести, що числа збігаються самі з собою й з каноном.
- Звірити `taxStatus` ↔ `businessOverview` ↔ `/tax/ledger.csv` на одній фікстурі: сума кварталу
  має бути ОДНА в трьох місцях.
- Перевірити межі кварталу/року в Києві проти UTC (31 грудня 22:30, 1 січня 00:30).
- Перевірити, що `refreshObligations` ідемпотентний і не переписує сплачене.
- Валютні кейси: надходження у вихідний, свято, 1 січня; від'ємний «дохід» (повернення клієнту).
- Групи 1/2: квартал без доходу, зміна групи посеред року, `single_override` = 0.
- Перевірити §TAX-UAH: чи ніде на `/fop` не просочився `baseSign()`.
- **Готово-коли:** кожне розходження або виправлене, або записане в §7 із причиною.

### A2. Аудит усього застосунку — консистентність чисел
- Пройтись §-правилами `docs/CANON.md` і знайти місця, де те саме число рахується двічі.
- Перевірити, що жоден новий SQL не обійшов `repo/` (лінт C1 ловить лише `routes/`).
- `bad-input.test.ts` — чи всі нові ендпоінти (`/tax/*`, `/settings/search/*`) під `numParam`.
- Перевірити гілки помилок на нових екранах: `/fop` без даних, без мережі, з 500.

### A3. Аудит периметра
- Перечитати `docs/PERIMETER.md` — **кожен виняток цитує ФАКТ, а факт може протухнути.**
  Перевірити дати й підстави, особливо `HRYVNIA_OK` і `RAW_RATES_OK` у `check-currency.mjs`.
- Новий вихідний трафік: `bank.gov.ua` (§TAX-FX), `tax.gov.ua` (§TAX-WATCH), Workers AI
  (§SEARCH-VEC). Перевірити, що жоден не несе даних користувача в URL чи заголовках.
- Перевірити, що `reg_sources.url` не може стати SSRF-вектором (зараз лише `https://` — чи
  достатньо? локальні адреси, редіректи на 127.0.0.1).

### A4. Розширити eval-харнес
- Довести датасет до 150–200 кейсів (зараз 74).
- Додати групи: чеки з OCR, CSV-імпорт без MCC, операції з нотаткою.
- Додати окремий зріз «ФОП»: чи модель правильно розпізнає ділові надходження.

### A5. Аудит продуктивності
- Знайти запити, що сканують усю таблицю на кожне відкриття сторінки.
- `pendingCount` у `search-vec.ts` — перевірити на 10k операцій.
- `businessOverview` робить 6×3 запити послідовно — виміряти, чи варто батчити.

---

## §3 — Як ще прокачати ФОП

- ⛔ **Квартальна декларація-чернетка** — потребує знання САМОЇ ФОРМИ (номери рядків, і чи ЄСВ
  взагалі декларується в тій самій декларації), а помилка тут коштує штрафу. Числа застосунок уже
  має; чого немає — доведеної розкладки форми. Або власник дає скрін/посилання на чинну форму, або
  картка робиться без номерів рядків: «дохід наростаючим за І кв / півріччя / три квартали / рік»
  плюс ЄП і ВЗ окремо, словами, з прямим «це не декларація, а числа для неї» (2026-09-18).
- **Сезонність бізнесу** — який квартал стабільно слабший. ⏳ Потребує 3 років історії: щоб
  сказати «Q1 стабільно слабший», треба ≥2 спостереження КОЖНОГО квартала, а чесний варіант на
  молодих даних уже є — `year_ago` (той самий квартал рік тому). Робити зараз означало б картку,
  яка в 99% випадків показує «даних замало» (2026-09-18).
- ⛔ **Мультивалютні інвойси** (§FX-COST для бізнесу) — заблоковано ДАНИМИ, не кодом: різниця
  «курс на дату інвойса vs курс на дату зарахування» потребує ДАТИ ІНВОЙСА, а застосунок знає лише
  дату надходження (§TAX-FX замерзає базу саме на ній). Або поле «дата інвойса» на операції (ручне),
  або картка неможлива (2026-09-18).
- **Історія ставок** — як росли ЄСВ і ліміт по роках. ⏳ У `BASIS` поки ДВА рядки (2025, 2026),
  тож графік — це дві точки. Картка стане змістовною з третім роком; сама таблиця вже правильна
  (§TAX-RATES: рядок додається, не редагується) (2026-09-18).

---

## §4 — Продуктові картки з `ROADMAP.md`

- **Річний звіт «Wrapped»** — слайди зроблено 2026-09-18; лишився крок «поділитись картинкою», і він упирається в рішення про залежність (`ROADMAP.md`).
- ⛔ **Хвости AI 4.0** (structured outputs замість `repairTruncatedJson`, нативний PDF-блок) —
  обидва міняють КОНТРАКТ із провайдером, і перевірити їх можна лише живим ключем: якщо
  structured outputs поводяться інакше, ламається кожна AI-фіча одночасно, а `npm run check` цього
  не побачить (там немає мережі). Це прохід на живому власнику, не на нічному (2026-09-18).

---

## §5 — Технічний борг

- `src/pages/Stats.tsx` — 418 → 359 рядків (винесено `StatsPeriodBar`). Далі різати нема по чому: лишились СТАН і один спільний запит, які оболонка й мусить тримати.
- ~24 інлайн-`style={{ padding }}` на `.card` (`DESIGN.md §2`).
- `STYLELESS_OK` має скорочуватись, а не рости.

---

## §6 — ЯКЩО ВСЕ ЗРОБЛЕНО, А ТОКЕНИ ЩЕ Є

Придумуй собі задачі сама — і **одразу записуй їх сюди, у §6**, перш ніж робити. Багато, не одну.

**Придумано й зроблено в ніч 2026-09-18:**
- ✅ **C10 розширено на §TAX-UAH** — кожен `<Money>` під `components/fop/` мусить назвати валюту.
- ✅ **C12 (новий лінт) — §APP_TZ у воркері**: жодного `new Date(x).getMonth()` тощо.
- ✅ **Мертвий код**: `limitHeadroom`, `monthLabel` — експорти без імпортерів.
- ✅ **Число у двох домах**: пʼята копія норми заощаджень у `Stats.tsx`.

**Придумано, ЩЕ НЕ зроблено (черга для наступного прогону):**
- ⬜ **Golden-сценарії для решти write-ендпоінтів, що змінюють стан для ІНШОГО екрана** —
  `/goals/*`, `/notifications/*`, `/settings/saved-filters`. `/tax/obligations/:id/paid` уже
  зроблено 2026-09-18 (зонд `tax_obligations` у `EXTRA_PROBES` є). ⚠️ `/tax/accounts/:id/business`
  свідомо НЕ додано: зонд `accounts` довелось би розширити на `is_business`, а це переписує кожен
  golden із рахунками — окреме рішення, не побічний ефект.
- ⬜ **`enrichPending`, `indexPending`, `/tax/backfill-rates` — однакова форма, різні слова.**
  Усі три: «пачка за виклик, клієнт повторює, поки `remaining > 0`». Один спільний інтерфейс
  зробив би четвертий бекфіл справою десяти рядків, а зараз кожен новий — це новий діалект.

Де шукати роботу, коли черга порожня:
- Читай `docs/*.md` і шукай правило, яке тримається на памʼяті, а не на перевірці. Кожне таке —
  кандидат на детермінований лінт або тест (принцип «Перевірка > інструкція»).
- Шукай число, яке рахується у двох місцях. Кожна пара — майбутній баг.
- Читай `HISTORY.md` і питай: чи цей клас багів уже неможливий, чи просто ще не повторився?
- Пройдись екранами й шукай стан, якого ніхто не намалював: порожньо, помилка, один рядок,
  дуже довга назва, дуже велике число, свіжий акаунт.
- Пиши тести на те, що зламається тихо: гроші, дати, валюти, межі періодів.
- Шукай мертвий код: експорти без імпортів, класи без правил, рядки i18n без використання.

**Обмеження, яких не порушувати навіть із вільними токенами:**
- нічого, що потребує живої перевірки, — це §0;
- жодного рефактора «бо красивіше» без бага, який він закриває;
- не піднімати стелі лінтів; не додавати винятків;
- не перезаписувати baseline, щоб позеленіти;
- кожна вигадана задача мусить мати «Готово-коли», яке перевіряється без обговорення.

---

## §7 — ЖУРНАЛ ПРОГРЕСУ

> Дописувати ЗВЕРХУ, одразу після кожної картки. Формат:
> `дата · картка · стан (✅ / ⛔ блоковано / ⚠️ знахідка) · один рядок ЩО саме · тести`

## Ніч 2026-09-18 — коротко (деталі рядками нижче)

Тестів **986 → 1050**, зелений бар тримався після кожної картки (остання перевірка 16:27 — зелено).
Лінтів стало **C1–C14**: три нові (C12 §APP_TZ, C13 `run_worker_first`, C14 власні кошти) і три
розширені (C7 — id зі шляху, C10 — §TAX-UAH і ₴ на клієнті, i18n — ключі без використання). Пройдено §2 (A1–A5) цілком, §3
майже цілком, по одному пункту з §4 і §5, далі §6.

**Що знайдено й полагоджено (баги, а не полірування):**
1. **§TAX-FX був МЕРТВИЙ** — `nbu.ts` індексував мапу валют не в той бік, тож до НБУ не йшло
   ЖОДНОГО запиту: кожне валютне надходження лишалось без бази, «Підтягнути курси» заповнювала 0.
   `tsc` цього не бачив (тип `Record<string, number>` приймає число як ключ).
2. **Конверт білився ДВІЧІ** після зміни групи — `refreshObligations` тільки upsert-ив, стара
   форма нарахування лишалась жити поруч із новою.
3. **Дедлайни Q4 не доходили до стрічки взагалі** — фільтр `period >= '2026'` сортує «2025-Q4»
   нижче, тож 10 лютого ФОП на 3 групі бачив ПОРОЖНІЙ список дедлайнів.
4. **Мовчазні записи на `/tax/*`** — `Number(param)` → NaN → UPDATE нічого не чіпає → 200 «ok».
5. **SSRF-вектор у §TAX-WATCH** — URL джерела приймався по `^https://\S+$`, редіректи не
   перевірялись; реєстрація ВІДКРИТА.
6. **§ENRICH-GATE не бачив нотатки** (червоний кейс евалу) — і блокер був у квоті, тож квоту
   зроблено.

**Що лишилось ВЛАСНИКУ** (крім §0): три знахідки A1, що потребують §-рішення, декларація-чернетка
(треба форма), мультивалютні інвойси (немає дати інвойса), «поділитись картинкою» для `/wrapped`
(рішення про залежність), `graphify label` і платний `npm run eval` — усе розписано в `ROADMAP.md`
і в пунктах вище.

| дата | картка | стан | що саме | тести |
|---|---|---|---|---|
| 2026-09-18 | §6 стале-стан після перемикача ФОП | ✅ | `setAccountBusiness` інвалідував лише `Tax` і `Account`, а §TAX-BASE каже, що операція БЕЗ власної відповіді успадковує рахункову — тобто один перемикач міняє те, ЧИМ Є кожен такий рядок, включно з перемикачем на екрані самої операції. Без `Tx` той показував би «за рахунком: ні» над ФОП-рахунком, доки щось інше не перечитає | 1050 |
| 2026-09-18 | 🔨 мовчазні записи: решта 34 | ✅ | закрив картку, яку сам поставив уранці: усі `Number(c.req.param(…))` у роутах переведено на `idParam` + 400 — 34 сайти в 9 файлах, плюс **35-й**, якого ранковий підрахунок не побачив (`routes/admin.ts`, бо він будує власний Hono). Через нього ж і розширив `idParam`: тип параметра тепер СТРУКТУРНИЙ (`{ req: { param } }`), тож помічник дістає й ті файли, що не поділяють `ApiEnv`. Лінт C7 тепер відмовляє самій ідіомі, а `bad-input.test.ts` тримає поведінкову половину — по кейсу на родину (16 нових) | 1050 |
| 2026-09-18 | §6 golden на ФОП-запис | ✅ | замість того щоб лишити C6-прогалину в черзі, закрив найважливішу її частину: три сценарії на `/tax/obligations/:id/paid` + зонд `tax_obligations`. Вони пінять саме ті три стани, що відрізняються: сплачено ОПЕРАЦІЄЮ (`paid_tx_id` = id), сплачено БЕЗ неї (200, але `paid_tx_id` null — видима різниця), і невідомий id (**404**, рядок не зрушив) — останній є регресійним замком на мовчазний запис, що його я лагодив уранці. Обʼєкт нарахування пишеться в таблицю НАПРЯМУ, а не через `refreshObligations`: інакше golden рухався б щоразу, коли в таблицю ставок додають новий рік | 1034 |
| 2026-09-18 | §6 ще одна заява, більша за чек | ⚠️ | `ARCHITECTURE.md` казав, що C6 — це «golden-стан БД після КОЖНОГО write-ендпоінта». Порахував: 142 сценарії покривають ~48 зі 101 write-роуту. 53 решти переважно НЕ забуті, а недосяжні (AI потребує ключа, push — VAPID, десяток налаштувань пишуть поле, яке тут-таки й читають), але це не «кожен». Таблицю виправлено на те, що чек реально робить, і названо середину, яку варто закрити: `/tax/*`, `/goals/*`, `/notifications/*`, `/settings/saved-filters` — вони міняють стан, який ІНШИЙ екран прочитає пізніше, тобто саме та форма, яку ловить golden і не ловить юніт | 1031 |
| 2026-09-18 | §6 C12 накрив і SQL-половину | ✅ | після того, як ручний скан знайшов цим справжній баг, правило зроблено механічним: C12 тепер ловить і КЛЮЧ дати/місяця в UTC. Збігається ВУЗЬКО — помилка має форму `new Date(<unix> * 1000).toISOString().slice(…)` або `new Date().toISOString().slice(…)`; календарна арифметика над уже локальними частинами (`Date.UTC` у `tax.ts`, зсув рядка-дати в `nbu.ts`) не чіпається, бо чек, що кричить вовк, — це кнопка «видалити» з правдоподібним поясненням. Три винятки, кожен із ФАКТОМ: `rateDayKey` (дата ПУБЛІКАЦІЇ курсу), `ym` у `networth.ts` (точка ряду будується в UTC), префікс R2 у `receipt.ts`. Доведено: повернув старий рядок → червоний саме на ньому | 1031 |
| 2026-09-18 | §6 §APP_TZ: ще один живий баг | ✅ | `/analytics/patterns` будував ключ ПОТОЧНОГО місяця через `toISOString()` (UTC), тоді як матриця, яку він цим ключем індексує, згрупована `localYmSql` (Київ), а шість трейлінг-ключів — через `localYm` (теж Київ). З 00:00 до 03:00 за Києвом ПЕРШОГО числа ці календарі розходяться, і ендпоінт віддавав ТОРІШНІЙ… тобто МИНУЛОМІСЯЧНИЙ підсумок як витрати цього місяця — і той самий місяць ще й потрапляв у список, з яким порівнюється. Доведено числом: у тесті старий код каже **20 000 ₴** (увесь липень) о 00:30 1 серпня, новий — **150 ₴**. Знайдено скануванням `toISOString()` у воркері (та сама ідея, що C12, але для SQL-половини) | 1031 |
| 2026-09-18 | §6 власні кошти: 5-та і 6-та копії | ✅ | `own-funds.ts` написаний після ЧОТИРЬОХ копій `balance − credit_limit`, і його ж хедер каже: «пʼята копія — це вечір, який проєкт втрачає». Пʼята й шоста існували — на `/accounts`, де їх не можна було уникнути: модуль лежав у `worker/lib/`, куди клієнт не дотягнеться. Переїхав у **`shared/own-funds.ts`** (той самий хід і та сама причина, що `shared/currency.ts`), клієнт тепер кличе `ownFundsMinor`/`debtMinor`. Плюс **лінт C14**, бо попередження всередині файлу читає той, хто редагує ТОЙ файл, а не той, хто пише сьому копію деінде | 1030 |
| 2026-09-18 | ⚠️ моя помилка з `git checkout` | ⚠️ | перевіряючи, що C14 ловить, я зламав `Accounts.tsx` навмисно й відкотив через `git checkout <file>` — а це відкат до HEAD, тобто ЗМІЛО всі мої зміни в тому файлі за ніч (на щастя, лише мої: у власника незакомічених правок там не було). Відновив усі чотири по памʼяті, перевірив чеками. **Правило на майбутнє: у дереві з незакоміченою роботою пробні зламування відкочувати КОПІЄЮ у скретчпаді, ніколи `git checkout`** | 1030 |
| 2026-09-18 | §6 новий лінт C13 (`run_worker_first`) | ✅ | ще один твердий інваріант, що тримався на памʼяті: роут поза `/api/*` мусить бути в `assets.run_worker_first`, інакше статика Cloudflare віддає SPA-ШЕЛЛ — 200 з HTML усередині, і ЖОДНОЇ помилки ніде. Симптоми звучать як «MCP-клієнт каже, що сервер віддав невалідний JSON» або «вебхук банку отримує 200, а операції немає» — тобто не як конфіг роутингу, і саме тому це коштує пів дня щоразу. Лінт звіряє `index.ts` (+ під-застосунки, змонтовані в `/`) з `wrangler.jsonc`. Доведено в обидва боки: прибрав `/quick-add` зі списку → червоний, повернув → зелений | 1030 |
| 2026-09-18 | §6 ₴-літерал на КЛІЄНТІ | ✅ | тверді інваріанти кажуть «Клієнт: `baseSign()`, ніколи літерал `₴`», а C10 перевіряв це ЛИШЕ у воркері — клієнтська половина трималась на памʼяті. Додано правило, і воно одразу знайшло ДВА справжніх: `placeholder="₴"` на полі мінімального платежу кредитки (яка може бути в доларах — тепер валюта САМОГО рахунку) і на сумі в плані події (тепер `baseSign()`). Два законних випадки стали винятками з причиною: матч-ключ у `highlight.tsx` (він шукає символи в тексті моделі, це дані) і сама таблиця символів у `shared/currency.ts` | 1030 |
| 2026-09-18 | самоперевірка: власна нетестована гілка | ✅ | у `quarterSummary` я написав КОМЕНТАР про те, що групи 1–2 нараховують помісячно й квартал мусить їх зібрати, — і не написав на це тесту. Тепер є: квартал групи 2 БЕЗ доходу дає 3×ЄП + 3×ВЗ + ЄСВ, а не порожній квартальний рядок. Саме та група, що платить незалежно від виторгу, тобто випадок, заради якого модуль і рахує тихий квартал як борг | 1030 |
| 2026-09-18 | §6 стан, якого ніхто не намалював | ✅ | «Календар платежів» на 1–2 ГРУПІ — це не 6 рядків, а ~28 на рік (ЄП і ВЗ щомісяця) і ~56 за два, причому сплачені — більшість. Несплачені лишаються повністю (вони і є суть), а сплачена історія згортається за поріг 6 — там, де колонка перестає проглядатись оком і стає архівом. Кнопка стоїть НА МІСЦІ рядків, які розкриє, щоб список не стрибав під курсором | 1029 |
| 2026-09-18 | самоперевірка нічного коду | ✅ | пройшовся по написаному за ніч і виправив ДВІ власні хиби, яких не бачить жоден лінт: (1) `cashGap` віднімав МОМЕНТ від ДАТИ — дві-три години скосу всередині числа, виміряного в ДНЯХ, і саме на межі, заради якої блок існує; тепер обидві сторони — київські дати, як у `days_left` (§APP_TZ). (2) `quarterSummary` рахував межі кварталу ВДРУГЕ (year, q → півночі), хоч `quarterWindow()` уже відповідає на це питання — переведено на нього, поки копії не розійшлись | 1029 |
| 2026-09-18 | граф коду | ✅ | `graphify update .` після ночі змін (інструкція `CLAUDE.md`, яку я не виконував по ходу): 3769 вузлів, 11772 ребра, 160 спільнот. ⚠️ Граф попереджає, що набір спільнот змінився й 81 перейменовано за хабом — `graphify label` (LLM) лишається власнику, бо потребує ключа | 1029 |
| 2026-09-18 | §6 мертвий код: компоненти | ✅ | скан «експорт без імпортерів» по всьому дереву знайшов дві речі. (1) **МОЮ ЖЕ помилку цієї ночі**: картку `BusinessCosts` написано, застилено, перекладено — і НЕ змонтовано в `Fop.tsx` (заміна тексту не спрацювала, а `tsc` про це не скаже: файл валідний, просто нікому не потрібен). Змонтовано. (2) `SubsCalendar.tsx` — 89 рядків, які нічого не рендерить: `HISTORY.md` прямо каже, що `CashflowCalendar` «замінив `SubsCalendar` на Підписках», а файл лишився. Видалено | 1029 |
| 2026-09-18 | §6 зчеплення чеків спрацювало | ✅ | видалення `SubsCalendar` ланцюжком підсвітило решту: свіжий чек на невикористані ключі i18n назвав `sc.title`/`sc.subtitle`, а C9 (другий напрямок) — 9 мертвих CSS-правил у `dashboard.css` (23 рядки). Тобто «видалити компонент» тепер автоматично питає «а рядки? а стилі?» — саме заради цього обидва чеки й існують | 1029 |
| 2026-09-18 | §6 мертві рядки i18n + чек | ✅ | 26 ключів, яких НІЩО не досягає (парольний вхід, що зник разом із Google-only; мовний перемикач, який тепер малює власні підписи; шість легенд Статистики, переписаних відтоді) — видалено з ОБОХ словників, 2227 → 2201. І лише ПІСЛЯ того, як список став нулем, додано перевірку в `check-i18n.mjs`: лінт, що приїжджає з дозволеним списком на 26 позицій, вчить додавати двадцять сьому. Безпечно саме тут, бо рантайм-ключі вже закриті: оголошений префікс пропускається, а НЕоголошений відмовляє чек 3 — третього способу дістатись до ключа немає. Доведено, що ловить: підкинув ключ → червоний, прибрав → зелений | 1029 |
| 2026-09-18 | §6 гілка помилки на `/wrapped` | ✅ | побічні запити сторінки падали ТИХО за побудовою (слайд рендериться лише коли його число є), а «нічого не намалювалось» не відрізнити від «спитали й зламалось» — рівно те, що забороняє §Обробка помилок. Один рядок унизу «частина слайдів не завантажилась» + повтор, а не чотири `ErrorNote`: звіт лишається читабельним без будь-якого одного слайда, тож чесне твердження саме таке | 1029 |
| 2026-09-18 | §6 межа року для `/insights/*` | ✅ | тест на найважчій межі, яка існує: 31 грудня 22:30 за Києвом — це вже 1 січня в UTC. ⚠️ І він перевіряє ту половину, якої СУМА не бачить: підсумок вікна порівнює лише моменти, тож пережив би й UTC-групування; а от ДНІ — ні (у Києві це 31.12 і 01.01, тобто два дні з витратами; в UTC вони злипаються в один). Доведено, що кусає: тимчасово обнулив зсув у `localFmtSql` → тест червоний (1 замість 2), відкотив → зелений. C12 бачить лише JS-половину цього класу, SQL-половину тепер тримає тест | 1029 |
| 2026-09-18 | §6 новий лінт C12 (§APP_TZ) | ✅ | правило «календар київський, жодних `new Date(x).getMonth()`» стояло ДВІЧІ прозою (тверді інваріанти `CLAUDE.md` + хедер `time.ts`) і трималось лише на памʼяті — а провал тихий: з 00:00 до 03:00 за Києвом застосунок був на добу позаду й показував правдоподібне число (реальна аварія 2026-08-01). Тепер `check-app-tz.mjs` у `npm run check`: сканує ЛИШЕ воркер (на клієнті зона рантайму і є зоною читача), дозволяє `getUTC*`, один виняток — сам `time.ts`, із причиною. Перевірено, що ловить: зламав `quarterLabel` → червоний, відкотив → зелений. Заодно в таблицю лінтів `ARCHITECTURE.md` дописано C11, якого там бракувало | 1028 |
| 2026-09-18 | §5 розмір `Stats.tsx` | ✅ | 418 → 359: винесено `StatsPeriodBar` — єдину частину оболонки, що НЕ про дані (перемикач періоду, місячний степер §MONTH-VIEW, режим, валюта). Пропси — ІНТЕРФЕЙС (`onRange`/`onToggleMode`/`onYm`/`onCurrency`), а не мішок сетерів: передати вниз `setSearchParams` означало б дозволити компоненту писати в URL сторінки будь-який ключ, і наступна правка це б і зробила. Далі різати нема по чому — лишились стан і ОДИН спільний запит, які оболонка й тримає навмисно | 1028 |
| 2026-09-18 | §4 два пункти розібрано, не зроблено | ⚠️ | «Черга масових прогонів»: розписано В ROADMAP, що саме треба (progress у рядку + пачка за тік + тік будильника) і ЧОМУ не наосліп — помилка в умові зупинки = DO, що крутиться вічно й пече гроші, а корисне навантаження без ключа не тестується; запропоновано безпечний порядок (фіктивний `noop_batch` із тестом спершу). «Хвости AI 4.0» — міняють контракт із провайдером, перевіряються лише живим ключем | 1028 |
| 2026-09-18 | §4 Wrapped | ✅ | сторінка `/wrapped`: рік у сумах, найдорожчий місяць, мерчант і категорія року, що росло найдовше (§MOMENTUM), тихі дні (§SPEND-PROFILE), ціни, що зрушили. **ЖОДНОЇ нової математики** — усі числа з наявних ендпоінтів, як і вимагала картка: рік-у-огляді це остання сторінка, куди можна вносити неперевірене число, бо саме її скриншотять і саме їй вірять. Балансів немає взагалі (звіт про рік — не виписка). Тонкий рік виглядає НАВМИСНО: слайд рендериться лише коли його число існує, порожній рік каже ЧОМУ. Вхід — зі Звітів і ⌘K (урок §MONTH-VIEW: сторінка, куди можна лише вписати шлях, не існує) | 1028 |
| 2026-09-18 | §5 чи ділити `drafts-due` | ✅ | ТАК, і причина — не розмір (226 рядків під стелею 400), а ЗАЯВА, що стала неправдою: хедер казав «кожен драфтер тут відповідає ‹гроші йдуть у дату, якої ти не обирав›», і це правда про підписку й кредитку, але не про «офіційна сторінка змінилась» і «клієнт замовк». Три ФОП-драфтери переїхали в `drafts-fop.ts`; спільне в них — не форма, а ПЕРЕДУМОВА: усі троє мовчать, поки модуль ФОП вимкнено, і тримати це в одному файлі — те, що робить правило перевірним поглядом. `drafts-due` 226 → 104 | 1028 |
| 2026-09-18 | C3 знову тримав | ✅ | переїзд додав `notify.ts` рівно 2 рядки (імпорт + коментар) і C3 упав: 696 проти стелі 694. Виняток НЕ піднято — обидва рядки стиснуто до нуля приросту (причини й так живуть у `quota.ts` і хедері `drafts-fop.ts`, де їх читають). Файл назад на 694 | 1028 |
| 2026-09-18 | §6 лінт замість памʼяті | ✅ | §TAX-UAH тримався НА ПАМʼЯТІ (знахідка A1 уранці): `<Money>` без `currency` друкує базу ЧИТАЧА, тож один забутий проп у майбутній ФОП-картці дав би правдоподібне число в чужій одиниці — рівно те, чого `tsc` не бачить. C10 розширено: кожен `<Money` під `components/fop/` мусить назвати валюту. Перевірено, що лінт ЛОВИТЬ: навмисно зламав `TaxHeader` → червоний із точним рядком, відкотив → зелений (чек, який ніколи не спрацьовує, нічого не перевіряє) | 1028 |
| 2026-09-18 | §5 борг: пʼята копія числа | ✅ | `Stats.tsx` рахував норму заощаджень САМ, хоч сервер віддає `summary.savings_rate_pct` — і це була ПʼЯТА копія тих самих трьох токенів (`MonthPulse` віддав четверту, `MonthlyHistory` третю, AI-репорт цитує серверну з липня). Арифметика ідентична сьогодні, і саме це робить копію небезпечною: розбіжність приїде з першою правкою будь-якої зі сторін. Прибрано; golden-файли пінять серверне число | 1028 |
| 2026-09-18 | §4 §ENRICH-GATE бачить нотатку | ✅ | НОТАТКА тепер перебиває КОЖЕН skip: «це я вивів свою зарплату з крипти через P2P» на рядку з MCC 4829 гейт зупиняв на «transfers bucket», і єдиний сигнал, який є однозначним ЛЮДСЬКИМ реченням, до моделі не доходив (виняток на Sonnet від 2026-07-14 був недосяжний саме для тих рядків, для яких його писали). Блокером у беклозі була КВОТА, і вона зроблена, а не вгадана: `countNoteEnrich` — 40/добу київського дня, тим самим механізмом, що `DAILY_RECEIPTS`; над стелею рядок падає у звичайні правила, а не викидається | 1028 |
| 2026-09-18 | §4 ціна зміни — видима | ✅ | `npm run eval -- --dry` (безкоштовно): гейт **100% (132/132)** — уперше, разом із кейсом `note-says-salary`, що був червоним до цієї ночі. Ask rate 74% → 75%, тобто ціна написана в тому ж звіті. Кейс `note-says-medicine`, який я сам запінив уранці на «skip», ПЕРЕПИСАНО на «ask» із записом ЧОМУ (§1 п.5: діф — це навмисна зміна, і вона названа), плюс новий `note-blank-on-obvious` пінить тримання порожньої нотатки. +6 юніт-тестів на гейт, бо eval не входить у `check` | 1028 |
| 2026-09-18 | §3 Підсумок за квартали | ✅ | `GET /tax/summary.csv` + кнопка поруч із книгою доходів: рядок на квартал — дохід, надходжень, ЄП, ВЗ, ЄСВ, сплачено, лишилось. Читається зі ЗБЕРЕЖЕНИХ зобовʼязань, не перераховується: сплачений квартал тримає суму, за якою його сплатили (§TAX-DUE, §BUDGET-MEMORY), а підсумок, який не звіряється з поданим, — це другий правдоподібний набір чисел, а не перевірка. Порожні квартали присутні нулями: «немає даних» і «ніхто не дивився» бухгалтер не відрізнить. Місячні рядки груп 1–2 враховані — інакше квартал показав би нуль. +1 тест | 1022 |
| 2026-09-18 | C3 два шви | ✅ | під час цієї картки C3 спрацював ДВІЧІ, і обидва рази назвав правильний шов: `business.ts` перетнув 400 → `quarterSummary` виніс у **`tax-report.ts`** (все в `business.ts` читає РЕЄСТР для екрана власника, а це — ЗОБОВʼЯЗАННЯ для третьої сторони; різні джерела правди, і ця різниця — вся цінність підсумку). `i18n.ts` перетнув 400 → сімʼя `csv*` виїхала в **`i18n-csv.ts`** (заголовок колонки для таблиці — інша аудиторія, ніж текст на екрані, і несе одиницю в собі). Жодну стелю не піднято | 1022 |
| 2026-09-18 | §3 Кеш-розрив | ✅ | `cashGap()` — те, що бачить лише цей застосунок: дедлайн настає РАНІШЕ, ніж приходять гроші, які його закривають. Фрилансер, якому платять 25-го, а податок 19-го, короткий КОЖНОГО кварталу, і жодна з двох цифр окремо не виглядає проблемою. Називається лише коли обидві половини реальні: несплачене зобовʼязання з датою + ритм, достатній, щоб назвати день (3 надходження, медіана ≥7 дн). Каже ДАТУ і на скільки днів коротко — і зупиняється: що робити (виставити раніше, тримати конверт, платити з особистих) — рішення власника. +1 тест на всі чотири гілки | 1021 |
| 2026-09-18 | §3 два пункти ⛔ | ⚠️ | «Квартальна декларація-чернетка» і «Мультивалютні інвойси» НЕ зроблено — і причини різні, обидві записані прямо в §3: перша потребує доведеної розкладки ФОРМИ (номери рядків, місце ЄСВ), а помилка в декларації коштує штрафу; друга заблокована ДАНИМИ — немає дати інвойса, є лише дата зарахування. Для першої описано варіант БЕЗ номерів рядків, який можна зробити наосліп | — |
| 2026-09-18 | §3 Клієнт, що замовк | ✅ | `quietClients()` — дохідний двійник §SUB-DETECT, і важливіший за нього: мертва підписка — це зекономлені гроші, а клієнт, що перестав платити, — це бізнес, який зменшується, і фрилансер помічає це на тижні пізніше. Судиться за ВЛАСНИМ ритмом клієнта (§RHYTHM): місячний прострочений на 6 тижні, а той, хто платить двічі на рік, не прострочений на 4 місяці — правило «90 днів» кричало б про другого щороку й місяць мовчало про першого. Мінімум 3 платежі й медіанний розрив ≥7 дн. Новий вид сповіщення `quiet_client` із власною преференцією (як `regulation`: хто заглушив витрати, не казав нічого про новини бізнесу). +1 тест на всі три випадки | 1020 |
| 2026-09-18 | C3 шов | ✅ | новий вид потребував 4 рядків у `notify.ts`, а той стоїть на своєму винятку — і виняток НЕ ПІДНІМАЄТЬСЯ. Шостий шов, який файл назвав сам: `draftPriceUps` + `draftDeadSubs` → `drafts-plans.ts` (обидва читають `plannedActuals()` проти `planned_payments` і відповідають «план, який ти оголосив, і списання, які ми бачимо, розходяться» — це НЕ «гроші йдуть за датою» і НЕ «місяць іде погано»). Стелю опущено 748 → 694 | 1020 |
| 2026-09-18 | §3 Робочі витрати по категоріях | ✅ | `businessExpensesByCategory()` + картка `BusinessCosts` на `/fop`: рол-ап до КОРЕНЯ (`COALESCE(parent_id, id)`, як усі розбиття в проєкті), гривня — за тим самим правилом, що й загальна сума, тож розбиття складається в число, надруковане над ним (це і перевіряє тест). Валютні робочі витрати НЕ переоцінюються: вони ніде не декларуються, тож курс вигадав би другу вартість тих самих грошей — сказано текстом у картці, а не пропущено. Візуально перевикористано `.fop-rank` з «Контрагентів»: «цей рядок — така частка» вже має словник на цій сторінці. +1 тест | 1019 |
| 2026-09-18 | §3 Сценарій ліміту + мертвий код | ✅ | картка ліміту тепер каже, ЩО СТАНЕТЬСЯ, а не лише що щось не так: «лишилось X ₴ до стелі; перевищення = 15% на надлишок + зміна групи з наступного кварталу», а в стані `exceeded` — що це вже подія. Відсоток відповідає на «як далеко я зайшов», але НЕ на «скільки ще можу виставити цього року» — питання того, хто зараз підписує договір | 1018 |
| 2026-09-18 | §6 мертвий код | ✅ | видалено два ЕКСПОРТИ БЕЗ ІМПОРТЕРІВ, знайдені під час §3: `limitHeadroom()` (13 рядків у `business.ts` — писалась саме під цю картку ліміту й ніколи не викликалась; свідомо НЕ реанімована, бо `taxStatus.limit` — єдине джерело річного ліміту, а друга функція на те саме число — це рівно та розбіжність, яку закривали всю ніч) і `monthLabel()` у `tax.ts` (однойменні функції на клієнті — інші). `rhythmOf` лишено: експорт чистого хелпера заради тестовності захищається | 1018 |
| 2026-09-18 | §3 ЄСВ у тихому кварталі | ✅ | найчастіше забутий платіж дістав ВЛАСНЕ речення, а не той самий рядок з меншим числом: «я нічого не заробив — нічого не винен» правда про ЄП на 3 групі й НЕПРАВДА про ЄСВ, фіксований у всіх групах. `draftTaxDue` питає дохід кварталу зобовʼязання (новий `quarterWindow()` — інверсія `quarterLabel`, щоб «який це квартал» не визначалось двічі) і при нулі бере `deadline_tax_quiet`. +1 тест (і зворотний бік: з доходом у тому ж кварталі рядок звичайний) | 1018 |
| 2026-09-18 | §3 Прогноз кварталу + порівняння груп | ✅ | `quarterOutlook()` у `business.ts`: нараховано вже / буде за темпом / прогноз доходу — і ціна КОЖНОЇ групи на ТОМУ САМОМУ доході (одна цифра, бо порахувати її двічі — це шлях до двох карток, що сперечаються про один квартал). Мінімальне вікно 14 днів: прогноз із однієї інвойсу 2-го числа — це впевнене твердження про три місяці (§CADENCE), тож до того `projected_*` = null і картка каже «надто рано». Група, що вже за своєю річною стелею, показана ПЕРЕКРЕСЛЕНОЮ — ціна без цього факту запрошує до рішення, якого закон не дозволяє. Віддається в тому ж `/tax/business`, +2 тести | 1017 |
| 2026-09-18 | §3 Календар платежів | ✅ | `TaxCalendar` на `/fop`: усі зобовʼязання за датою настання, несплачені першими, сплачені ЛИШАЮТЬСЯ у списку (питання «я вже сплатив другий квартал?» — саме те, на яке картка існує). Нового запиту НЕ додано: `/tax/status` уже віддає все від початку попереднього року. Кожен несплачений рядок дістає той самий `MarkPaid` — компонент, написаний годину тому, підійшов без правок. Для груп 1/2 це головна картка сторінки: там платіж 20-го ЩОМІСЯЦЯ, і «найближчий» не казав нічого про решту одинадцяти | 1015 |
| 2026-09-18 | §3 «Сплачено» як ЗВʼЯЗОК | ✅ | §TAX-DUE вимагав привʼязки до операції з самого початку, а кнопка ставила ГАЛОЧКУ без `tx_id` — тобто застосунок знав лише, що хтось натиснув. Додано `paymentCandidates()` (гривня, витрата, корінь 24 або без категорії, ±10%/200 ₴ від суми, ±45 днів від дедлайну, не привʼязана до іншого зобовʼязання; сортування — найближча сума, потім дата), ендпоінт `GET /tax/obligations/:id/candidates`, компонент `MarkPaid` (пропонує — вибирає ЛЮДИНА; «без операції» лишається, але другорядним). +2 тести. Закрито разом і «автопідбір операції-сплати» з §3 | 1015 |
| 2026-09-18 | §3 ФОП-рахунок з UI | ✅ | єдиний перемикач, на якому стоїть увесь модуль, існував ЛИШЕ як `POST /api/tax/accounts/:id/business` — тобто ставився тільки через curl, а непозначений рахунок робить усі надходження особистими й `/fop` показує квартал з нулем без жодного пояснення. Додано `AccountBusinessToggle` у редактор рахунку + бейдж «ФОП» на картці; НЕ третє значення `role` (`docs/TAX.md` відкинув це, бо конверт почав би рухати runway). Рішення записано в «Журнал рішень» `DESIGN.md`, §0.2 оновлено на новий шлях | 1013 |
| 2026-09-18 | A5 продуктивність | ✅ | `businessOverview` коштував 26 мс НА КВАРТАЛ при 20k операцій (6 кварталів = 129 мс, дозволені 12 = 250 мс) — бо канон у `IN (SELECT …)` обчислювався по ВСІЙ таблиці на кожен агрегат, а зовнішній `t.time` потім викидав майже все. Вікно продубльовано ВСЕРЕДИНУ підзапиту: **129 → 32 мс** (12 кварталів: **250 → 33 мс**). Відповіді байт-у-байт ті самі; форму `IN` збережено + тест на роздільну операцію (§SPLIT: SUM по розмножених рядках дав би 120k замість 60k) | 1013 |
| 2026-09-18 | A5 продуктивність | ✅ | `pendingCount`/`indexPending` у `search-vec.ts` виміряно на 20k: **1.2 мс**, `remaining=20079` — побоювання картки не підтвердилось, фулскану немає. Гарячі ендпоінти по плану запитів: `/analytics/overview` 13 стейтментів, `/transactions` і `/summary` <1.5 мс, `t.time` усюди йде через `idx_tx_time` | 1013 |
| 2026-09-18 | A5 продуктивність | ⚠️ | батчити 18 стейтментів `/tax/business` НЕ треба — і це відповідь на питання картки: усередині Durable Object SQL — це локальний СИНХРОННИЙ виклик `SqlStorage`, тож амортизувати нічого (немає раунд-трипів). Коштувала не кількість запитів, а повторний прохід по таблиці в кожному — що й виправлено вище | 1013 |
| 2026-09-18 | A4 eval-харнес | ✅ | датасет 74 → **131** кейс: нові групи **`fop`** (Payoneer/Deel/інвойс у ₴/комісія РКО/робочі інструменти з ритмом/сплата ЄП-ВЗ-ЄСВ) і **`csv`** (шлях імпорту БЕЗ MCC — Ашан латиницею й кирилицею, мультимаркет на три корені, комуналка, названа місяцем), +7 нотаток, +8 підписок, +3 carry, +3 перекази. Гейт кожного нового кейса підтверджено БЕЗКОШТОВНО через `--dry`: 60/60 | 1012 |
| 2026-09-18 | A4 eval-харнес | ⚠️ | 150–200 НЕ досягнуто свідомо: правило 4 датасету («лише реальні описи») — стеля. Далі потрібен анонімізований експорт власника; вигадати ще 70 мерчантів = міряти вигадану проблему. Група «чеки з OCR» у цьому харнесі неможлива взагалі: він ганяє `categorize()→enrichVerdict()→enrichOne()` над РЯДКОМ, а фото чека йде через `receipt.ts` — інша драбина, потрібен окремий харнес. Обидва обмеження записані в `__eval__/README.md` | — |
| 2026-09-18 | A4 eval-харнес | ⚠️ | `baseline.json` НЕ перезаписано: `--dry` не має вердикту моделі, тож `--record` після нього затер би реальний базлайн нулями. Нові кейси показуються як «new, not in the baseline», доки власник не зробить платний прогін (§0.5 вже містить «`npm run eval` після зміни промту») | — |
| 2026-09-18 | A3 периметр | ✅ | SSRF у §TAX-WATCH: URL джерела приймався по `^https://\S+$`, що правда й для `https://127.0.0.1/`, `https://[::1]/` і для будь-якого хоста, що віддає 302 на них. Реєстрація ВІДКРИТА, тож це незнайомець вибирає запит, який воркер робить щоночі з мережі Cloudflare. Додано `isWatchableUrl()` (https, без IP-літералів у будь-якому написанні, без хостів без точки й без зарезервованих суфіксів) і застосовано ДВІЧІ — на вставці й на `res.url` ПІСЛЯ редіректів. +2 тести | 1012 |
| 2026-09-18 | A3 периметр | ✅ | ДЕДЛАЙНИ Q4 НЕ ДОХОДИЛИ ДО СТРІЧКИ: `draftTaxDue` фільтрував `period >= '2026'`, а «2025-Q4» сортується НИЖЧЕ — тож 10 лютого ФОП на 3 групі з повним Q4 отримував ПОРОЖНЮ стрічку дедлайнів, хоч ЄП/ВЗ треба сплатити 19 лютого, а ЄСВ прострочений із 19 січня. Вікно тепер від попереднього року (як у `taxStatus`) + тест | 1010 |
| 2026-09-18 | A3 периметр | ✅ | `tax-watch.ts` і `docs/TAX.md` §0.2 обидва казали «записано в `docs/PERIMETER.md`» — там цього НЕ БУЛО. Додано §TAX-OUT: що саме несуть обидва вихідні виклики (НБУ: валюта+дата; сторінка: нічого), і правило URL. Індекс § у `CLAUDE.md` оновлено | 1012 |
| 2026-09-18 | A3 периметр | ✅ | винятки лінтів перевірено на протухлість: `RAW_RATES_OK`(money.ts) і `HRYVNIA_OK`(i18n.ts) — обидва факти ще правдиві, у i18n.ts ₴ лише там, де НАЗИВАЄ одиниці. ⚠️ Структурна слабкість без бага: виняток дається на ФАЙЛ, тож другий ₴ у тому ж файлі пройде мовчки. `deadline_tax` тепер друкує `hryvniaSign()`, а не літерал (§TAX-UAH) | 1012 |
| 2026-09-18 | A2 консистентність | ✅ | МОВЧАЗНІ ЗАПИСИ на `/tax/*`: `Number(c.req.param("id"))` давав NaN → біндився як NULL → UPDATE не чіпав нічого, а роут відповідав 200 (`{ok:true}` на видалення, що нічого не видалило; повний статус на «сплачено», що нічого не позначило). Додано `idParam()` у `_shared.ts`, а `markPaid`/`setBusiness`/`setAccountBusiness` тепер повертають `meta.changes > 0` → 400/404. Тести пінять КЛАС у `bad-input.test.ts` (7 кейсів) | 1008 |
| 2026-09-18 | A2 консистентність | ✅ | `businessOverview` читав контрагентів по `[qStart, now)`, а `top_share_pct` ділив на дохід кварталу `[qStart, qEnd)` — операція з майбутньою датою потрапляла в знаменник і не в чисельник. Одне вікно на один квартал + тест «клієнти складаються в квартал, надрукований вище» | 1009 |
| 2026-09-18 | A2 консистентність | ⚠️ | C1 НЕ те, що писала документація: лінт — це флет-бан на `.prepare()` у `routes/`+`services/`, а не «SQL лише в `repo/`». У `lib/` 300 виклик-сайтів проти 285 у `repo/` — і так замислено. `CLAUDE.md` і `ARCHITECTURE.md` тепер кажуть, що чек реально стежить; твердим лишається вужче правило «другого визначення того самого числа не буває» | 1009 |
| 2026-09-18 | A2 консистентність | ✅ | гілки помилок `/fop` перевірено без правок: скелет → `ErrorNote` з `onRetry` на обидва запити → картка профілю, коли модуль вимкнено; `RegWatchCard` має власну гілку. `/settings/search/*` числових параметрів не має взагалі | 1009 |
| 2026-09-18 | A1 ФОП | ✅ | §TAX-FX БУВ МЕРТВИЙ: `nbu.ts` індексував `CURRENCY_BY_CODE` (letters→number) числом 840, отримував `undefined` і віддавав «курсу немає» — жодного запиту до НБУ не йшло, кожне валютне надходження лишалось без бази, «Підтягнути курси» заповнювала 0. Тип `Record<string, number>` робив це невидимим для `tsc`. Виправлено на `currencyCode()` + 4 тести (вихідні, новорічні свята, офлайн, гривня) | 995 |
| 2026-09-18 | A1 ФОП | ✅ | мапу перейменовано `CURRENCY_BY_CODE` → `CURRENCY_NUM_BY_LETTERS`, щоб назва казала напрямок: саме назва й породила баг вище. Оновлено `CLAUDE.md` (мапа коду) | 995 |
| 2026-09-18 | A1 ФОП | ✅ | `refreshObligations` був UPSERT-ONLY: зміна групи 3→2 (а також увімкнений `esv_exempt` і `single_override`=0) лишала попередню форму нарахування живою, і один відкритий квартал білився ДВІЧІ в конверті. Додано `pruneObligations` + 3 тести | 989 |
| 2026-09-18 | A1 ФОП | ✅ | §APP_TZ і «одна сума в трьох місцях» перевірено й ЗАПІНЕНО: податковий рік перевертається о київській півночі (31.12 22:30 vs 01.01 00:30), а квартал однаковий у `taxStatus` / `businessOverview` / `incomeLedger` — розбіжностей не було | 991 |
| 2026-09-18 | A1 ФОП | ⚠️ | три знахідки НЕ виправлено наосліп (кожна потребує §-рішення, не запиту) — повернення клієнту не зменшує дохід кварталу; операція з майбутньою датою оподатковується, але не йде в річний ліміт; темп ліміту не має мінімального вікна, тож 1 січня прогнозує з однієї доби. Розписано в `ROADMAP.md` → Беклог → ФОП | — |
| 2026-09-18 | A1 ФОП | ✅ | §TAX-UAH чистий: кожен `<Money>` у `components/fop/*` передає `currency={HRYVNIA}` явно, `baseSign()` на `/fop` не просочився. Правило тримається на памʼяті — кандидат на лінт (записано в §6) | — |
| 2026-09-18 | (старт файлу) | ✅ | черга розписана, код не змінювався | 986 |
