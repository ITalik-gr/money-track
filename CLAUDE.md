# CLAUDE.md — Money Track (головний довідник проєкту)

> **Цей файл = єдина точка входу.** Claude Code вантажить його автоматично щосесії.
> Тут — усе глобальне, що треба знати ЗАВЖДИ: стек, інваріанти, як працює статистика,
> категоризація, AI-модель, ops. Історія «раунд-за-раундом» тут НЕ живе (вона в git/пам'яті).
> Оновлено 2026-07-20 (AI 4.0 §A1–A5 закрито; §SPLIT-регресія й обробка помилок полагоджені;
> Центр сповіщень фаза 1 — ⚠️ міграція 0023 на remote).

## 📁 Система документів (як усе влаштовано)
- **`CLAUDE.md`** (цей файл) — durable-довідник = колишній PROJECT.md. Глобальні налаштування, інваріанти, «як усе працює зараз». Сюди **виписуй важливе, коли доробив фічу** (див. робочий процес).
- **`DESIGN.md`** — дизайн-система (живий документ). **Читай ПЕРШИМ перед будь-якою роботою над UI/UX.** Токени, патерни, референси, «Журнал рішень». Код токенів — `src/index.css`.
- **`ROADMAP.md`** — жива черга задач/фіч (що зробити й доробити). **Завжди існує.** Додавай сюди нові таски; коли **доробив — викреслюй** (видаляй пункт); якщо результат важливий довгостроково — переноси стислий підсумок у `CLAUDE.md`.

## graphify

This project has a knowledge graph at graphify-out/ with god nodes, community structure, and cross-file relationships.

Rules:
- For codebase questions, first run `graphify query "<question>"` when graphify-out/graph.json exists. Use `graphify path "<A>" "<B>"` for relationships and `graphify explain "<concept>"` for focused concepts. These return a scoped subgraph, usually much smaller than GRAPH_REPORT.md or raw grep output.
- If graphify-out/wiki/index.md exists, use it for broad navigation instead of raw source browsing.
- Read graphify-out/GRAPH_REPORT.md only for broad architecture review or when query/path/explain do not surface enough context.
- After modifying code, run `graphify update .` to keep the graph current (AST-only, no API cost).


### 🔁 Робочий процес (тримати завжди)
1. **UI/UX** → спершу `DESIGN.md`, потім код. Кожну дизайн-зміну фіксуй у «Журналі рішень» DESIGN.md.
2. **Нова задача/ідея/баг** → одразу в `ROADMAP.md` (щоб не загубити).
3. **Доробив задачу** → (а) видали пункт з `ROADMAP.md`; (б) якщо це змінює «як усе працює» (новий інваріант, нове канонічне визначення, новий ops-крок) — онови відповідний розділ `CLAUDE.md`.
4. **Гроші/статистика** → будь-яка нова аналітика рахується ТІЛЬКИ через `worker/lib/stats.ts` (єдине джерело). Не дублюй SQL-фільтри в ендпоінтах.
5. **Green-бар перед «готово»:** `npm run check` (tsc app+worker **+ SQL-лінт**) + `npm run build`. Канонічний SQL — валідуй на D1.
   `npm run check` тепер ганяє `scripts/check-stats-sql.mjs`: запит, що згадує канонічний хелпер
   (`amountSum`/`SPEND_WHERE`/`EFF_*`…), МУСИТЬ мати `STATS_JOINS` — інакше падає в рантаймі
   (`no such column: sp.amount`), а `tsc` цього не бачить (SQL = рядок). Див. §Обробка помилок.
6. **Деплой/жива перевірка — рутина КОРИСТУВАЧА** (див. §Ops). Секрети й реальний API-ключ — не в мене.

## Стек
PWA на одному **Cloudflare Worker (Hono) + D1 + R2**. Monobank (webhook + бекфіл). React 19 + Redux Toolkit + React Router 7 + Recharts, Vite + vite-plugin-pwa, `@cloudflare/vite-plugin`. AI = гібрид **Haiku 4.5** (масово: enrich/OCR/parse/insight/batch) / **Sonnet 5** (розумний user-facing: порадник, репорти, txChat, бюджет-план, рев'ю). **Виняток (2026-07-14):** enrich бере Sonnet, коли користувач САМ описав операцію нотаткою (`user_note` непорожній) — розпізнання поважає пояснення (не плутає «вивів зарплату» з «Подарунком»); авто-enrich без нотатки лишається на Haiku. Telegram-бот (каркас). Мова UI — українська. Шрифти: Geist Sans (герой-суми) / Geist Mono (колонки, `tabular-nums`).

### Скрипти
`npm run dev` · `npm run build` (tsc -b + vite build) · `npm run check` (tsc) · `npm run deploy` (build + wrangler deploy) · `npm run db:migrate:local` / `:remote` · `npm run db:seed:local`.

### Мапа коду
- `worker/index.ts` — Hono-застосунок; `worker/routes/*` (api, setup, telegram, webhook, ingest); `worker/lib/*` — уся логіка (**`stats.ts`** канонічні розрахунки, `categorize`, `subscriptions`, `ai`, `advisor`, `report`, `insight`, `finance`, `transfers`, `mono`, `enrich`, `receipt`, `repo`, `auth`, `alert`, `proactive`, `backfill`, `telegram`).
- `src/` — React: `App.tsx`, `components/*` (BalanceCard, EnvelopeGrid, TransactionList, CategoryModal, GroupModal, GoalModal, TransferReviewModal, KpiRow, CashflowChart, Gauge, Select…), `lib/` (brands, markdown).
- `migrations/*` — 0001→0015. `shared/types.ts` — спільні типи. `wrangler.jsonc`, `.dev.vars` (локальні секрети, у .gitignore).

## 🔒 Інваріанти (тримати ЗАВЖДИ)
- Гроші — **INTEGER-копійки** скрізь; ділимо на 100 лише в показі.
- Агрегація по `COALESCE(parent_id, id)` (рол-ап підкатегорій у батька).
- Кредитний ліміт НІКОЛИ не зливати з власними: власні = `balance − credit_limit`, борг окремо.
- Мультивалюта: `transactions.currency_code` = валюта РАХУНКУ (mono `amount` у ній); `original_amount`/`original_currency` = валюта операції. Зведення в ₴ — лише через `toUAHMinor`/курси (`app_state.rates`).
- **Пара-переказ = ЄДИНО `transfer_pair_id`** (ставить лише крок 1 `detectTransfers`). `is_transfer=1` НЕ згортає пару в один рядок — його ставлять 5 шляхів (вставка `repo.ts` через `descriptionIsTransfer`, AI-enrich, alias, ручне, одностороннє), і жоден з них pair_id не дає. Список ховає «+» сторону тільки по `transfer_pair_id` (`api.ts` `/transactions`). **Holds парують** (2026-07-15): моно лишає внутрішній рух («Округлення балансу», «Поповнення «На квартиру»») холдом надовго, а вхідну сторону на банці постить одразу `hold=0` — старий фільтр `hold=0` у детекті різав мінусову сторону, тож пара не збиралась і обидва рядки висіли окремо. Зміна суми на сеттлменті розпарює ОБИДВІ сторони (`repo.upsertMonoTx`) → наступний detect збирає заново.
- Секрети лише у Worker secrets, ніколи в git/на клієнті. Вебхуки — секретний сегмент шляху.
- `Select` замість native `<select>`. Тема світла/темна рівноправні. Дизайн свідомо НЕ «аішний» (див. DESIGN.md).
- **Ручна назва операції авторитетна:** `transactions.name_locked=1` (ставиться при ручній зміні мерчанта в TxDetail) → enrich/ре-світ НЕ перезаписує `merchant` (`merchant = CASE WHEN name_locked=1 THEN merchant ELSE ? END`); категорію/ai_note ще уточнює. Знімається кнопкою «дозволити AI оновлювати». Міграція 0019.
- **Роль рахунку (§R3):** `accounts.role` = `liquid` (дефолт/NULL) або `investment`. Ліквідна ПОДУШКА/runway рахуються лише з `liquid`; `investment` (крипта/брокер) — окремий інвест-резерв, НЕ подушка за замовчуванням. `accounts.ai_note` — опис рахунку для AI. Єдине джерело коштів — `fundsBreakdown()` (advisor.ts): `{cushion, debt, investment, net, accounts[]}`. Міграція 0018.
- **Сума плану в ₴ — лише `plannedUAH()`/`sumPlannedUAH()`** (subscriptions.ts, §CUR-PLAN 2026-07-20).
  `planned_payments.period_amount` — у ВАЛЮТІ ПЛАНУ (`currency_code`), як і в транзакціях. Пʼять місць
  сумували його напряму, тож підписка $5 важила 5 ₴: «Скоро спишеться», Cashflow-календар, прогноз місяця,
  safe-to-spend і **AI-контекст порадника** (там поле навіть звалось `amount_uah`, а конверсії не було).
  У SQL-агрегатах зводь через `uahMult(rates, "currency_code")`. **Ніколи не сумуй `period_amount` сирим.**
  UI: суму показуй у валюті плану, ₴-еквівалент — окремим підписом; підсумки/проєкції — завжди в ₴.
- **AI-повернуті id категорій валідуй перед записом (§FK-GUARD 2026-07-20).** Категорій 45, але id
  доходять до 54 — у діапазоні дірки від видалених, і «правдоподібний» id від моделі влучає в неіснуючий
  рядок → `D1_ERROR: FOREIGN KEY constraint failed` на enrich. Хелпер `existingCategoryIds()` (enrich.ts)
  фільтрує `category_id` + `tag_ids` одним запитом; alias пише лише перевірений id.
- **Наступне списання плану — лише `nextChargeUnix(startDate, period, count, now)`** (subscriptions.ts, ЄДИНЕ джерело; враховує `period_count` — «кожні N періодів»). Не дублювати логіку в ендпоінтах.
- **Фінансовий контекст для AI — лише `collectFinanceSnapshot(env)`** (advisor.ts, ЄДИНЕ джерело, 2026-07-14). І Порадник (`buildAdvice`), і Чат (`chatReply`) беруть ТОЙ САМИЙ знімок: розбивка коштів (`fundsBreakdown`), канонічний burn (`sumLevels`)/runway, підписки, бюджети, вагомість, тренд 6 міс, разові/регулярні, найближчі списання (`upcoming_charges`). → цифри чату = цифри Порадника. НЕ будувати збіднений контекст для чату вручну (це давало баг «домисленої подушки $780» + розсинхрон runway).

- **Спліт транзакції (§SPLIT, міграція 0021):** одна витрата ділиться на кілька категорій (`tx_splits`). ЄДИНА точка інтеграції — `STATS_JOINS` (LEFT JOIN `tx_splits sp` + `sc`/`scp` для рол-апу категорії частини) + `EFF_AMOUNT = COALESCE(sp.amount, t.amount)` + `EFF_CAT_*`/`EFF_IMPORTANCE` (частина спліту має пріоритет). Спліт-tx розмножується на рядки-частини → сума/категорія беруться з частини, тож спліт іде в УСЮ категорійну аналітику узгоджено. **Правило:** у будь-якому STATS_JOINS-запиті сума витрати = `-EFF_AMOUNT` (НЕ `-t.amount`), інакше спліт-tx переоблічується. Дохід (`INCOME_WHERE`) і balance-реконструкція (capital-trend — з `accounts.balance`) спліт НЕ чіпають. Лише витрати. Валідувати на D1.
  **⚠️ Дзеркальне правило (регресія 2026-07-20):** зворотне теж обовʼязкове — запит, що вживає
  канонічний хелпер, МУСИТЬ мати `STATS_JOINS`. `amountSum/spendSum/incomeSum` і `EFF_*` посилаються
  на аліаси `sp`/`sc`/`scp`, які дає лише він; без джоїнів D1 кидає `no such column: sp.amount`.
  Ловиться `npm run check` (SQL-лінт), бо `tsc` цього не бачить.
  **Рахунок операцій у спліт-запитах — `COUNT(DISTINCT t.id)`**, не `COUNT(*)`: джоїн розмножує рядок
  на частини, тож `COUNT(*)` завищує кількість (і занижує середній чек).

## 🚨 Обробка помилок (ЄДИНІ правила, 2026-07-20)
Введено після регресії, де вся Статистика мовчки порожніла, а AI-чат казав лише `[object Object]`.
Три шари, кожен — обовʼязковий:
- **Сервер:** `app.onError` (`worker/index.ts`) віддає для `/api`+`/ingest` JSON `{error, detail}` і логує стек.
  Без нього неспійманий throw давав порожній 500 `text/plain` — клієнту не було що показати.
  Ендпоінти й далі можуть повертати свій `{error}` — форма та сама.
- **Клієнт, текст помилки — ЛИШЕ `errText(e)`** (`src/lib/errors.ts`). **Ніколи `String(e)`**: RTK Query
  відхиляє проміс простим обʼєктом `{status, data}`, тож `String(e)` = `"[object Object]"` — рівно нуль
  інформації. `errText` розгортає тіло `{error}` → статус → `Error.message`. Є ще `errStatus(e)` для гілок за кодом.
- **Клієнт, видимість:** (а) `apiErrorMiddleware` (`src/store/errorMiddleware.ts`) — toast на БУДЬ-якому
  впалому запиті (дедуп 8с на ендпоінт, 401 мовчить бо є редірект на вхід); (б) `<ErrorNote error={…}
  what="…" onRetry={refetch} />` — інлайн там, де сторінка інакше показала б порожнечу.
  **Правило:** сторінка з `data?.x ?? []` МУСИТЬ мати гілку помилки — порожнеча й збій виглядають по-різному.
- **AI-гілки не глушити:** `catch` навколо AI-виклику показує `errText(e)`, а не «Спробуй ще раз» —
  інакше ліміт/ключ/збій моделі не діагностуються.

## 🧮 Канонічне визначення статистики — `worker/lib/stats.ts` (ЄДИНЕ джерело)
Використовується УСІМА `/analytics`-ендпоінтами + AI-контекстом (порадник, інсайт, репорти) → **цифри UI = цифри AI**.
- **§REFUND (2026-07-21): повернення коштів — це НЕ дохід, а ВІД'ЄМНА ВИТРАТА своєї категорії.**
  `IS_REFUND` (stats.ts) = надходження, поза бакетом 13, і (ефективна категорія ІСНУЄ та `is_income=0`
  АБО опис від банку «Скасування…»). Рефанд проходить `SPEND_WHERE` (тож `amountSum` віднімає його,
  бо рахує `-EFF_AMOUNT`) і НЕ проходить `INCOME_WHERE`. `SPEND_COUNT` рефанди НЕ рахує — інакше
  середній чек ділився б на більшу кількість.
  **Чому:** `INCOME_WHERE` був просто `amount > 0` — «Скасування. BlaBlaCar +145 ₴» їхало в ДОХІД,
  а покупка −145 ₴ лишалась повною витратою «Транспорту». На реальних даних (remote) дохід був
  завищений на **10 776 ₴**, витрати — на **2 533 ₴**.
  ⚠️ Категорія мусить ІСНУВАТИ: `COALESCE(..., 0)` зробив би рефандом вхідний P2P «Від: Кирило».
  ⚠️ `INCOME_WHERE` тепер виключає й бакет 13 (симетрично до `SPEND_WHERE`): надходження-перекази
  з `is_transfer=0` (виплата банки, «З Білої картки») рахувались доходом, хоча їхня вихідна сторона
  вже виключалась — саме ця асиметрія давала більшу частину завищення (8 243 ₴).
  ⚠️ `categoryMonthlyLevels` має підлогу 0: місяць може вийти від'ємним, якщо повернення перевищило витрати.
- **Витрата:** `amount<0` (або рефанд, див. §REFUND), `transfer_pair_id IS NULL`, НЕ (`is_transfer=1 AND real_category_id IS NULL`), ефективна категорія `IS NOT 13` («Перекази і зняття»). **Holds рахуються** (mono надсилає лише виконані; коли hold закривається — той самий `id` перезаписується, тож без подвійного рахунку). Прапорець `hold` лишається на рядку для UI-бейджа «в обробці». *(Раніше `hold=0` різав свіжий тиждень у репорті — виправлено 2026-07-10.)*
- **Ефективна категорія** = рол-ап `real_category_id` (готівка/зняття за реальною суттю), інакше рол-ап `category_id`. Хелпери: `EFF_CAT_*`, `SPEND_WHERE`, `INCOME_WHERE`, `STATS_JOINS`, `spendSum/incomeSum/amountSum`, `uahMult` (₴-зведення inline-CASE з курсів), `valueMode` (₴ або «чиста» валюта), `periodBounds/currentPeriodToDate/lastCompletePeriod`.
- **Вагомість (§6):** `EFF_IMPORTANCE` = `COALESCE(t.importance, рол-ап importance ефективної категорії, 'discretionary')`. Рівні `essential|discretionary|optional`. Задається на категорії (дефолт) + override на транзакції. Міграція 0016.
- **Разові vs регулярні (§E1):** `isRecurringExpr` — операція регулярна, якщо прив'язана до плану (`planned_id IS NOT NULL`, напр. квартальна підписка) АБО її мерчант має витрати у ≥3 різних місяцях трейлінг-вікна. `recurringOneoffSplit()` дає {recurring, oneoff, oneoff_items}.
- **Місячний рівень категорії — `categoryMonthlyLevels()` (ЄДИНЕ джерело, 2026-07-12).** Один «скільки на місяць» на категорію, узгоджений скрізь (Патерни `usual`, Порадник/Бюджети `avg_month_uah`): fixed-кости (рента/підписка — останні 2-3 повні міс стабільні, CV≤0.12) → рівень = середнє останніх платежів (ловить стрибок ціни, напр. рента 8к→12500); змінні категорії → середнє за вікно (не хапає випадковий пік). Рахує лише по ПОВНИХ місяцях. Замінив розсинхрон «6-міс середнє / 90д÷3 / останній платіж». НЕ рахувати місячну суму категорії деінде вручну — брати звідси. **§A1 (2026-07-19):** наприкінці функції — `applyFactAdjustments()`: ПІДТВЕРДЖЕНІ факти (`facts.confirmed_at IS NOT NULL`, активні на `now`) коригують рівень (multiplier ×/ delta_minor +). Це ЄДИНЕ місце, де факт рухає число (не в ендпоінті) — тож burn/runway/Патерни/чат лишаються узгодженими.
- **Місячний BURN (знаменник runway) — `sumLevels(levels)` = сума рівнів категорій (ЄДИНЕ джерело, P1 2026-07-14).** Замінив «витрати_90д ÷ 3» у Пораднику (`buildAdvice`), AI-бюджет-плані (`proposeBudgets`) і бюджет-чаті (`budgetChatReply`). Тепер «Витрати/міс» = сумі `usual` Патернів → одна цифра всюди; не роздувається разовими лумпами (податок/лікар — рівень їх усереднює/виключає), ловить стрибок fixed-косту одразу. `runway = ліквідна_подушка ÷ burn`. **Виняток:** `/analytics/forecast.projectedSpend` — це ІНША цифра (проєкція саме поточного місяця, з його разовими), її свідомо НЕ чіпали.
- **Прогноз темпу (`/analytics/patterns`) — `projectSpend()` (stats.ts):** прогноз кінця місяця = «вже витрачено + історичний залишок» (НЕ наївний `spent/elapsedFrac`, що роздував рано в місяці / лумпи). Лумпи (1-2 великі операції: податок/оренда/заправка — детект `n≤1 OR biggest≥55%`, або fixed-кост ще не сплачений) НЕ екстраполюємо; кеп 3× звичного. `usual` — з `categoryMonthlyLevels`. `mostly_oneoff`/lumpy — поза «Радаром аномалій». Агрегат `/analytics/forecast` — бленд поточного темпу з історією 3 міс. Стара логіка (elapsedFrac × регулярна частина) замінена.
- **Період:** `app_state.period_mode` (`calendar`|`rolling`), перемикач у Статистиці; Головна й Статистика рахують ОДИН період.

## 🧠 Категоризація (детермін.-first, AI-last)
Порядок у `categorize()`/`enrich()`: 1) навчений `merchant_alias` (точний опис) → 2) активна підписка (мерчант+сума+валюта, `subscriptions.ts`) → 3) консенсус мерчанта (корінь назви ≥3× ≥80% в одну кат.) → 4) `mcc`/`text` rules → 5) AI-enrich (Haiku, з `known_subscriptions`-нюджем).
- **Alias source (0014):** колонка `source` (`manual`|`ai`). `learn`→manual; enrich→`writeAiAlias` НІКОЛИ не перетирає manual; авто-ре-світ пропускає manual; консенсус важить ручні ×3.

## 💸 AI-модель і вартість
- Ціни за MTok: **Haiku $1/$5, Sonnet $3/$15, Opus 4.8 $5/$25.** Cache read ≈0.1× input, write ≈1.25–2×.
- **Лічильник** (`app_state.ai_usage`, міграція 0010): `recordUsage` акумулює usage+вартість у `callHaiku`/`callHaikuMessages`; `GET /ai-usage`; картка «💸 Витрати на AI» в Налаштуваннях.
- **Задачі-моделі:** `report`/`advisor`/`insight`/`chat`/`budget`/`group`/**`notify`** (спостереження у стрічці сповіщень, дефолт Haiku, ≤1 виклик/добу).
- Реалістично ~$1–2/міс на Sonnet-гібриді; user-facing на Opus ≈ $1.5–3/міс. Модель-за-задачею через `MODEL_SMART` в `ai.ts`.

## ✅ Зроблено (компактно)
**База:** мультивалюта; гібрид AI; профіль «про мене» в усіх викликах; детермінована категоризація (5 ланок); пари-перекази 1 рядком (`transfer_pair_id`); TxDetail (ai_note+user_note, тег-пікер, інлайн-чат); лічильник витрат AI; календарне порівняння+MTD.
**Аналітика 2.0** (`stats.ts` канон): таби Статистики (Огляд/Категорії/Тренди/Мерчанти), дрил Витрати/Надходження з підзаголовком «що рахується», ₴-зведення, `period_mode`. Аналітика: середній чек (загальний + по категорії/мерчанту), топ-рухи категорій, прогноз витрат, найдорожчий день/днів без витрат, **кумулятивний потік** (`CumulativeChart`, з пунктирною прогноз-лінією), **heat-map по днях місяця**, **аналітика доходу** (`/analytics/income` — джерела/стабільність-CV/дельта), **вагомість §6**.
**Головна (дашборд):** BalanceCard, `QuickBar` (швидкі дії+детерміновані інсайти), Safe-to-spend (`/analytics/safe-to-spend`), MonthPulse (норма заощаджень+топ-категорії), ForecastCard, `UpcomingSubs` («скоро спишеться» 30 дн), `CapitalTrendCard` (`/analytics/capital-trend` — реконструкція власних коштів від `computeSummary`), CashflowCard, EnvelopeGrid.
**Бюджети:** картки-конверти (стан ok/warn/over, інлайн-ліміт, rollover — міграція 0017), **AI-бюджет-діалог** (`/budgets/chat`).
**Порадник:** структурований (runway, 6-міс тренд, бюджети, підписки, вагомість), історія (`advisor_history`), цитування операцій `[tx:ID]`.
**Репорти:** окрема сторінка `/reports` (+`:id`), Sonnet 5 cron тиждень+місяць, історія/ручна генерація, донат+тренд-лінія, вагомість, цитування (`citations.tsx renderRich`).
**Підписки:** каденція/категорія/note, факт vs план, ознака подорожчання, `SubsCalendar` (місячний календар списань), автодетект.
**Транзакції:** пошук/фільтр — комбіновані фільтри в URL (q/тип/рахунок/сума-діапазон/період/категорії).
**Чеки:** OCR-позиції аналізуються — `/analytics/receipt-items` (топ товарів), секція в Статистиці.
**AI 3.0 (2026-07-11):** моделі-за-задачею — `getTaskModel(env, task)` + `ai_model_<task>` (report/advisor/insight/chat/budget/group), роут `/settings/ai-models`, селектори в Setup з ціною MTok. Порадник «реальна ситуація» (ліквідна подушка / борг / чесний runway окремо). Репорти анти-«по книжці»: контекст +разові/регулярні +профіль-ситуація, прогноз на подушку (не нетто з кредиткою) + burn = середнє за 3 завершені міс (не ×30), навчання на `prior_reports`; **hero-блок Прогнозу** нагорі сторінки. AI-огляд (`generateInsight`) збагачений: вагомість, разові/регулярні, топ-аномалії, профіль. Нова аналітика Статистики: разові/регулярні (E1), радар аномалій, темп по категоріях, дрейф цін (`/analytics/price-drift`). Пояснювальні `InfoTip` на секціях Статистики/Репортів.
**Фідбек live-тесту #1–#7 (2026-07-11):** #1 разові/регулярні ловлять підписки (`planned_id` в `isRecurringExpr`); #2 радар/темп проєктують лише регулярне (разове плоско, `mostly_oneoff` поза аномаліями); #5 канонічний `nextChargeUnix` (period_count) — квартальна підписка не «спливає» щомісяця; #3 роль рахунку `investment` + `ai_note`, `fundsBreakdown` (подушка без інвестицій, інвест-резерв окремо), крипта=investment, картка «Інвест-резерв» у пораднику (⚠️ 0018); #4 бюджет-чат показує реальну помилку + не губить пропозиції, редизайн AI-плану (сер.→пропозиція з дельтою); #7 `name_locked` — ручна назва авторитетна, «Моя нотатка» видима (⚠️ 0019); #6 репорт «Категорії» рендерить детерміновані суми/дельти (AI-нотатка за назвою), prev=0→«новий». **Хвости (відкладено):** «AI думає між сторінками», косметика календаря підписок — у ROADMAP.
**Хвости-фікс (2026-07-12):** (а) **Прогноз «зі здоровим глуздом»** — `stats.projectSpend()` замінив наївний темп `spent/elapsedFrac` (роздував рано в місяці / лумпи): прогноз = «вже витрачено + історичний залишок», лумпи (1-2 великі операції: податок/оренда/заправка — детект `n<=1 OR biggest≥55%`) НЕ екстраполюються, кеп 3× звичного. Застосовано в `/analytics/patterns` (per-cat + аномалії + бейдж «не щоденне» через `lumpy`), `/analytics/forecast` (бленд темп↔історія 3 міс), Stats-огляд (проєкція лише ≥40% періоду). Прибирає «10к податків» / «2500 транспорт». (б) **AI-надійність** (порадник/бюджети «не валід») — корінь = обрив по `max_tokens`: `parseJson` тепер має `repairTruncatedJson` (рятує обірваний масив, відкидає неповний хвіст, дозакриває дужки — покрито тестом), ретрай ескалує токени при `stop_reason=max_tokens`, ліміти підняті (advisor/budget-plan 2200, budget-chat 1400), inline-помилка в блоці «Поради на твоїх числах». Потребує live-перевірки з реальним ключем. (в) **Порадник +1 lvl:** «Історія порад» тепер має тренд-графік runway + дельти runway/burn у знімках; смуга «зміни від минулого разу» над порадами (порівняння з попереднім знімком); кнопка «Очистити» (DELETE `/advisor/history`); знімок історії зберігає `cushion`, глибина 12→24. Без міграцій. (г) **Консистентність місячних сум (8к vs 12500):** новий канон `categoryMonthlyLevels()` (див. §Канонічне) — рента/підписки тепер = поточний рівень (останні платежі) скрізь однаково (Патерни/Порадник/Бюджети), не 6-міс-середнє в одному екрані й 90д÷3 в іншому. (д) **UI:** `.btn.sm` + `.section-head > .btn` авто-право — виправлено оверсайз-кнопку в заголовках секцій (Advisor).

**Продуктові фічі (2026-07-14):** (а) **Сторінка мерчанта** `/merchant/:name` — уся історія витрат, тренд 6 міс (`AreaChart`), KPI (усього/операцій+перша покупка/середній чек/категорія+частка), список операцій. Ендпоінт `/analytics/merchant?name=` (канон stats.ts). Вхід: Топ-мерчантів Статистики (лінк-рядки замість інлайн-дрилу) + назва мерчанта в TxDetail. Без міграцій. (б) **What-if симулятор** (`WhatIf.tsx` на Пораднику) — повзунки «зрізати категорію на N%» → миттєвий перерахунок runway/заощаджень; клієнтський, cushion/burn з `/advisor` + `usual` з `/analytics/patterns`. (в) **Цілі: ставка накопичення** — у `GoalCard` «Щоб устигнути — відкладай X/міс» = залишок ÷ місяців до дедлайну (фіча цілей 0007 вже була; додано лише date-математику). (г) **Layout Порадника** — виправлено головну діру: `.advisor-grid.single` не мав CSS → грід був 2-кол з 1 дитиною (права половина порожня). Тепер справжній 2-кол дашборд (`.advisor-main` поради + `.advisor-rail` розбивка/огляд/історія). (д) **Чат — фікс висоти/вводу:** блок фіксованої висоти зі скролом всередині (не росте сторінкою), рейл лишається; textarea вирівняно з `+`/send, скролбар лише на максимумі; `--content-w` ширший на великих екранах. Деталі — `DESIGN.md §Журнал`.

**Чат: агентні tool-use + Порадник-косметика (2026-07-14):** (а) **Tool-use** — чат має інструменти до ПОВНОЇ бази операцій поза фіксованим контекстом: `query_spend` (сума витрат/доходу за період + фільтр категорія/мерчант + групування), `find_transactions` (конкретні операції з id), `list_categories`. Транспорт — `runToolConversation` (`ai.ts`, цикл tool_use→tool_result, кеп 5 ходів, фінал без tools; кожен хід у лічильник). SQL інструментів — `runFinanceTool`/`financeChatTools` (`advisor.ts`, канон `SPEND_WHERE`/`INCOME_WHERE`/`mult`). Модель сама рахує дати відносно «сьогодні» → «скільки на таксі влітку торік» працює. Нові структуровані виклики з інструментами роби через `chatAdvice(..., {tools, executor})`. (б) **Порадник:** runway-метрики `1fr 1fr`→`auto-fit` KPI-стрічка (заповнили пусте місце). ⏳ live-звірка tool-use.

**AI 4.0 — §A2+§A1 (2026-07-19):** (§A2) **Фікс цін AI-лічильника** (`ai.ts`): `priceFor()` date-aware — Sonnet 5 вступна `$2/$10` до 01.09.2026 UTC, після стікер `$3/$15`; cache-write множник за TTL (читає `usage.cache_creation.ephemeral_5m/1h`, 5хв=1.25×/1год=2×, фолбек агрегат×2 бо `buildSystemPrefix` пише `ttl:"1h"`); `now` прокинуто в `callCostUsd`/`recordUsage`. (§A1) **Шар фактів** (⭐, міграція **0020**, `category_id` nullable → глобальні факти): факт про світ доходить до ДВОХ місць — наратив (`advisor.activeFacts`→`collectFinanceSnapshot.context.facts`, бачать Порадник і Чат) І число (`stats.categoryMonthlyLevels`→`applyFactAdjustments`, ЄДИНЕ місце override; лише `confirmed_at IS NOT NULL`; multiplier/delta_minor). Чат-tool `remember_fact` (AI пропонує unconfirmed, ефект детерміновано через `find_transactions`). Гейт підтвердження: CRUD `listFacts/addFact/confirmFact/deleteFact` + ендпоінти `/facts*`; UI `FactsCard` на рейлі Порадника (ручна форма + «Застосувати»). **⏳ чекає live** (метро 8→30 має однаково рухати usual/burn/runway; перетин із §A4 tool-use). (§A3) **web_search у чаті**: `ai.ts webSearchTool(model)` (варіант за моделлю; `allowed_domains` офіційні — bank.gov.ua/minfin/tax), `runToolConversation` отримав `serverTools` + обробку `pause_turn`. **⏳ live.**

**Дрібні фічі (2026-07-19):** (а) **Підписки** — річна вартість (місяць×12) у hero, секція «Найдорожчі» (топ-3), детект «мертвих» (бейдж «не видно списань» коли активна >60 дн і `actuals.count===0`) — усе клієнтське в `Subscriptions.tsx`. (б) **CSV-експорт** — `GET /export/transactions.csv` (BOM+CRLF, сума у валюті рахунку, опційні from/to) + кнопка «CSV» у page-head Транзакцій.

**Чат-порадник lvl-up (2026-07-14):** чат = «фінменеджер зі стажем». (а) **Контекст = ЄДИНЕ джерело** `collectFinanceSnapshot` (див. §Інваріанти) — чат отримує все, що Порадник (розбивка коштів/рахунки з ролями+нотатками, канонічний burn/runway, підписки, бюджети, вагомість, тренд, разові, `upcoming_charges` найближчі списання). Виправляє баг «домисленої подушки $780» + розсинхрон runway чат↔Порадник. (б) **Персона** (`chatAdvice`): досвідчений фінменеджер — радить рішення/тайминг/пріоритет платежів, пояснює поля коштів; графіки/таблиці ЛИШЕ коли треба (макс 1/відповідь, не спам). (в) **Таблиці** `[table:…]` у `markdown.tsx` поряд із `[chart:…]`. (г) **UX чату** (`Chat.tsx`): кілька розмов (localStorage-рейл, авто-заголовок, видалення, міграція старого `mt-chat`), textarea з автовисотою (Enter/Shift+Enter), скрол пінить моє повідомлення вгору, копі/регенерація. (д) **Модель чату обирається** в Налаштуваннях (task `chat`, окремо від `advisor`). ⏳ live-звірка з реальним ключем. **Відкладено:** стрімінг (SSE), агентні tool-use, пусте місце на екрані Порадника.

**Аналітичний шар (2026-07-20).** (а) **Індекс фінздоровʼя під'єднано до UI** — бекенд `financeHealth`
(`/analytics/health`) існував, але фронт юзав гірший клієнтський плейсхолдер-скор. Тепер `HealthIndexCard`
тягне `useGetHealthQuery` (runway / норма заощаджень / борг-дохід / стабільність → зважений скор 0..100 + band);
формулу звірено з зовнішніми джерелами (подушка 3-6 міс, заощадження 20%, DTI). **Тренд** (⚠️ міграція 0022
`health_history`): ендпоінт пише скор за добу (upsert) і вертає `trend` 45 дн → спарклайн + дельта в картці.
(б) **Порадник розбито на таби** (`stat-tabs`/`?tab=`): «Поради» (runway/What-if/поради/AI-огляд/історія) та
«Стан фінансів» (Індекс здоров'я на всю ширину + Факти ∣ Корпус знань). (в) **Спарклайни** — `Sparkline.tsx`
(міні SVG 6 міс, проп `goodUp`) + `/analytics/spark`; у рядках Топ-категорій і Топ-мерчантів Статистики
(ховаються <640px). (г) **Cashflow-календар** — `CashflowCalendar.tsx` (таб «Стан фінансів» + замінив
`SubsCalendar` на Підписках): місячна сітка очікуваних списань по днях + проєкція подушки наперед
(провали ліквідності). Ендпоінт `/analytics/cashflow-calendar` — ВСІ входження через крокування
`nextChargeUnix`, `end_date` для розстрочок. **Обмеження: аутфлоу-only**, планового доходу в моделі нема.
(д) **Прогноз із діапазоном довіри** — `/analytics/forecast` вертає `projectedLow/High` (σ місячної історії,
звужена на решту місяця), `ForecastCard` показує «≈ 12–15к ₴».

**UI-раунд (2026-07-20, друга частина).** (а) **Cashflow-календар читається без наведення:** назви списань
(до 2) з сумами тепер прямо в клітинці дня; повний список — у власному поповері (`.cf-pop`) на hover/focus/click,
із підсумком дня та проєкцією подушки на кінець дня. Нативний `title` прибрано (неможливо стилізувати,
не показував суми окремих платежів). День зі списаннями став `<button>` (клавіатура + `aria-label`).
Проєкція балансу перерахована ОДНИМ проходом (була O(n) на клітинку → O(n²)). Бекенд не змінювався —
`/analytics/cashflow-calendar` уже віддавав `title`/`amount`/`kind`. (б) **Rollover-конверти видимі:**
перенесений залишок став бейджем `.bc-carry` у шапці картки (був рядком у лейблі чекбокса), у смузі —
засічка `.bc-tick` на межі базового ліміту; «rollover» → «переносити залишок». (в) **Подорожчання підписки
в грошах:** бейдж показує абсолютну дельту й вплив на рік («↑ +50 ₴ · +600 ₴/рік») замість голого відсотка;
інлайн-хардкод `#e5484d`/білий текст замінено на токен-класи `.sub-badge.up/.down` (ламався в темній темі).
(г) **Тех-борг layout-transition закрито:** `.bc-bar`/`.env-bar` анімують `transform:scaleX`, `.wd-bar` —
`scaleY` (замість `width`/`height` — layout-thrash), усі з `prefers-reduced-motion`. Стовпчик дня має
мінімальний масштаб 0.02, бо `min-height` трансформ не рятує.

**🔔 Центр сповіщень — повністю (2026-07-20, ⚠️ міграція 0023).** Стрічка того, що система
«хоче сказати». **ЄДИНЕ джерело — `worker/lib/notify.ts`** (генерація + CRUD + prefs + TG-пуш +
ретеншн); ендпоінти `/notifications*` — лише транспорт; AI-промт — у `ai.ts` (`generateNotifyObservations`),
як усі інші.

**14 типів. 13 детермінованих:** `report` (витяг + лінк на `/reports/:id`) · `deadline` (списання
плану ≤3 дн, сума через `plannedUAH` §CUR-PLAN; **+ платіж по кредитці** — картки з `payment_day` +
використаним кредитом, той самий kind, `entity=account`) · `anomaly` (темп категорії — `categoryMonthlyLevels`
+ `projectSpend`, канон Радара) · `budget` (≥90% / ≥100% конверта по `SPEND_WHERE`) · `price_up`
(`plannedActuals`, абсолютна дельта + вплив на рік) · `liquidity` (подушка − планові списання в мінус,
вікно 45 дн, ОДИН прохід) · `big_tx` (≥3× середнього чека категорії) · `duplicate` (той самий мерчант
+ сума у вікні доби) · `health_drop` (−8 п. проти запису ≥5 дн тому, `health_history`) · `goal_risk`
(прогрес відстає від часу >15 п.п. або дедлайн ≤7 дн) · `dead_sub` (план активний >60 дн без жодного
фактичного списання) · `win` (категорія ≥30% нижче звичного — **єдиний позитивний тип**, бо стрічка
з самої тривоги перестає відкриватись) · `todo` (≥10 витрат без категорії за 30 дн). **+ `ai`** —
Haiku (`AiTask "notify"`, перемикається в Налаштуваннях) по `collectFinanceSnapshot`: модель НЕ рахує,
лише називає те, що вже пораховано канонічно; ≤2 події/добу; промт прямо забороняє нові числа й
дозволяє порожню відповідь.

**Правила, які тримати:**
- **`dedup_key` UNIQUE + `INSERT OR IGNORE`.** Крон щодня бачить ті самі події. Ключ МУСИТЬ містити
  період актуальності (`anomaly:<cat>:<YYYY-MM>`, `deadline:<id>:<дата>`, `dead_sub` — місяць,
  `todo`/`goal_risk` — тиждень), інакше подія більше ніколи не повториться.
- **Ліміт рахується по СТВОРЕНИХ** (`MAX_PER_RUN=15`), не по переглянутих чернетках — інакше десяток
  уже наявних зʼїдав би квоту й глушив справді нові події.
- **AI-гілка має власний запобіжник вартості:** `dedup_key` рятує лише БД, а виклик моделі стався б
  однаково → перевірка «чи вже є `ai:<сьогодні>:%`» перед запитом. Без неї кнопка «Перевірити зараз»
  палила б токени на кожен клік.
- **`big_tx`/`duplicate` свідомо БЕЗ `STATS_JOINS`** — це сигнали про ОДНУ ОПЕРАЦІЮ, а не агрегат:
  спліт ділить операцію для категорійної аналітики, але банк списав одну суму, її й показуємо (`TX_SPEND`).
- **`big_tx` виключає регулярне через `isRecurringExpr`** — інакше оренда 12 500 ₴ летіла б у стрічку
  щомісяця як «велика витрата» (спіймано на реальних даних remote D1).
- **TG-пуш — лише `severity >= warn`.** Якщо слати все, сповіщення почнуть ігнорувати, і важливе
  загубиться разом з рештою. `pushed_tg_at` захищає від повторів.
- **Ретеншн 90 дн — лише ПРОЧИТАНІ.** Непрочитане не видаляємо: інакше можна втратити те, чого не бачив.

**Полірування за першим живим прогоном (2026-07-20):** (а) `report` — вікно 14 дн→**3 дн, ≤2 шт**
(перший прогін вивалював увесь архів: 4 однакові «Готовий тижневий репорт» поспіль), заголовок
тепер несе період — інакше їх не розрізнити. (б) `anomaly` — поріг **`MIN_USUAL` 500 ₴/міс**: при
мізерному `usual` відсоток вибухає й нічого не означає («Підписки 1 613 ₴ проти звичних 99 ₴ — 1629%»);
плюс коли спрацював кеп (`projected === spent`) — формулювання в МИНУЛОМУ часі («вже вище звичного»),
бо перевитрата вже сталась, а не прогнозується. (в) **`anomaly` глушиться, коли на ту саму категорію
є `budget`-подія** — обидві казали те саме тими самими цифрами («Транспорт 1 314 ₴» двічі); бюджет
конкретніший, бо має ліміт для порівняння. (г) AI-промт: вимога природної української (title —
іменникова фраза) і заборона внутрішніх термінів (`optional`/`burn`/`runway`) — модель видала
«Мініатюрний дохід vs квартира не робить».

Крон **`0 6 * * *`** (≈08:00 Київ) = генерація + TG-пуш + ретеншн одним проходом; гілки ізольовані
(впала одна — решта доїде, текст у `skipped` і в тості). UI: `/notifications` (групи по днях, чипи-фільтр,
тумблери типів, «прочитано», «Перевірити зараз»), дзвіночок у `Layout` — `Link` із РЕАЛЬНИМ лічильником
(був статичний `dot-badge`, що світився завжди) + polling 10 хв. **Відкладено:** Web-Push (PWA) — у ROADMAP.

**§REFUND + індекс на Головній + бюджет події (2026-07-21, ⚠️ міграція 0025).**
(а) **Рефанди більше не дохід** — див. §Канонічне «§REFUND». Найбільша правка цифр за весь проєкт:
дохід −10 776 ₴, витрати −2 533 ₴ на реальних даних. Кожен варіант запиту прогнано на remote D1
(що саме вважається рефандом, дохід до/після, розбивка по категоріях).
(б) **Індекс фінздоровʼя на Головній** — `HealthMini` на рейлі дашборда: той самий `/analytics/health`
і той самий скор, що в повній картці Порадника (двох різних «індексів» бути не може); тут лише
gauge + band + спарклайн-тренд, увесь блок — посилання на «Стан фінансів». `Gauge` отримав проп `size`
(був жорстко 130px) + клас `.sm` — геометрія масштабується у viewBox, а текст усередині ні, тому
герой-число зменшується окремо.
(в) **Бюджет на подію/подорож** (міграція **0025** `event_groups.budget`) — СВІДОМО окрема колонка,
а не рядок у `budgets`: та таблиця прив'язана до категорії + періоду, а подія не має ні того, ні того.
`PATCH /events/:id`, смуга на картці групи + блок на сторінці події (мова конвертів: ok/warn/over).
⚠️ Заразом закрито валютну дірку: `/events` фільтрував `currency_code = 980`, тобто валютні витрати
групи НЕ рахувались (для подорожі — найгірше можливе місце), а `EventDetail` рахував суму на клієнті
тим самим фільтром → сторінка й список показували РІЗНІ числа. Тепер обидва беруть серверний
₴-агрегат (`uahMult`).

**Автобюджет + історія курсів + bulk (2026-07-20, ⚠️ міграція 0024).**
(а) **Автобюджет із історії** — `GET/POST /budgets/auto` + `AutoBudget.tsx` (Бюджети, над AI-планом).
Детерміновано, без AI: ліміт = канонічний `categoryMonthlyLevels` мінус `trim`% (перемикач 0/5/10/15/20),
округлення до 50 ₴. **`importance='essential'` НЕ ріжемо** — ліміт = рівень як є: урізаний бюджет на
оренду/продукти неможливо виконати, він червонітиме з першого дня й знеціниться. GET лише ПРОПОНУЄ,
POST застосовує обране (чекбокси) — щоб один тап не переписав уже налаштовані конверти; наявний ліміт
показано в рядку («буде замінено»).
(б) **Історія курсів** (міграція **0024** `rate_history`) — `snapshotRates()` у добовому й
тижневому кроні; `ratesForDays()` (finance.ts) віддає курс на кожну дату з переносом останнього
відомого вперед. Нетворт тепер рахує кожну точку курсом ТІЄЇ дати, а не сьогоднішнім;
caveat про курс показується ЛИШЕ коли історія не покриває період (раніше він був завжди —
після накопичення історії це була б неправда в інший бік). Ретроспективно не заповнюється
(минулих курсів нема) → фолбек на поточні + `covered:false`. `ratesForDays` мовчки деградує,
якщо таблиці ще нема на remote — нова аналітика не має ламати вже робочий графік.
(в) **Bulk-дії: вагомість і теги** — `/transactions/bulk` отримав `importance` (валідація рівня;
`null` = зняти override, успадкувати від категорії) і `tag_ids` (**ДОДАЄ**, не заміщає: гуртова
дія не повинна знести поштучно розставлені теги). **§FK-GUARD:** `INSERT OR IGNORE` гасить лише
конфлікт УНІКАЛЬНОСТІ, а не FK — один неіснуючий id тега завалив би весь батч
(`FOREIGN KEY constraint failed`, перевірено на D1), тож теги фільтруються по наявних категоріях
перед записом.

**Нетворт + Ctrl-K + збережені фільтри (2026-07-20).**
(а) **Нетворт у часі** — `/analytics/networth` + `NetworthCard` (таб «Стан фінансів»). Відрізняється
від `capital-trend` (одна лінія нетто): тут ВИДНО СКЛАД — подушка / інвестиції / борг (борг стеком
УНИЗ, нетворт лінією поверх). Реконструкція йде **поточково по рахунках**, бо знак рахунку вирішує,
куди він іде (подушка чи борг) — зводити перед реконструкцією не можна; складання cushion/debt/investment
повторює `fundsBreakdown` (§R3), інакше «зараз» на графіку не збіглося б із Порадником. **Дві межі
точності віддаються клієнтом у `caveats` і друкуються під графіком:** курси ПОТОЧНІ (історичних нема,
тож рух курсу виглядає як рух грошей) і рахунки без історії операцій (крипта/ручні) назад ПЛОСКІ.
(б) **Командна панель Ctrl-K / ⌘K** — `CommandPalette.tsx` + `GET /search` (мерчанти з агрегатом,
категорії, операції; сторінки й дії статичні на клієнті). Відкривається шорткатом або подією
`mt:cmdk`. Інпут-пошук у топбарі **замінено кнопкою**, що відкриває панель: два пошуки поруч були
зайвим вибором, а `kbd ⌘K` на кнопці — єдиний спосіб дізнатись про шорткат.
**⚠️ `LIKE` для кирилиці:** SQLite згортає регістр ЛИШЕ для ASCII — `LOWER('Сільпо')` = `'Сільпо'`
(перевірено на D1), тож `LIKE '%сільпо%'` не знаходив НІЧОГО. Варіанти регістру складаємо в JS
(`toLocaleLowerCase("uk")` тощо) і матчимо через OR. **Це стосується будь-якого пошуку по тексту
в цьому проєкті — не пиши наївний `LIKE`/`LOWER` для української.**
(в) **Збережені фільтри Транзакцій** — `SavedFilters.tsx` + `/settings/saved-filters` (app_state,
без міграції). Зберігаємо САМ QUERY-РЯДОК, а не розібрані поля: фільтри й так живуть в URL, тож нове
поле фільтра почне зберігатись автоматично. Збіг назви = перезапис (щоб не плодити «Робочі (2)»).

**Порівняння місяців + fallback поради (2026-07-20).** (а) **Таб «Порівняння» у Статистиці** —
`MonthCompare` (Stats.tsx): два селекти місяців (24 назад) → діфф по категоріях. Бекенд НЕ мінявся:
`/analytics/compare` від початку приймав дві незалежні пари меж (`from/to` + `bfrom/bto`), тож канон
спільний із «цей період проти минулого». Сортування рядків — за БІЛЬШОЮ з двох сум (категорія, що
зникла, мусить лишитись видимою — її зникнення і є відповіддю). Попередження, коли один із місяців —
поточний незавершений. `DeltaChip` отримав проп `goodUp` (надходження: зростання = зелене) — міняє
лише КОЛІР, не число: підміна a/b показала б «+20% доходу» як «−20%».
(б) **Детермінований fallback поради** — `fallbackAdvice()` (advisor.ts) на тих самих канонічних
числах (`collectFinanceSnapshot`): runway, найбільша категорія з ефектом −15% (+`create_budget`),
необовʼязкові витрати (§6), перевитрачені бюджети, підписки/рік, списання за 7 днів.
`/advisor/generate` віддає його замість 502, коли нема ключа / AI впав, із `fallback_reason`.
**НЕ зберігається** в `app_state.advisor` і не пише історію — інакше слабша детермінована порада
затерла б останню нормальну AI-пораду; клієнт тримає її в стейті. UI позначає `.fb-note`, бо
непозначений fallback читається як повноцінний розбір і збій лишається невидимим.

**🐞 Валюта планів + FK на enrich (2026-07-20).** Обидва знайдені з реального фідбеку, обидва —
клас «мовчазна брехня в числі».
- **§CUR-PLAN:** `period_amount` сумувався без `currency_code` у 5 місцях (див. §Інваріанти). CLOUDFLARE $5
  показувався як «5 ₴» у «Скоро спишеться», і та сама помилка текла в підсумок 30 днів, Cashflow-календар,
  прогноз місяця, safe-to-spend та AI-контекст. Введено канонічні `plannedUAH`/`sumPlannedUAH`;
  `/planned/upcoming` тепер віддає `currency_code` + `amount_uah`, календар — `amount` (₴) + `amount_orig`.
  UI показує «$5» із ₴-підписом.
- **§FK-GUARD:** enrich падав на `FOREIGN KEY constraint failed`, бо писав вигаданий моделлю `category_id`
  (id-дірки від видалених категорій). `existingCategoryIds()` валідує категорію й теги перед записом.
  Помилку побачили лише тому, що `errText` уже показував справжній текст (до фіксу було `[object Object]`).

**🐞 Регресія §SPLIT + обробка помилок (2026-07-20).** Два баги з одним корінням — німота.
- **Корінь:** §SPLIT переписав `amountSum()` на `EFF_AMOUNT = COALESCE(sp.amount, t.amount)`, але **5 запитів
  лишились без `STATS_JOINS`** → D1 кидав `no such column: sp.amount`. Підтверджено на remote D1.
  Місця: `api.ts` (розбивка по групах-подіях у `/analytics/breakdown`; дрил бакета «Перекази і зняття» —
  підкатегорії + мерчанти), `advisor.ts` `collectFinanceSnapshot` (події), `insight.ts` (події).
  Оскільки всі вони — гілки одного `Promise.all`, ОДИН впалий запит валив цілий ендпоінт: звідси
  «статистика всюди пуста», а через `collectFinanceSnapshot` — ще й Порадник із Чатом.
  **Фікс:** додано `STATS_JOINS` + `EFF_AMOUNT` у фільтри + `COUNT(DISTINCT t.id)`. Обидва полагоджені
  запити прогнані на remote D1 — вертають реальні дані.
- **Чому не спіймали раніше:** SQL — це рядок, `tsc`/`build` його не перевіряють. → доданий
  `scripts/check-stats-sql.mjs`, вшитий у `npm run check` (див. §Робочий процес крок 5).
- **Друга половина — помилки були невидимі:** Статистика не мала жодної гілки `isError` (порожньо й тихо),
  а `toast.error(String(e))` на RTK-обʼєкті друкував `[object Object]`. Повний фікс — §Обробка помилок.

**Статистика +5 · скелетони (2026-07-23).** (а) **Новий канонічний ендпоінт `/analytics/monthly-history`**
(`?months=`, дефолт 6): spend/income по місяцях через `spendSum`/`incomeSum`+`STATS_JOINS`, пропущені
місяці добиваються нулями, останній місяць — поточний (частковий). Прогнано на D1. (б) **5 нових
робочих графіків/статистик на Статистиці:** `MonthlyHistory` (таб «Тренди» — composed bars spend/income
+ net-лінія + норма заощаджень по місяцях, з нового ендпоінта); `SpendDonut` (таб «Категорії» — пончик
топ-7+«інші», реюз `report-donut`/`donut-center`); `AvgCheckByCategory` (таб «Категорії» — рейтинг
середнього чека spent÷n, ловить дорогі окремі покупки); `TopSpendDays` (таб «Тренди» — топ-5 найдорожчих
днів із денних бакетів, клік→дрил дня). Три останні — клієнтські з `overview` (нуль зайвих запитів,
гарантовано робочі). (в) **Скелетони завантаження** — `Skeleton`/`SkeletonRows`/`StatsSkeleton`
(`components/Skeleton.tsx`) + CSS `.skeleton` (shimmer, глушиться глобальним `prefers-reduced-motion`):
головний loading Статистики й «Завантаження…» у `CatDrill`/`SliceDrillPanel` замінені на форму-натяк
майбутнього контенту. Інші сторінки — у ROADMAP.

**Рахунки lvl-up + фікс пончика (2026-07-23, 0 міграцій).** (а) **Огляд коштів** — `FundsOverview`
на сторінці Рахунків через новий `GET /accounts/funds` = канон `fundsBreakdown` (§R3): подушка /
інвестиції / борг / нетворт + композитна смуга. **СВІДОМО не рахуємо композицію на клієнті** — інакше
розсинхрон із Порадником (той самий баг, що «домислена подушка»). (б) **Підсумок секції** (сума ₴ у
заголовку) + **сортування карток за ₴-величиною** спадно (`uahValue` дзеркалить `shown`: кредитка =
власні). (в) **Архів/видалення** (нова функція): `GET /accounts/archived`, `PATCH /accounts/:id/active`
(is_active, історія tx лишається), `DELETE /accounts/:id` (ЛИШЕ ручний і ЛИШЕ без операцій — інакше
сирітські tx; mono — тільки архів). Секція «Архів» згорнута з «Відновити». (г) **Інваріант дизайну:**
native `<select>` у формі додавання → `Select`. (д) Скелетон замість «Завантаження…». (е) **Баг пончика**
(`SpendDonut`/Reports): центр-лейбл (герой-сума) проступав крізь тултіп — центр у DOM після графіка, тож
без z-index малювався поверх. Фікс: `.donut-center{z-index:0}` + `.report-donut .recharts-tooltip-wrapper{z-index:5}`.

**Рахунки lvl-up ч.2 — історія балансів + кредитка (2026-07-23, ⚠️ міграції 0026/0027).**
(а) **Історія ручних балансів** (0026 `account_balance_history`): зріз балансу пишеться при
створенні/редагуванні ручного рахунку (`recordBalance`, один запис/день — перезапис останнього того ж
дня). `/analytics/networth` тепер КРОКУЄ ручні/крипто-рахунки по цих зрізах замість плоского назад
(`manualBalanceAt`: останній зріз ≤ t, до першого — найраніший відомий). Caveat «плоскі назад» лишається
ЛИШЕ для ручних БЕЗ жодного зрізу. Гілка обгорнута try/catch — до міграції на remote деградує до
плоского. (б) **Справжня кредитка** (0027 `accounts.statement_day/payment_day/min_payment`): поля в
редакторі рахунку (показуються при `credit_limit>0`), рядок «платіж до N числа» на картці. Нагадування —
через наявний `deadline` (не новий kind): `draftDeadlines` додає гілку по картках з `payment_day` +
використаним кредитом (`ліміт − balance > 0`), нагадує за ≤3 дні, `severity:"warn"` (у TG-пуш),
`dedup_key deadline:credit:<id>:<YYYY-MM>`, сума = `min_payment` або весь борг. `nextMonthlyDay` рахує
дату (UTC-полудень, кеп на останній день короткого місяця). Гілка в try/catch (колонки можуть ще не бути).

**Рахунки ч.3 — модалка додавання + історія на картці (2026-07-23, 0 нових міграцій).**
(а) **Додавання рахунку — модалка** `AddAccountModal` (кнопка «＋ Додати рахунок» у page-head, не форма
внизу сторінки). Пресети-«типи»: Дебетова/Кредитна/Валютна/Готівка/Крипта/Інвестиції/Банка — виставляють
`type`/`role`/валюту/кредит-поля, юзер може змінити валюту+роль. Кредитна: вводиш ліміт+борг → `balance =
ліміт − борг` (own = −борг, як у mono). Одразу поле **AI-опис** (`ai_note`) при створенні. `POST
/accounts/manual` розширено: `type` (whitelist manual_card|crypto|cash|jar) + `role`/`credit_limit`/`ai_note`.
(б) **Історія балансу на картці** — міні-`Sparkline` тренду 6 міс (нове `GET /accounts/history` — місячні
зрізи з `account_balance_history`, carry-forward; лише ручні рахунки з історією; try/catch до 0026 на remote).
(в) **Менше скролу:** кнопка додавання вгорі, тумблер «Сховати нульові (N)» (ховає 0-баланс, кредитку з
боргом лишає). (г) **Графік на сторінці:** `NetworthCard` (склад нетворту 12 міс) у секції «Історія капіталу».
(д) **Розподіл по валютах** — `CurrencyBreakdown` (₴-величина кожної валюти, смуга+легенда) над секціями.

## 🔴 Ops / деплой (рутина КОРИСТУВАЧА)
1. **Міграції на remote:** застосувати всі до **0027** (**0023 `notifications` — Центр сповіщень;
   0024 `rate_history` — історія курсів; 0025 `event_groups.budget` — бюджет події; 0026
   `account_balance_history` — історія ручних балансів (нетворт крокує); 0027
   `accounts.statement_day/payment_day/min_payment` — умови кредитки + нагадування про платіж**) (0018 `accounts.role`+`accounts.ai_note` §R3; 0019 `transactions.name_locked` §R7; 0020 `facts` §A1; **0021 `tx_splits` — спліт транзакції §SPLIT; 0022 `health_history` — тренд індексу здоров'я**) включно перед деплоєм: `npm run db:migrate:remote`. ⚠️ Локальний `db:migrate:local` зараз падає на переприкладанні 0018 (`role` вже є — розсинхрон стану local D1); нові таблиці валідовано напряму через `wrangler d1 execute --local`. (Решта останніх фіч — AI 3.0/4.0-наратив, аналітичне ядро, бюджети — **міграцій не потребують**, крім 0018/0019/0020.)
2. **Деплой:** `npm run deploy` (потрібен `wrangler login`).
3. **Секрети** (`npx wrangler secret put`): `MONO_TOKEN`, `ANTHROPIC_API_KEY`, `WEBHOOK_SECRET`, `APP_PASSWORD`; бот — `TG_BOT_TOKEN`, `TG_SECRET`, `TG_CHAT_ID`.
4. **Перший запуск даних** (Налаштування): рахунки → вебхук → бекфіл ~90 дн (1 req/60с) → курси → перекази.
5. **Стан (звірено 2026-07-20):** git ініціалізовано, гілка `main`, робоче дерево чисте.
   Міграції **0020/0021/0022 вже застосовані на remote** (`facts`/`tx_splits`/`health_history` — перевірено
   запитом до `sqlite_master`). **0023 `notifications` — ЩЕ НІ**, застосувати перед деплоєм (інакше
   `/notifications` віддасть `no such table`; помилку буде видно завдяки `ErrorNote`, але стрічки не буде).
   **0024 `rate_history` — теж ще ні**; без неї нетворт просто працює на поточних курсах (деградація
   передбачена в `ratesForDays`), але історія курсів не накопичується — а вона пишеться лише вперед.
   **0025 `event_groups.budget` — теж ще ні**; без неї сторінки Груп упадуть на `no such column: budget`
   (деградації тут свідомо нема — колонка читається прямим `SELECT e.*`).
   **0026/0027 — теж ще ні** (застосовані лише на local через `wrangler d1 execute --local`); обидві гілки
   в try/catch, тож без них нетворт працює по-старому (ручні плоскі) і кредит-нагадування мовчить —
   нічого не падає, але функції не активні, доки не накотиш на remote.
   Local D1 має лише тестові дані (~5 tx) — перевіряти на remote.

## 🧭 AI 4.0 — ТЗ наступної великої фази (спроєктовано 2026-07-15, НЕ реалізовано)

> **Це дизайн, не історія.** Картки-виконавці (Ціль·Файли·Кроки·Готово-коли) — у `ROADMAP.md §Черга`,
> вони посилаються сюди за обґрунтуванням. Прочитай цей розділ ПЕРШИМ, перш ніж брати картку.
> Джерело API-фактів нижче — скіл `claude-api` (звірено 2026-07-15). **Не переписуй їх із пам'яті:**
> кілька пунктів суперечать «загальновідомому» й були перевірені саме тому.

### §A1 — Шар фактів («метро подорожчало з 8 до 30 ₴») ⭐ головна задача
> **✅ РЕАЛІЗОВАНО 2026-07-19 (⏳ live).** Деталі — §Зроблено «AI 4.0 — §A2+§A1». Нижче — початковий дизайн-довідник.
> Рішення на старті: `category_id` **nullable** (глобальні факти дозволені).

**Задача користувача:** написати системі факт про світ («з 15.07 метро 30 ₴ замість 8»), щоб вона
його запам'ятала, пояснювала й **рахувала з ним**.

**🔑 КОРІНЬ, який легко не помітити: це НЕ проблема знань AI, а проблема ЧИСЛА.**
Згодуй моделі ідеальний факт — чат скаже «транспорт подорожчає», а Порадник покаже той самий runway.
Бо `categoryMonthlyLevels()` (`stats.ts:156`) бере `K=6` і рахує **лише ПОВНІ місяці**
(`from` = початок місяця −6, `to` = `monthStart`):
- зміна тарифу 15 липня не впливає ні на що — липень ще не завершився;
- у серпні вона впаде в середнє по 6 міс, 5 з яких — по старій ціні;
- «Транспорт 805 ₴/міс» доживе до осені, а `sumLevels()` → burn → `runway = подушка ÷ burn`
  тягтиме застарілу цифру.

→ **Факт мусить дійти до ДВОХ місць:** у текстовий контекст AI (щоб пояснив) І в числовий конвеєр
як явний override рівня категорії (щоб runway був чесний). Інакше це косметика, а розсинхрон
«чат каже одне, `/advisor` рахує інше» — це рівно той баг, який уже ловили (§Інваріанти,
«домислена подушка $780»).

**Модель даних — два рівні на один запис** (нова міграція 0020):
```sql
CREATE TABLE facts (
  id INTEGER PRIMARY KEY,
  text TEXT NOT NULL,              -- «Метро подорожчало 8 → 30 ₴»
  effective_from INTEGER NOT NULL, -- unix; з коли діє
  expires_at INTEGER,              -- NULL = безстроково
  category_id INTEGER,             -- NULL = глобальний факт («я звільнився»)
  -- рівень 2 (опційний): коригування ЧИСЛА. Лише воно рухає burn.
  adjust_kind TEXT,                -- 'multiplier' | 'delta_minor' | NULL
  adjust_value REAL,               -- 3.75  |  +100000 (копійки/міс)
  confirmed_at INTEGER,            -- ⚠️ NULL = НЕ застосовувати до чисел
  source TEXT,                     -- 'user' | 'ai_proposed'
  created_at INTEGER NOT NULL
);
```
- **Рівень 1 (наратив):** вливається в `collectFinanceSnapshot()` (`advisor.ts`). Це ключ —
  снапшот уже є ЄДИНИМ джерелом контексту для Порадника Й Чату (§Інваріанти), тож факти
  автоматом бачать обидва, без розсинхрону. **Не додавай факти окремо в чат.**
- **Рівень 2 (коригування):** тільки `confirmed_at IS NOT NULL` має право посунути
  `categoryMonthlyLevels`. Реалізувати як override ПОВЕРХ мапи рівнів у самій
  `categoryMonthlyLevels()` — це ЄДИНЕ джерело місячної суми (§Канонічне), обхід = новий розсинхрон.

**🔒 Гейт підтвердження — не прибирати.** Цифру пропонує AI, **застосовує користувач**.
Якщо модель сама тихо посуне burn — це знову «домислена подушка», але дорожча, бо тече в runway.
Звідки береться цифра: **не з голови AI**, а детерміновано з наявного інструмента
`find_transactions` — порахувати реальні поїздки метро за історією (мерчант/MCC) × нова ціна.
AI лише формулює: «за історією ти їздиш N разів/міс → +X ₴/міс. Застосувати з 15.07?».

**Вхідні точки:** (1) чат — новий tool `remember_fact` (AI пропонує, юзер тисне «застосувати»);
(2) ручна форма в Налаштуваннях/Пораднику. **Відкрите питання (вирішити на старті):**
`category_id` nullable — глобальні факти («я звільнився») без коригування числа, лише наратив.

### §A2 — Фікс цін AI-лічильника (2 однорядкові правки, S) 🐞
> **✅ РЕАЛІЗОВАНО 2026-07-19.** Деталі — §Зроблено «AI 4.0 — §A2+§A1». Нижче — початковий опис.

Знайдено при звірці зі скілом `claude-api`. Обидві — у `worker/lib/ai.ts`:
1. **Завищує Sonnet ~на третину.** `ai.ts:80` = `{in: 3.0, out: 15.0}`, але у Sonnet 5 діє
   **вступна ціна $2/$10 за MTok до 2026-08-31**. Зробити ціну date-aware (після 31.08 → $3/$15).
2. **Занижує cache-write.** `ai.ts:92` рахує `cacheWrite * p.in * 1.25`, але `buildSystemPrefix`
   пише кеш із `ttl: "1h"`, а **година = 2× write** (5 хв = 1.25×). Enrich-шлях недорахований.

### §A3 — Актуальні дані про світ (web_search, S)
> **✅ РЕАЛІЗОВАНО 2026-07-19 (⏳ live).** `ai.ts webSearchTool()` + `runToolConversation` serverTools/pause_turn; чат. Нижче — початковий дизайн.

Сервер-сайд інструмент Anthropic: оголошуєш у `tools`, виконується на їхній інфраструктурі,
результат приходить блоком у тій самій відповіді. **Ні парсерів, ні крону, ні Vectorize.**
- **Sonnet 5** (`advisor`/`chat`/`budget`) → `web_search_20260209` (динамічна фільтрація,
  `allowed_domains`, `max_uses`). **Haiku 4.5** → лише базовий `web_search_20250305`.
- `allowed_domains` обмежити офіційними джерелами (напр. `bank.gov.ua`), щоб не тягнути шум.
- `web_fetch_20260209` тягне **лише URL, які вже є в розмові** — як краулер не працює.
- **Приватність витримана:** знімок фінансів і так їде в Anthropic; пошук іде тим самим каналом,
  не третій стороні (пор. рішення прибрати clearbit — `DESIGN.md §Журнал 2026-07-05`).

### §A4 — ⚠️ Ризик наявного tool-use чату (перевірити ПЕРШИМ на live)

`thinkingOff()` (`ai.ts:41`) вимикає thinking для всього, що не Haiku — тобто **Sonnet 5 у чаті
працює з `thinking: {type: "disabled"}`**. Це свідомо (Sonnet 5 вмикає adaptive-thinking, коли поле
відсутнє). АЛЕ: за докою Anthropic **Sonnet 5 із вимкненим thinking РІДШЕ тягнеться до інструментів** →
`query_spend`/`find_transactions` можуть просто не викликатись, і модель відповість із фіксованого
контексту. Лікується явним нюджем у промті («коли відповідь залежить від даних поза контекстом —
ОБОВ'ЯЗКОВО виклич інструмент») або adaptive-thinking саме для чату.

### §A5 — Корпус знань (PDF/MD) — коли буде що класти

- **PDF читається нативно:** `{"type":"document","source":{"type":"base64","media_type":"application/pdf"}}`
  у user-контенті, **без beta-хедера**. Ліміти: 32 MB/запит, 600 сторінок — але **100 сторінок для
  моделей з 200k-контекстом (= Haiku 4.5)**; Sonnet 5 має 1M. `citations:{enabled:true}` дає посилання
  на сторінку — лягає на наявний патерн цитування `[tx:ID]`.
- **Векторний RAG НЕ потрібен:** кілька MD — це тисячі токенів. Правильний хід — кеш: у нас уже є
  `CACHE_GUIDE` з `cache_control` + `ttl:"1h"` (`ai.ts:558`), це готова полиця.
- **Мінімум кешу:** Haiku 4.5 = **4096 тк** (наш замір в `ai.ts:534` правильний, підтверджено).
  ⚠️ **Для Sonnet 5 мінімуму в доці НЕМА** (Opus/Haiku 4.5 = 4096, Fable 5/Sonnet 4.6 = 2048,
  Sonnet 4.5 = 1024). **Заміряти `count_tokens` перед тим, як вішати `cache_control` на Sonnet** —
  нижче порога кеш МОВЧКИ не працює.
- Економіка: read ≈0.1×, write 1.25× (5 хв) / 2× (1 год) → при 1-год TTL корпус окупається з 3-го виклику.
- **Структуровані виходи** (`output_config.format`, `strict:true`) підтримують Sonnet 5 І Haiku 4.5 —
  кандидат замінити хак `repairTruncatedJson` (§Зроблено, 2026-07-12). Не скасовує ліміт `max_tokens`.

**Порядок робіт:** ✅ §A2 + ✅ §A1 + ✅ §A3 (2026-07-19) → ✅ §A4 (tool-use live-підтверджено 2026-07-20) → ✅ §A5 (вбудований корпус знань у чаті, 2026-07-20). **Фаза AI 4.0 закрита** (лишились опційні хвости §A5: юзер-аплоад PDF/MD, нативний PDF-блок — у ROADMAP).

**§A5 — Корпус знань (вбудований, 2026-07-20):** curated-довідник `worker/lib/knowledge/*` (3 доки: принципи фінансів / методологія застосунку `UI=AI` / інвестиції-крипта; ФОП свідомо поза скоупом). `KNOWLEDGE_CORPUS` вливається в `chatAdvice` (`ai.ts`) СТАБІЛЬНИМ блоком з `cache_control ttl:1h` ПЕРЕД динамічним контекстом — корпус+персона (~5-7k тк) читаються з кешу ≈0.1× (персона раніше слалась без кешу — тепер дешевше). Лише чат. UI — `KnowledgeCorpusCard` (рейл Порадника) через `GET /knowledge`. Новий корпус знань пиши як `KnowledgeDoc` у `knowledge/`, НЕ роздувай динамічний блок. ⏳ live: `cache_read_input_tokens>0` на 2-му+ повідомленні.

## ➡️ Що далі
**Наступна велика фаза — AI 4.0: читати §A1–A5 вище**, картки-виконавці в `ROADMAP.md §Черга`.
**UI/UX-роботи — читати `DESIGN.md` ПЕРШИМ** (там же §2 «🔒 Падінг картки» — правило системи),
зміни фіксувати в його «Журналі рішень». Решта форвард-роботи — у **`ROADMAP.md`**.
**Перед деплоєм:** міграції на remote застосовані до **0022** включно (звірено 2026-07-20) — див. §Ops.